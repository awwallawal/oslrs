/**
 * Home Feature - Homepage components and sections.
 *
 * @example
 * // Lazy load in App.tsx
 * const HomePage = lazy(() => import('./features/home'));
 */

export { default } from './HomePage';
