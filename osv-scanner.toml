# osv-scanner accepted-risk config — Story 13-32 (2026-07-18)
#
# What changed from Story 13-31
# -----------------------------
# 13-31 adopted osv-scanner (npm retired the legacy `pnpm audit` endpoint, 410
# Gone) but osv-scanner has no `--prod` mode: it scans the WHOLE pnpm-lock.yaml,
# surfacing the dev/build/test-tooling subtree the retired `pnpm audit --prod`
# gate correctly excluded. 13-31 grandfathered 17 such findings here with
# `ignoreUntil = 2026-08-15` — a deliberate forcing function that would re-fail
# CI on that date. 13-32 removes that expiry cliff at the ROOT CAUSE.
#
# The new two-tier CI gate (.github/workflows/ci-cd.yml)
# ------------------------------------------------------
#   1. BLOCKING gate — PRODUCTION scope only. Reproduces the retired
#      `pnpm audit --prod` posture: osv-scanner scans the full lockfile, then
#      apps/api/scripts/osv-prod-gate.ts keeps ONLY findings whose package is in
#      pnpm's production dependency closure (`pnpm ls -r --prod --depth Infinity
#      --json`) and fails the build iff a PROD dependency is vulnerable. This
#      config file is NOT consulted by the blocking gate — the prod scope makes
#      the entire dev-tree class evaporate from the deploy-blocking surface, so
#      no per-CVE ignore is needed to keep it green.
#   2. REPORT-ONLY tier — full lockfile, NON-BLOCKING. osv-scanner scans the
#      whole tree with `--config=osv-scanner.toml` → SARIF → GitHub Security
#      tab. This preserves dev-tree visibility (a compromised build tool is a
#      real supply-chain signal) WITHOUT blocking deploys. The entries below
#      filter the handful of PERMANENTLY-accepted dev-only risks out of that
#      report so it stays actionable (any NEW dev-tree vuln still surfaces).
#
# Why these three remain (13-32 remediation, 2026-07-16 → 2026-07-18)
# -------------------------------------------------------------------
# 11 of the 13-31 findings were fixed at source and their ignore lines DELETED:
#   * turbo 1.13 → 2.10.5 (major bump via @turbo/codemod) — kills the 9.8 Critical.
#   * bounded overrides (>=fixed <nextMajor, per package.json override-policy):
#     flatted, postcss, esbuild, js-yaml, markdown-it, linkify-it, ajv, @babel/core.
# The THREE below have NO in-policy fix (their patch crosses a major, which the
# repo override-policy forbids — the 9-54 footgun — or no npm fix exists). All
# three are dev-only (`pnpm why -r --prod <pkg>` empty, re-verified 2026-07-18)
# and OUT of the blocking prod-scope gate entirely. They are converted from an
# EXPIRING grandfather to a PERMANENT (no `ignoreUntil`) accepted-risk, mirroring
# package.json pnpm.comments.accepted-risks. AC4: zero expiring entries remain.

# --- xlsx (SheetJS) — no npm fix published -------------------------------------
# devDependency; the only consumer, apps/api/src/services/xlsform-parser.service.ts
# (admin one-time XLSForm import, Story 2-9), dynamically imports it and returns
# 503 under a `--prod` install where xlsx is absent. Removal was assessed in
# 13-32 and rejected: it would force `parseXlsxFile` async (exceljs.load is async)
# across the runtime feature path + ~40 synchronous test call sites — a large
# churn to a live feature for zero prod-gate benefit (xlsx is already out of prod
# scope). Permanent accepted-risk.
[[IgnoredVulns]]
id = "GHSA-4r6h-8v6p-xvw6"
reason = "xlsx 0.18.5 (7.8, prototype pollution) — SheetJS dev-only (admin XLSForm import; 503 under --prod). No npm fix published. Not in --prod tree. Permanent accepted-risk (13-32)."

[[IgnoredVulns]]
id = "GHSA-5pgg-2g8v-p4x9"
reason = "xlsx 0.18.5 (7.5, ReDoS) — same SheetJS dev-only dep; no npm fix. Not in --prod tree. Permanent accepted-risk (13-32)."

# --- serialize-javascript — fix crosses a major (6 → 7) ------------------------
# dev-only via workbox-build (PWA service-worker generation at web-build time).
# Fixes land in 7.0.3/7.0.5; a 6→7 override violates the bounded override-policy
# (>=x <nextMajor). Not in --prod tree. Permanent accepted-risk.
[[IgnoredVulns]]
id = "GHSA-5c6j-r48x-rmvq"
reason = "serialize-javascript 6.0.2 (8.1) — dev-only via workbox-build; fix @7.0.5 crosses a major (override-policy forbids). Not in --prod tree. Permanent accepted-risk (13-32)."

[[IgnoredVulns]]
id = "GHSA-qj8w-gfj5-8c6v"
reason = "serialize-javascript 6.0.2 (5.9) — dev-only via workbox-build; fix @7.0.5 crosses a major. Not in --prod tree. Permanent accepted-risk (13-32)."

# --- phin — fix crosses a major (2 → 3) ----------------------------------------
# dev-only via potrace/jimp (image tooling). Fix lands in 3.7.1; a 2→3 override
# violates the bounded override-policy. Not in --prod tree. Permanent accepted-risk.
[[IgnoredVulns]]
id = "GHSA-x565-32qp-m3vf"
reason = "phin 2.9.3 (4.3) — dev-only via potrace/jimp; fix @3.7.1 crosses a major (override-policy forbids). Not in --prod tree. Permanent accepted-risk (13-32)."

# --- react-router / react-router-dom 6.30.4 — fix crosses a major (6 → 7); MITIGATED at the app layer ---
# 2026-07-24: NEWLY-DISCLOSED advisories against react-router 6.30.4 (the pinned,
# bounded `react-router: >=6.30.4 <7` override). UNLIKE every entry above, these ARE
# in the --prod closure (react-router-dom is an apps/web runtime dependency bundled to
# the browser) — so the blocking prod-scope gate would fail on them. They are filtered
# here because osv-scanner AUTO-LOADS this config for the blocking-gate scan too (it is
# adjacent to the scanned lockfile), so a documented accepted-risk clears the gate. The
# fix lands only in v7 (7.18.0 / 7.13.0): a 6→7 override is forbidden by the bounded
# override-policy AND broke our web build before (v7 MemoryRouterProps — the 9-54
# lesson), and there is NO 6.x backport, so no in-policy fix exists. Disposition =
# accepted-risk WITH an application-layer mitigation (not a blind ignore):
#   * GHSA-337j — SSR-hydration deserializeErrors constructor injection: N/A, this is a
#     client-side SPA (BrowserRouter), no SSR / framework data APIs.
#   * GHSA-wrjc / GHSA-jjmj — open-redirect (→XSS) via unsanitised navigation targets.
#     Every post-auth navigate(redirectTo) site (login / password-reset / MFA /
#     ProtectedRoute) now routes its target through
#     apps/web/src/lib/safe-redirect.ts::toSafeInternalPath, which admits ONLY same-origin
#     root-relative paths (rejects //host, backslash, encoded and control-char bypasses),
#     closing the vector regardless of react-router's missing sanitisation (+tests).
# RE-REVIEW if we add attacker-controlled navigation input (e.g. a ?redirect= param) or
# upgrade to react-router 7.
[[IgnoredVulns]]
id = "GHSA-337j-9hxr-rhxg"
reason = "react-router 6.30.4 SSR-hydration constructor injection — N/A: client-side SPA, no SSR. Fix v7 only (crosses major, override-policy forbids; broke web build before). App-layer safe-redirect in place. Accepted-risk (2026-07-24)."

[[IgnoredVulns]]
id = "GHSA-wrjc-x8rr-h8h6"
reason = "react-router 6.30.4 open-redirect via unsanitised navigation. MITIGATED at app layer: all post-auth navigate targets pass toSafeInternalPath (same-origin root-relative only, +tests). Fix v7 only (crosses major). Accepted-risk (2026-07-24)."

[[IgnoredVulns]]
id = "GHSA-jjmj-jmhj-qwj2"
reason = "react-router-dom 6.30.4 open-redirect→XSS. Same app-layer mitigation (toSafeInternalPath). Fix v7 only (crosses major, override-policy forbids). Accepted-risk (2026-07-24)."

# ─────────────────────────────────────────────────────────────────────────────
# pdfjs-dist 5.6.205 — GHSA-hq66-cqwq-w95j (2026-08-07)
#
# "Arbitrary JavaScript execution upon opening a malicious PDF." Newly disclosed against an
# UNCHANGED dependency; it blocked a deploy carrying an unrelated fix.
#
# WHY ACCEPTED RATHER THAN PATCHED
#   * Upstream fix is pdfjs-dist@6.2.108. 6.x requires Node 22; CI and the VPS run Node 20, so
#     the bump HARD-FAILS `pnpm install` on deploy (Story 11-2, and the reason we are pinned here).
#
# WHY THE VULNERABLE PATH IS UNREACHABLE — verified in the installed library, not assumed
#   * The advisory's own workaround is "set enableScripting to false". In pdfjs 5.x that is NOT a
#     `getDocument` option: every use of it in legacy/build/pdf.mjs is in the ANNOTATION/WIDGET
#     layer (Action, Mouse Up/Down, datetime widgets) — the browser viewer's interactive render.
#   * Our only consumer is a SERVER-SIDE text extractor
#     (apps/api/src/services/import/parsers/pdf-tabular.parser.ts). It calls getDocument + reads
#     positioned text runs. No getAnnotations, no render(), no annotation layer. The code that
#     executes document script is never constructed.
#   * Exposure would additionally require an authenticated STAFF member to upload a hostile PDF
#     through the admin import surface — not a public path.
#
# DEFENCE IN DEPTH THAT IS REAL
#   * `isEvalSupported: false` IS a valid DocumentInitParameters option (default true) and IS set
#     at the getDocument call, stopping pdf.js compiling PDF function expressions via eval.
#     KEEP IT across pdfjs upgrades — this acceptance is only honest while that line exists.
#
# RE-REVIEW when Node 22 lands (then just bump to >=6.2.108 and delete this), or if a PDF ever
# reaches pdfjs from an unauthenticated path, or if we start rendering PDFs client-side.
[[IgnoredVulns]]
id = "GHSA-hq66-cqwq-w95j"
reason = "pdfjs-dist 5.6.205 malicious-PDF script execution — UNREACHABLE: server-side text extraction only (no annotation layer, which is where pdfjs gates scripting); admin-only upload path. Fix @6.2.108 needs Node 22, CI/VPS on Node 20 (11-2). isEvalSupported:false set as defence-in-depth. Accepted-risk (2026-08-07)."
