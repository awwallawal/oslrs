# Story 13-29: Wizard — evaluate calculated-field section relevance reactively (fix the two-pass survey loop) + acquisition-question prominence

Status: done

<!-- Authored 2026-07-11 by Bob (SM) via *create-story. EMERGENT from the Dry-run #2 registration (Awwal, live): every public registration hits a confusing two-pass loop — the summary appears with Submit greyed + "Some required survey questions still need an answer… Go back and fill survey", and only AFTER looping back do the occupation questions (main_occupation/employment_type/years_experience) appear, THEN Submit enables. Root-caused (agent-traced, confirmed): grp_labor is gated by group relevance `${age} >= 15`, `age` is a CALCULATED field from `dob`, and the FormRenderer evaluates calculated fields for section-navigation from a STALE mount-time useState snapshot (FormRenderer.tsx:136) taken before dob is answered → grp_labor is skipped on pass 1 → completeness is only checked at Review (review-completeness.ts:59) where age finally computes → grp_labor is now required+unanswered → bounce-back. NOT a form-content bug (13-19's age gate is correct); a FormRenderer calculated-field REACTIVITY/ordering bug. Also folds in the "How did you hear about us?" (13-1) prominence (currently a plain optional select below the summary — easy to miss). LAUNCH-RELEVANT: on the paid-conversion path. -->

## Story
As **a member of the public registering via the wizard**,
I want **the survey to show all its relevant questions in one natural forward pass (and the "how did you hear" question to be visible)**,
so that **I reach the summary once with everything answered and a live Submit — instead of a confusing "go back and fill survey" loop that makes me abandon.**

## Context & Evidence (agent-traced, confirmed 2026-07-11)
- **Symptom (live, Dry-run #2):** in the Public Core survey, ~3-4 questions render, the SUMMARY appears with Submit greyed + "Some required survey questions still need an answer before you can save. Go back and fill survey." The go-back link returns to Main Occupation/Job Title, revealing another ~3-4 questions (main_occupation/employment_type/years_experience) that DIDN'T show the first time; then Submit turns bright (enabled). Every registration.
- **Root cause (a calculated-field reactivity/ordering bug in the FormRenderer):**
  - `grp_labor` carries group relevance `showWhen: ${age} >= 15` (correctly converted from the xlsform group relevance — `xlsform-to-native-converter.ts:184-189`, preserved by 13-19). `age` is a CALCULATED field = `int((today() - dob) div 365.25)`.
  - `FormRenderer.tsx:136` computes the calculated fields (`seedEval = withCalculatedFields(initialResponses ?? {}, …)`) in a **`useState` INITIALIZER** — a one-time snapshot at section mount, BEFORE `dob` is answered. So `age` is `undefined` → `${age} >= 15` is false → `getNextVisibleIndex` (`FormRenderer.tsx:137-143`) SKIPS `grp_labor` in the forward flow. The user never sees the occupation questions on pass 1.
  - Completeness is evaluated ONLY at the Review step (`review-completeness.ts:58-59`), which computes calculated fields across ALL answers for the first time → now `dob` is present → `age` computes → `grp_labor` is "visible + required + UNANSWERED" → `incompleteQuestionnaire=true` → Submit disabled + the go-back message (`Step5ReviewAndSave.tsx:245`, link at :195).
  - The go-back re-mounts the section; `initialResponses` now includes `dob` → the stale seed recomputes `age` → `grp_labor` renders (pass 2).
- **This is NOT 13-19** (13-19 stripped dead FIELD-level relevance; the `${age} >= 15` GROUP gate is intentional and correct). It's a renderer reactivity gap: **section relevance that depends on a calculated field is evaluated from a stale mount-time snapshot, and completeness is deferred to Review.**
- **Acquisition question (13-1):** "How did you hear about us?" renders at `Step5ReviewAndSave.tsx:211-237` as a plain optional `select` BELOW the summary table (gated by `ATTRIBUTION_ENABLED`), labeled "(optional)" — easy to miss on a scroll-to-submit. 13-1's whole point is channel attribution; a missed question = lost CPA signal for the paid launch.

## Acceptance Criteria
1. **AC1 — Calculated-field section relevance is reactive.** Entering `dob` makes `grp_labor` (and any section gated on a calculated field) appear in the SAME forward pass — section navigation recomputes visibility from the CURRENT answers (with calculated fields recomputed), not a stale mount-time `useState` seed. No dependence on a re-mount.
2. **AC2 — No spurious "go back and fill survey" for a straight-through registration.** A fresh public registration that answers every relevant question in order reaches the summary ONCE with Submit ENABLED — no phantom "required unanswered" from a section that was never shown. (Regression test reproducing the dob→age→grp_labor path.)
3. **AC3 — General, not grp_labor-specific.** The fix is in the renderer's calculated-field/relevance evaluation ordering, so ANY section gated on a calculated field re-evaluates as its inputs change (future-proof). Do NOT special-case age/grp_labor or weaken 13-19's correct `${age} >= 15` gate.
4. **AC4 — Legitimate skip still works.** An under-15 registrant (age < 15) still correctly SKIPS `grp_labor` (occupation not asked; guardian path) and reaches the summary without those questions marked missing — the gate must still HIDE when it should, just reactively.
5. **AC5 — Acquisition-question prominence (13-1).** "How did you hear about us?" is made prominent + legible on the review/summary step (clear position, readable label/affordance, not buried below the table as a faint optional select). Reconcile with 13-1's intent: it may be elevated toward prominent/soft-required while staying non-blocking per the 13-1 review's "never block the submit" guardrail. Confirm the exact treatment with PM.
6. **AC6 — Tests + suites green.** Renderer unit/integration test for reactive calculated-gate visibility (one-pass) + the under-15 skip; web component test for the acquisition-question placement. Full web (+ any api) suites green; tsc/eslint clean. A manual/e2e wizard pass confirms one-pass completion.

## Tasks / Subtasks
- [x] **Task 1 (AC1-AC4)** — fix the section-relevance evaluation so gates that depend on calculated fields re-evaluate reactively. **Root cause differed slightly from the premise:** the actual skip happens at the WIZARD level, not inside FormRenderer. `WizardPage.isStepSkippable` (the auto-skip decision for section STEPS) evaluated section visibility against the RAW `questionnaireResponses` (no `age`), while `deriveReviewCompleteness` evaluated against the calc-augmented map — so `grp_labor` was auto-skipped in the forward pass, then demanded at Review. Fix = extracted a pure `lib/section-relevance.ts` `isSectionStepSkippable()` that evaluates visibility against `withCalculatedFields(responses, form.calculations)` (mirroring `review-completeness.ts`), and pointed `isStepSkippable` at it. General (any calc-gated section), preserves 13-19's `${age} >= 15` gate, preserves the under-15 skip (age computes → hidden → still skipped, and completeness agrees).
- [x] **Task 2 (AC5)** — elevated the acquisition question on Step 5 from a faint "(optional)" select buried below the summary to a prominent, accessible `<section>` landmark (labelled region, base-weight semibold heading, larger control). Kept OPTIONAL + non-blocking per the 13-1 review guardrail (no submit gate).
- [x] **Task 3 (AC6)** — `section-relevance.test.ts` (9): calc-gated section visible when age≥15 / skipped when age<15 / skipped while dob unanswered / no-calc parity / no-form / head-step; PLUS an **agreement-invariant** block asserting skippability never contradicts `deriveReviewCompleteness` (the exact asymmetry that caused the loop). `Step5ReviewAndSave.test.tsx` +2 (AC5 prominence: accessible labelled region + still non-blocking). Existing 9-18 section-step + 13-1 attribution tests preserved green.

### Review Follow-ups (AI)
_Adversarial code-review (BMAD code-review workflow, 2026-07-12). Disposition: action items logged AND fixed automatically. No HIGH/CRITICAL — every AC implemented, no falsely-checked task, git↔File List clean (0 discrepancies)._
- [x] [AI-Review][MEDIUM] AC2 requested a wizard-level regression test for the dob→age→grp_labor one-pass path, but coverage was pure-unit only — the E5 integration test uses a question-level `showWhen` gate (no calculated field) and would pass with or without the fix, so the end-to-end draft-flush→skip-reevaluation WIRING was unguarded. **Fixed:** new `WizardPage.calcGatedSkip.test.tsx` drives the real `mergeFields`→re-render→`isStepSkippable` path (dob flushed on a separate event, mirroring FormRenderer.onAnswer, then Continue) — adult ⇒ grp_labor appears same pass / no bounce; under-15 ⇒ still skipped to Review. [apps/web/src/features/registration/pages/__tests__/WizardPage.calcGatedSkip.test.tsx]
- [x] [AI-Review][LOW] `isSectionStepSkippable` omitted `hideQuestionNames`, so a section composed entirely of wizard-prefilled (hidden) questions read as visible → not skipped → the user landed on FormRenderer's "No questions available" dead-end (pre-existing parity with the old inline logic). **Fixed:** threaded the prefilled-hide set (`draft.formData.prefilledQuestionNames`) through `isSectionStepSkippable`→`getVisibleQuestions`; +1 unit test (all-hidden ⇒ skippable, partial ⇒ not). Agreement with completeness preserved (prefilled = auto-answered ⇒ not "missing"). [apps/web/src/features/registration/lib/section-relevance.ts:47, WizardPage.tsx:322]
- [x] [AI-Review][LOW] AC5 named the acquisition region via a `<label>` styled as a heading — accessible name present, but no node in the heading outline (screen-reader `H` navigation skips it). **Fixed:** converted to a semantic `<h3 id="attribution-heading">`; the select keeps its accessible name via `aria-labelledby`. Existing AC5 + 13-1 tests still green. [apps/web/src/features/registration/pages/Step5ReviewAndSave.tsx:216]
- [~] [AI-Review][LOW] `isSectionStepSkippable` and `deriveReviewCompleteness` each default `today` to an independent `new Date()`; across an exact midnight boundary for a registrant turning exactly 15 the two could disagree by a day. **Won't-fix (documented):** negligible (both run within the same interaction's render pass); a shared per-render clock would have to be a `useCallback`/`useMemo` dep that changes every render, defeating memoisation for a non-issue — net-negative. Accepted.

## Dev Notes
- **The seam is `FormRenderer.tsx:136` (stale `useState` seedEval) + the section-navigation `getNextVisibleIndex` (:137-143) using it, vs the reactive `evalData` useMemo (:201-204) that already recomputes on `[formData, calculations]`.** The likely fix: drive section visibility/navigation off the reactive `evalData` (or recompute calc at navigation time), so `age` is fresh when `grp_labor`'s gate is checked. Confirm completeness (`review-completeness.ts`) and the section flow agree on the same computed answer-set.
- **Keep 13-19's `${age} >= 15` gate** — it is correct (occupation asks for adults; under-15s route to the guardian block). This story fixes WHEN/HOW the gate is evaluated, not the gate.
- **General fix preferred** — this class (section gated on a calculated field) will recur; fix the ordering once.
- **LAUNCH-RELEVANT / HIGH** — registrations complete today (via the loop), so not a hard blocker, but it's a confusing two-pass loop on the exact paid-conversion path the blast drives; abandonment risk. Prioritise with 13-27/13-25 in the pre-blast public-facing pass. PM to rule pre-blast vs fast-follow.
- **Acquisition prominence** rides along because it's the same Step-5 surface; keep it non-blocking (13-1 review guardrail).

### References
- [Source: apps/web/src/features/forms/components/FormRenderer.tsx:136 (stale useState seedEval), :137-143 (getNextVisibleIndex on stale seed), :201-204 (reactive evalData useMemo)]
- [Source: apps/web/src/features/registration/lib/review-completeness.ts:58-59 (first cross-form calc, at Review only)]
- [Source: apps/web/src/features/forms/utils/skipLogic.ts:117-120 (section visibility vs formData)]
- [Source: apps/web/src/features/registration/pages/Step5ReviewAndSave.tsx:195 (go-back onGoToStep), :211-237 (acquisition question), :245 (submit disabled)]
- [Source: apps/api/src/services/xlsform-to-native-converter.ts:184-189 (group relevance → showWhen, correct — 9-54 AC2)]
- [Source: 13-19 (the intentional ${age}>=15 grp_labor gate) · 13-1 (acquisition question / attribution) · 9-54 (native-form calculate evaluator)]

## Dev Agent Record

### Implementation Plan / Completion Notes
- **True root cause (refined from the story's FormRenderer:136 trace):** the section is skipped at the WIZARD step level, not inside FormRenderer. `WizardPage.isStepSkippable` (WizardPage.tsx:316-325) called `getVisibleQuestions(sectionQuestions, responses, form.sectionShowWhen)` with the RAW `draft.formData.questionnaireResponses` — so `grp_labor`'s section gate `${age} >= 15` was false (no computed `age`), the section step was auto-skipped by `advanceStep`, and only at Review (`review-completeness.ts:59`, which DOES `withCalculatedFields`) did `age` resolve → `grp_labor` became required+relevant+unanswered → the "go back and fill survey" bounce-back. The defect was the **asymmetry** between the two evaluators.
- **Fix (general, AC3):** extracted `lib/section-relevance.ts` → `isSectionStepSkippable(form, sectionId, responses, today?)`, which merges computed fields via `withCalculatedFields(responses, form.calculations, today)` before `getVisibleQuestions` — the SAME augmentation the completeness gate uses. `WizardPage.isStepSkippable` now delegates to it. No age/grp_labor special-casing; any section gated on a calculated field re-evaluates as its inputs change.
- **AC4 (under-15 skip preserved):** with `age` computed, a child (`age < 15`) still fails `${age} >= 15` → 0 visible questions → still skippable → still skipped; `deriveReviewCompleteness` agrees (section hidden → not required). Pinned by the agreement-invariant test.
- **AC5 (acquisition prominence):** `Step5ReviewAndSave.tsx` — the "How did you hear about us?" question is now a bordered, tinted `<section aria-labelledby>` landmark with a base-size semibold prompt and a larger select. Still OPTIONAL and never gates submit (13-1 review guardrail; `wizard-save-button` disabled state unchanged — it keys only on `incompleteQuestionnaire`).
- **Verification:** web tsc 0, eslint 0. Targeted registration+forms regression 413/413 green (incl. the 9-18 section-step auto-skip integration test, unchanged behavior for no-calc forms since `withCalculatedFields` is a no-op when `calculations` is empty). Full web suite run for DoD.
- **AC6 residual (manual/e2e) — TRACKED for close-out:** a live one-pass wizard registration through the Public Core (dob→age→grp_labor without a Review bounce) must be confirmed on the actually-pinned prod form. **Tracked in `docs/runbooks/pre-launch-operator-runbook.md` → Step 5b (launch-gate row) + `docs/runbooks/backfill-operator-residuals.md` → §D.** Standing automated guards meanwhile: the `section-relevance.test.ts` skippability↔completeness agreement invariant + `WizardPage.calcGatedSkip.test.tsx` (AI-Review M1: adult one-pass / under-15 skip). Note: `dob` is wizard-prefilled+hidden in prod (stamped on Step-4 mount), so the calc-timing path is only fully exercised by the manual run — see Step 5b for why.
- **Test-hygiene fix (post-implementation, 2026-07-12):** `Step5ReviewAndSave.test.tsx` emitted a benign but noisy `Query data cannot be undefined` stderr in the 13-1 and new 13-29 describe blocks — their `['wizard','lgas','public']` query mock (`fetchPublicLgas`) was a bare `vi.fn()` returning `undefined`, which TanStack Query rejects. Fixed by adding a top-level `beforeEach` seeding a default LGA mock (the two blocks that assert specific LGA names still `mockReset()` + reseed in their own `beforeEach`, which runs after and is unaffected). Warning gone; Step5 suite 18/18 green, eslint clean.

### File List
- `apps/web/src/features/registration/lib/section-relevance.ts` (new) — pure `isSectionStepSkippable` (calc-augmented section-step relevance)
- `apps/web/src/features/registration/lib/__tests__/section-relevance.test.ts` (new) — 9 tests incl. the skippability↔completeness agreement invariant
- `apps/web/src/features/registration/pages/WizardPage.tsx` — `isStepSkippable` now delegates to `isSectionStepSkippable`; import swap (`getVisibleQuestions` → `isSectionStepSkippable`)
- `apps/web/src/features/registration/pages/Step5ReviewAndSave.tsx` — acquisition question elevated to a prominent accessible region (AC5)
- `apps/web/src/features/registration/pages/__tests__/Step5ReviewAndSave.test.tsx` — +2 AC5 prominence/non-blocking tests
- `apps/web/src/features/registration/pages/__tests__/WizardPage.calcGatedSkip.test.tsx` (new, AI-Review M1) — wizard-level regression for the calc-gated one-pass flow (adult one-pass / under-15 skip)
- `_bmad-output/implementation-artifacts/sprint-status.yaml` — 13-29 status transitions
- `_bmad-output/implementation-artifacts/13-29-wizard-calculated-relevance-reactivity-and-acquisition-prominence.md` — story record

## PM Validation (John, 2026-07-11)

**Validated — approved. LAUNCH-RELEVANT/HIGH. The reactivity fix is the priority; acquisition prominence rides along.**

1. **Severity:** registrations complete today, so it's not a hard blocker — but a "go back and fill survey" loop on EVERY registration is a conversion killer on the paid-traffic path the whole blast exists to feed. That makes it effectively pre-blast: we're about to pay to send people into a confusing two-pass form. Put it in the pre-blast public-facing pass with 13-27/13-25.
2. **AC3 (general fix) + AC4 (under-15 skip still works) are the guardrails.** Don't special-case age; don't weaken 13-19's correct gate; and prove the legitimate skip still hides grp_labor for under-15s. A fix that makes grp_labor always show would be a different bug.
3. **AC1 is the crux** — section visibility must track the *current* answers (dob→age) reactively, not a mount-time snapshot. The trace points right at it (stale useState seed vs the reactive evalMemo already present). Should be a contained renderer fix.
4. **AC5 acquisition prominence — keep it non-blocking.** 13-1's review deliberately made "how did you hear" best-effort so a mandatory front-loaded question wouldn't self-own conversion days before spend. So: make it PROMINENT and legible, optionally soft-required, but do NOT hard-block submit on it. Prominence ≠ mandatory.
5. **Scope:** the two are bundled because they share the Step-4/5 surface and both are launch-UX; that's efficient, but the reactivity fix must not wait on the prominence polish — ship them together, prioritise AC1-4.

**No AC changes.** Dev-ready.

## Change Log
| Date | Change | By |
|------|--------|-----|
| 2026-07-11 | Story drafted via *create-story — the wizard's calculated-field-dependent section relevance (grp_labor gated on ${age}>=15, age from dob) is evaluated from a stale mount-time snapshot, so occupation questions are skipped on pass 1 and only surface after a "go back and fill survey" loop at Review. Fix = reactive section-relevance evaluation (general, preserves the under-15 skip + 13-19's gate) + elevate the 13-1 acquisition question's prominence (non-blocking). EMERGENT from the Dry-run #2 live registration. LAUNCH-RELEVANT. | Bob (SM) |
| 2026-07-12 | dev-story implemented (Amelia/Claude). Traced the true root cause to the WIZARD auto-skip (`WizardPage.isStepSkippable`), which evaluated section visibility against RAW responses while `deriveReviewCompleteness` used the calc-augmented map — the asymmetry that skipped `grp_labor` then demanded it at Review. Fix = extracted pure `lib/section-relevance.ts` `isSectionStepSkippable` evaluating visibility with `withCalculatedFields` (mirrors review-completeness); general, preserves 13-19's gate + the under-15 skip. AC5 = acquisition question elevated to a prominent accessible region, still non-blocking. Tests: +9 section-relevance (incl. skippability↔completeness agreement invariant), +2 Step5 AC5. web tsc/eslint clean; registration+forms 413/413 + full web suite green. | Amelia (Dev) |
| 2026-07-12 | Test-hygiene follow-up: silenced the `Query data cannot be undefined` stderr in `Step5ReviewAndSave.test.tsx` (13-1 + 13-29 blocks) by seeding a default `fetchPublicLgas` mock in a top-level `beforeEach`. No product-code change; Step5 18/18 green, eslint clean. | Amelia (Dev) |
| 2026-07-12 | Adversarial code-review (BMAD code-review workflow). 0 HIGH/CRITICAL; git↔File List clean; tsc/eslint/targeted-suites independently re-verified green. Findings logged + fixed: **M1** added the missing wizard-level regression (`WizardPage.calcGatedSkip.test.tsx` — proves the fix fires end-to-end through `mergeFields`→`isStepSkippable`, not just the pure fn); **L1** threaded `hideQuestionNames` through `isSectionStepSkippable` so an all-prefilled section auto-skips instead of hitting FormRenderer's "No questions available" dead-end (+1 unit test); **L2** acquisition prompt `<label>`→semantic `<h3>` (heading outline) with the select named via `aria-labelledby`. **L3** (midnight/age-boundary clock drift) accepted as won't-fix (negligible; a shared clock would defeat memoisation). Affected suites 35/35 green; tsc 0 / eslint 0. | Amelia (Review) |
