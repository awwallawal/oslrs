# Story 13-35: Wizard UX polish — skills-picker close affordance + prefilled-question framing

Status: done

## ⚠️ OPERATOR / ADJUDICATION RESIDUAL — read before commit + push

**The Playwright wizard e2e is the ONE layer verified by inspection, not execution.** Everything else in AC5 was run locally and is green (full web suite **2821 passed + 2 todo / 259 files**, `tsc --noEmit` clean, `eslint` clean). The e2e was not executed because it needs a booted API + DB + Vite dev server (`playwright.config.ts` `webServer`), which was out of scope for a review pass.

**Action on push:** confirm CI's **`smoke-e2e`** job goes green before deploy — it gates deployment, so this closes AC5's e2e leg. Do not treat AC5 as fully discharged until it passes.

**What was verified statically:** `e2e/wizard-registration.spec.ts` asserts none of the selectors this story changed — no combobox, prefilled-banner, or empty-state references; existing `data-testid`s were preserved and only new ones added.

**If `smoke-e2e` DOES red, start here (prime suspect, in order):**
1. **The landing-step correction effect** (`WizardPage.tsx:392-421`, added by review F1) rewrites `?step=` via `setSearchParams(..., { replace: true })` — and the spec asserts exact URLs (`toHaveURL(/[?&]step=0\b/)` etc. at `wizard-registration.spec.ts:153,172,177,182,243,264,279`). It should never fire on the head steps the spec walks: steps 0–2 are Basics/Contact/Consent, which have no `sectionId`, and `isSectionStepSkippable` returns `false` when `sectionId` is undefined (`section-relevance.ts:54`). Confirm that holds against the REAL pinned form rather than assuming it.
2. **`effectiveFormData`** (`WizardPage.tsx:362-375`, review F2) changed what `questionnaireResponses` is submitted and what the Review gate reads. Values are identical to what Step 4 stamps — it is the same pure `computePrefill`, not a second writer — but a golden-path submit assertion is where any divergence would surface.

Both are new navigation/submit behaviour on the public conversion path, which is why they are called out rather than assumed inert.

<!-- Authored 2026-07-18 by Bob (SM). Two wizard-frontend UX frictions on the public conversion path, bundled (same feature area, same deploy). (1) The skills picker (ComboboxMultiSelect, the searchable multi-select for the 150-skill taxonomy) only closes on an outside click — no discoverable "done", so it feels stuck, esp. on mobile where it covers the form. (2) The Public Core questionnaire duplicates identity/demographic questions the wizard already collected; the dedup (prefilledQuestionNames) correctly hides them, but the "collected earlier" framing reads as broken/empty. NOT close-on-select (skills is genuinely multi-select) and NOT removing the dedup (it's the load-bearing plumbing that populates raw_data for analytics — see 13-33). Frontend-only, no data-shape change. Pre-blast-relevant (conversion), not launch-gating. -->

## Story
As **a member of the public filling the registration wizard**,
I want **a skills picker I can clearly finish with, and a questionnaire that doesn't look empty/broken when it has already captured my details**,
so that **I move through the wizard confidently instead of feeling stuck or confused — protecting completion on the blast.**

## Context & Evidence (verified 2026-07-18)
- **Skills picker = multi-select, closes only on outside-click.** `ComboboxMultiSelect.tsx` renders `skills_possessed` (>20 options → searchable combobox with sector groups + chips). `toggleChoice` (:87) adds/removes without closing; the ONLY close path is the outside-click handler (:55-64). There is no in-dropdown "Done" and no Escape handler → on mobile the open panel covers the form with no obvious exit. Closing on each select would be WRONG (multi-select — users pick several skills), so the fix is a deliberate close affordance, not close-on-select.
- **Dedup "collected earlier" reads as empty/broken.** The Public Core form repeats wizard-collected identity/demographics (nin/name/dob/gender/lga); `Step4Questionnaire.tsx` auto-fills + hides them via `prefilledQuestionNames` (9-18 Pattern C / 13-23). A section that is all-prefilled would otherwise dead-end on FormRenderer's "No questions available" (:350). **13-29 already added all-prefilled-SECTION auto-skip** (its L1, via `isSectionStepSkippable` + `hideQuestionNames`), so the structural skip exists — but the remaining **banner wording still frames it as "collected earlier"**, which the user reports reads as confusing/empty rather than reassuring.
- **The dedup is load-bearing — do NOT remove it.** The auto-fill copies wizard values into `questionnaireResponses` → `raw_data`, which analytics reads (`raw_data->>'gender'`, `->>'dob'`, etc.; see 13-33 convergence). This story changes *framing*, never the prefill mechanism.

## Acceptance Criteria
1. **AC1 — Skills picker close affordance.** Add a discoverable, sticky **"Done ({n} selected)"** control in the dropdown that closes it, plus **Escape-to-close**; keep the existing outside-click close. Do NOT close on select (multi-select preserved). Keep search **focused after each selection** for fast multi-add. +tests (Done closes, Escape closes, select keeps open + refocuses search).
2. **AC2 — No dead-end in the wizard.** Confirm (and extend if any gap) that a fully-prefilled section — and the whole-questionnaire-all-prefilled case — never lands the user on FormRenderer's "No questions available"; it is skipped to Review (or a clean confirmation). Verified with a wizard-level test (builds on 13-29's `isSectionStepSkippable`; do not duplicate its section logic).
3. **AC3 — Reframe the prefilled banner as positive.** Where some questions are prefilled, the banner reads as a reassuring confirmation — e.g. **"✓ We already have these from your earlier answers — no need to re-enter"** — not a bare/alarming "collected earlier" list or an empty screen. The dedup/prefill mechanism itself is unchanged (load-bearing; 13-33).
4. **AC4 — Accessibility + mobile.** The Done control is reachable and visible on small screens (sticky within the dropdown, not below the fold); dropdown has proper roles/`aria`, Escape works, and focus is managed on open/close. Chips remain keyboard-removable.
5. **AC5 — Green, UX-only.** Full web suite + wizard e2e pass; tsc + eslint clean; **no data-shape change** (skills still stored as `skills_possessed` array; `prefilledQuestionNames` unchanged; `raw_data` keys identical). No API/DB change.

## Tasks / Subtasks
- [x] **Task 1 (AC1, AC4)** — `ComboboxMultiSelect`: sticky "Done ({n})" button, Escape handler, retain-focus-after-select, a11y roles; unit tests.
- [x] **Task 2 (AC2)** — Verify the all-prefilled skip covers section + whole-questionnaire; add a wizard-level regression if a gap exists (else document that 13-29 covers it).
- [x] **Task 3 (AC3)** — Reframe the prefilled banner copy (Step4Questionnaire / FormRenderer) to a positive confirmation; keep prefill logic untouched.
- [x] **Task 4 (AC5)** — Full web suite + e2e + tsc + eslint; confirm no data-shape drift.

### Review Follow-ups (AI) — adversarial code-review 2026-07-25, ALL FIXED
Severity order. Every item in THIS list was found against the `review`-state tree (i.e. in the dev's implementation) and fixed in the same pass; the suite was re-run green after each. A **second** list below records what a re-review of those fixes turned up — kept separate so the two are not conflated.

- [x] **[AI-Review][HIGH] H1 — AC2 was NOT satisfied; the all-prefilled dead-end still fired on the first forward pass.** `WizardPage.unreachableQuestionNames` derived the hide-set from `draft.formData.prefilledQuestionNames`, which is stamped by `Step4Questionnaire`'s effect — i.e. by the very step the skip is meant to avoid. On a fresh registration that array is empty when `handleContinue` evaluates `isStepSkippable`, so an all-prefilled section read as "has visible questions", the wizard navigated into it, and `FormRenderer` painted "No questions available". Proven RED with a wizard-level probe before fixing. Contrast 13-34's geopoint half, which works precisely because `unionGeopointNames` is FORM-derived. **Fix:** extracted the Pattern C derivation to `lib/wizard-prefill.ts` (`computePrefill` + `buildWizardIdentitySignature`) and made `WizardPage` derive the hide-set itself, unioning the stamped names for resumed drafts. [`WizardPage.tsx:328-346`, `lib/wizard-prefill.ts`]
- [x] **[AI-Review][HIGH] H2 — the AC2 test was not the wizard-level test the AC demands, and was structurally unable to catch H1.** `section-relevance.test.ts` hand-supplied the hidden set production had not computed yet, and hand-built a 4-step model commented "Mirrors WizardPage.steps" while `buildSteps` has THREE head steps. **Fix:** added `WizardPage.prefilledSectionSkip.test.tsx` driving the REAL wizard (3 tests in this pass; 6 after the follow-up below), sibling to 13-34's geopoint file; corrected the misleading comment in the unit test. **RED-verified:** reverting H1 fails the new file 3/3 while the old unit test stays 12/12 green — the exact blind spot. → [[pattern-ship-a-fix-that-never-fires]]
- [x] **[AI-Review][HIGH] H3 — the new refocus-after-select was a keyboard regression.** `toggleChoice` refocused the search box unconditionally, so a keyboard user activating option #5 was thrown back to the top of the widget and had to re-Tab the whole 150-option list to pick a second skill. **Fix:** refocus is now pointer-only (`e.detail > 0`); keyboard activation leaves focus on the option. +2 tests (pointer refocuses, keyboard does not). [`ComboboxMultiSelect.tsx:95-113, 243`]
- [x] **[AI-Review][MEDIUM] M1 — invalid ARIA: `role="listbox"` owned non-option children, including the new Done button.** Untyped `<div>` sector wrappers broke option ownership, and the Add-custom + Done blocks sat inside the listbox (ARIA permits only `option`/`group`; axe `aria-required-children` fails). It put AC1's close affordance where AT option-navigation may never surface it — undermining AC4 for the users it targets. **Fix:** listbox scoped to options only, sector wrappers `role="group" aria-label`, footer moved outside (which also makes Done *always* visible rather than sticky-within-scroll). +1 test asserting listbox child roles. [`ComboboxMultiSelect.tsx:212-232, 320`]
- [x] **[AI-Review][MEDIUM] M2 — focus was dropped to `<body>` on close; AC4's "focus is managed on open/close" was half-done.** Escape from an option, or clicking Done, unmounted the focused element. Non-trivial: the naive fix re-fires the input's `onFocus` and re-opens instantly. **Fix:** `closeDropdown` restores focus behind a `skipOpenOnFocusRef` latch, plus an explicit `onClick` re-open (focus alone can't re-open when focus never left). +3 tests. [`ComboboxMultiSelect.tsx:60-84, 190-200`]
- [x] **[AI-Review][MEDIUM] M3 — two divergent close paths + state leak.** The outside-click effect still inlined its own close instead of calling `closeDropdown`, and neither cleared `customValue`, so abandoned custom-skill text survived close/re-open. **Fix:** single `closeDropdown(returnFocus)` used by both paths; clears `customValue`. +1 test. [`ComboboxMultiSelect.tsx:60-96`]
- [x] **[AI-Review][MEDIUM] M4 — AC5's e2e leg was asserted, not run.** Completion Notes reasoned the Playwright flow was "unaffected" rather than reporting a run. **Resolution:** verified by inspection that `e2e/wizard-registration.spec.ts` asserts none of the touched selectors (no combobox / banner / empty-state references), and the run itself is gated by CI's `smoke-e2e` job on push. Stated as a CI residual rather than claimed locally.
- [x] **[AI-Review][LOW] L1 — `combobox-listbox-${question.name}` is not unique** on a page rendering the same schema twice (the component is shared with `SupplementalSurveyPage`). **Fix:** `useId()`.
- [x] **[AI-Review][LOW] L2 — Escape inside the custom-skill field nuked the whole dropdown** instead of cancelling just the nested entry. **Fix:** first Escape cancels the custom field, second closes the dropdown. +1 test.
- [x] **[AI-Review][LOW] L3 — stale doc comment** still claimed "collapsible sector headers" the component has never implemented (the story noted the gap and descoped the feature). **Fix:** comment corrected to describe what the code does.
- [x] **[AI-Review][LOW] L4 — banner copy.** "**Click** Back" is desktop-centric for a mobile-first blast audience → "Use Back"; the leading `✓` inside `role="status"` is now an `aria-hidden` span so the announcement starts at "We already have your…" instead of "check mark".

### Review Follow-ups (AI) — SECOND pass, 2026-07-25 (review of the fixes above)
**These were NOT defects in the dev's implementation** — they were found by re-reviewing the H1 fix itself, and **F2 is a regression H1 introduced**. Recorded separately so the epic retro doesn't misattribute them. Severity order; each RED-verified individually (not as a batch).

- [x] **[AI-Review][HIGH] F1 (follow-up) — the H1 fix did not cover ARRIVAL on a skippable step, only navigation into one.** `isStepSkippable` was consulted solely by `handleContinue` / `handleBack` / the step indicator — never at mount. So a **resumed draft whose saved `currentStep` points at an all-prefilled section still painted "No questions available"**, and this is not hypothetical: pre-fix builds *did* land users there and `persistStepToDraft` mirrors the step into the draft, so real drafts record exactly that index (draft population is live; timers expire 2026-08-28). **Fix:** a landing-step correction effect in `WizardPage` that forward-skips when the current step is skippable, `replace`-navigating (system correction, stays out of history) and bumping the ceiling exactly as `handleContinue` does. Cannot loop — `advanceStep` only returns non-skippable indices and stops at Review. Safe under 9-54 AC6.1: it only moves past steps with nothing to answer, so a crafted `?step` still can't inflate `maxReached`. **RED-verified** (disable the effect → the resume test fails alone, 1/6). [`WizardPage.tsx:392-421` — the landing-step correction effect]
- [x] **[AI-Review][HIGH] F2 (follow-up) — the H1 fix could HARD-BLOCK Submit, a regression it introduced.** `Step4Questionnaire` is the only writer of the prefill into `questionnaireResponses`; now that all-prefilled sections correctly skip, it may never mount. `deriveReviewCompleteness` excludes only pending-NIN and geopoint names (`review-completeness.ts:49-62`), so those never-stamped required identity questions read as **MISSING** → *"Please answer all required survey questions before saving"*, with `missingStepIndex` pointing at a section that renders nothing — an unescapable loop. The same gap emptied the `raw_data` identity keys 13-33 analytics reads. **Fix:** `effectiveFormData` derives the prefill values at the point of use and feeds all three consumers (skip predicate, Review gate, submit payload), making them immune to whether Step 4 ever rendered — the same pure function, not a second writer. Also fixes calc-gated sections (`${age}` from a prefilled dob) resolving differently in the skip than at Review. **RED-verified** (revert the gate to the raw draft → `data-incomplete="true"`). [`WizardPage.tsx:362-375` `effectiveFormData`, `:459` Review gate, `:513` submit payload]
- [x] **[AI-Review][MEDIUM] F3 (follow-up) — back-navigation was fixed by construction but untested.** `retreatStep` takes the same predicate, so it inherited the corrected hide-set; there was no test proving it. **Fix:** added a Back regression (retreats *past* the all-prefilled section to Consent, never onto it).

**Net AC2 coverage after both passes:** navigate-into, arrive-on (resume / deep link / indicator jump), Back, whole-questionnaire-all-prefilled, and the Submit gate downstream of the skip. The only unexecuted layer is the Playwright wizard e2e (statically verified as untouched; CI `smoke-e2e`-gated on push).

## Dev Notes
- **Skills is multi-select — never close-on-select.** `skills_possessed` is plural (SKILL_TAXONOMY, 150 options). The friction is a missing *deliberate* close, not the stay-open behaviour. Done button + Escape + keep-search-focused is the multi-add-friendly fix. `ComboboxMultiSelect.tsx:55-64` (outside-click) is the only current close.
- **Don't touch the dedup mechanism.** `prefilledQuestionNames` auto-fill feeds `raw_data` that analytics reads (13-33) — this story is *framing only*. Removing/altering the prefill would blank the insights breakdowns.
- **13-29 already did the structural skip.** Its L1 threaded `hideQuestionNames` through `isSectionStepSkippable` so an all-prefilled section auto-skips rather than dead-ending. Verify it also covers the *entire-questionnaire-prefilled* case; only add code if there's a real gap — otherwise this is banner copy + the skills picker.
- **Optional polish (state if descoped):** (a) collapsible sector headers — the component's doc comment claims "collapsible sector headers" but the code renders all groups expanded (`ComboboxMultiSelect.tsx:186-220`), a latent doc/code gap; (b) a "selected N skills" summary in the search header; (c) surface the top/common skills first. Nice-to-have; keep core scope tight.
- **Frontend-only, conversion-path.** Pre-blast-relevant polish; not launch-gating. Independent of 13-33/13-34 (though it sits alongside the same wizard surface).

### References
- [Source: ComboboxMultiSelect.tsx (skills multi-select; :55-64 outside-click-only close; :87 toggleChoice no-close; :186-220 all groups expanded)]
- [Source: Step4Questionnaire.tsx (prefill + prefilledQuestionNames banner, 9-18 Pattern C / 13-23); FormRenderer.tsx:350 "No questions available" dead-end]
- [Source: 13-29 (isSectionStepSkippable + hideQuestionNames all-prefilled-section skip — the structural piece already shipped); 13-33 (dedup is load-bearing raw_data plumbing — framing-only here)]
- [Source: packages/types SKILL_TAXONOMY (150 skills, sector grouping via skillSectorForSlug — 13-22)]

## Dev Agent Record

### Implementation Plan
Frontend-only, three-front polish on the public conversion surface; no API/DB/data-shape change.

- **Task 1 (AC1/AC4) — skills-picker close affordance.** `ComboboxMultiSelect` gained a deliberate, discoverable exit without abandoning multi-select:
  - A **sticky "Done ({n} selected)" button** pinned to the bottom of the scroll panel (`sticky bottom-0`, stays reachable on mobile where the panel covers the form) that calls a new `closeDropdown()` helper.
  - **Escape-to-close** via an `onKeyDown` on the component root, so Escape from the search box OR any option button closes it (mirrors the existing outside-click close).
  - **Retain search focus after each pick** — `toggleChoice` now calls `searchRef.current?.focus()`, keeping the multi-add flow fast; still NOT close-on-select (`skills_possessed` is plural).
  - **A11y roles** — search input is `role="combobox"` with `aria-expanded`/`aria-controls`/`aria-haspopup="listbox"`/`aria-autocomplete="list"`; dropdown is `role="listbox"` `aria-multiselectable`; each option is `role="option"` with `aria-selected`. Chips were already keyboard-removable (unchanged).
- **Task 3 (AC3) — positive banner framing.** `Step4Questionnaire.buildBannerCopy` reworded from "We've pre-filled … from your earlier answers." to a reassuring "✓ We already have your … — no need to re-enter. Click Back to edit anything." The dedup/prefill mechanism (`prefilledQuestionNames` → `raw_data`, load-bearing for 13-33 analytics) is **untouched** — copy only.
- **Task 2 (AC2) — no dead-end.** Confirmed the structural skip already shipped: the section-level all-prefilled skip is 13-29's `isSectionStepSkippable` + `hideQuestionNames` (its L1), and geopoint-only sections are covered by 13-34's `unionGeopointNames`. Added the missing **whole-questionnaire-all-prefilled** regression composing the helpers as `WizardPage` wires them (`isSectionStepSkippable(… unreachableQuestionNames)` → `advanceStep`): with every question prefilled, the forward pass skips all sections straight to Review, and a section with even one live question still stops the user (no over-skip). Concluded no production code was needed for AC2.
  - ⚠️ **CORRECTED BY CODE-REVIEW 2026-07-25 (H1/H2) — that conclusion was wrong.** The structural skip did NOT fire on a fresh registration's first forward pass: `WizardPage` read its hide-set from `draft.formData.prefilledQuestionNames`, which `Step4Questionnaire` only stamps once it has mounted — i.e. after the step the skip is supposed to avoid. So the user still landed on "No questions available". The unit test above passes regardless, because it *supplies* the hidden set that production hasn't computed yet. AC2 now holds via a real production fix (`lib/wizard-prefill.ts` + form-derived `unreachableQuestionNames`) and a wizard-level regression (`WizardPage.prefilledSectionSkip.test.tsx`) that is RED-verified against the defect. See Review Follow-ups above.

### Completion Notes

> ⚠️ **The notes below are the dev pass's record as written on 2026-07-24 and are SUPERSEDED in three places** — kept verbatim rather than rewritten, so the review trail stays honest. Current state = these notes **plus** both Review Follow-up lists above:
> - **"all 5 ACs satisfied" was not true of AC2** — the dead-end still fired (H1). It holds now, via a production fix, not by inspection.
> - **Counts are stale.** Current: `ComboboxMultiSelect.test.tsx` 30, `WizardPage.prefilledSectionSkip.test.tsx` 6 (new file), `section-relevance.test.ts` 12, `Step4Questionnaire.test.tsx` 8; full web suite **2821 passed + 2 todo / 259 files**.
> - **Implementation details changed** by M1/H3/L4: the Done button is no longer `sticky bottom-0` inside the scroll panel (it sits outside the listbox and is always visible), `toggleChoice` refocuses the search box for POINTER selections only, and the banner reads "Use Back" with an `aria-hidden` tick.

- All 4 tasks complete; all 5 ACs satisfied. Frontend-only, UX/a11y + copy only — **no data-shape change**: skills still stored as the `skills_possessed` array, `prefilledQuestionNames` and every `raw_data` key identical, no API/DB touch.
- **Tests:** `ComboboxMultiSelect.test.tsx` 23/23 (5 new: Done-count label, Done closes, Escape closes, keep-open+refocus-search on select, combobox/listbox/option roles). `Step4Questionnaire.test.tsx` 8/8 (2 banner-copy assertions updated to the new positive wording; field-name assertions unchanged). `section-relevance.test.ts` 12/12 (2 new AC2 whole-questionnaire regressions).
- **Validation:** `tsc --noEmit` clean, `eslint` clean on all touched files, **full web suite 2808 passed + 2 todo / 258 files, 0 failures** (no regressions).
- E2E (AC5 mention): the Playwright wizard flow is unaffected — changes are additive UX/a11y + copy; no selector or data contract the specs assert on was removed (existing `data-testid`s preserved; new ones added).
- Optional polish (collapsible sector headers, "N selected" search-header summary, top-skills-first) **descoped** to keep scope tight, as the story permits — the doc/code gap on collapsible headers is noted but left for a future story.

### File List
- `apps/web/src/features/forms/components/ComboboxMultiSelect.tsx` (modified — Done button, Escape, retain-focus, ARIA roles; **review**: pointer-only refocus, listbox scoped to options, focus-return on close, single close path, `useId`, scoped Escape)
- `apps/web/src/features/forms/components/__tests__/ComboboxMultiSelect.test.tsx` (modified — 5 AC1/AC4 tests; **review**: +7 → 30 total)
- `apps/web/src/features/registration/pages/Step4Questionnaire.tsx` (modified — AC3 positive banner copy; **review**: prefill derivation moved to the shared lib, `aria-hidden` tick, "Use Back")
- `apps/web/src/features/registration/pages/__tests__/Step4Questionnaire.test.tsx` (modified — banner-copy assertions)
- `apps/web/src/features/registration/lib/__tests__/section-relevance.test.ts` (modified — 2 AC2 unit regressions; **review**: corrected the "mirrors WizardPage.steps" comment)
- `apps/web/src/features/registration/lib/wizard-prefill.ts` (**NEW, review H1** — `computePrefill` / `buildWizardIdentitySignature` extracted from Step4 so `WizardPage` can derive the hide-set before that step mounts)
- `apps/web/src/features/registration/pages/WizardPage.tsx` (**modified, review H1 + F1/F2** — `unreachableQuestionNames` form-derived not draft-bootstrapped; landing-step correction effect; `effectiveFormData` feeding the skip predicate, Review gate and submit payload)
- `apps/web/src/features/registration/pages/__tests__/WizardPage.prefilledSectionSkip.test.tsx` (**NEW, review H2 + F1/F2/F3** — 6 wizard-level AC2 regressions: forward, whole-questionnaire, no-over-skip, Back, resume-arrival, submit-gate; all RED-verified)
- `_bmad-output/implementation-artifacts/sprint-status.yaml` (modified — 13-35 ready-for-dev → in-progress → review → done)

## PM Validation (John, 2026-07-18)

**Validated — approved. Conversion-path polish, NOT launch-gating; pre-blast-nice-to-have.**
1. **Priority:** the skills-picker close friction (AC1) could cost a few completions on the blast, so it's a nice-to-have before the send; the prefill re-framing (AC3) is pure polish. Neither blocks launch.
2. **Scope is right — correctly built AROUND 13-29:** it does NOT redo the all-prefilled section skip (13-29's L1 shipped it) and does NOT touch the dedup mechanism (load-bearing raw_data plumbing). Framing + close affordance only.
3. **Good call on NOT close-on-select** — skills is genuinely multi-select; the "Done ({n})" button is the right pattern.

**No AC changes.** Dev-ready; pre-blast-nice-to-have, else post-launch.

## Change Log
| Date | Change | By |
|------|--------|-----|
| 2026-07-18 | Story drafted via *create-story. Bundled wizard-frontend UX polish: (1) skills picker gets a deliberate close affordance — sticky "Done ({n})" + Escape, keep multi-select (NOT close-on-select), keep search focus for fast multi-add; (2) reframe the "collected earlier" prefilled-question banner as a positive confirmation and confirm no "No questions available" dead-end remains (13-29 already skips all-prefilled sections). Frontend-only, no data-shape change (dedup prefill is load-bearing raw_data plumbing — framing only). Conversion-path polish, not launch-gating. | Bob (SM) |
| 2026-07-25 | **Review follow-up — AC2 closed end-to-end (3 further findings, F1-F3).** Re-reviewing the H1 fix showed it covered only *navigation into* a skippable step, not *arrival on* one: a resumed draft saved on an all-prefilled section still hit the dead-end (and pre-fix builds DID record exactly that step, so live drafts carry it). Worse, H1 introduced a regression — with all sections skipped `Step4Questionnaire` never mounts, and it is the sole writer of the prefill into `questionnaireResponses`, so the Review gate read those required identity questions as MISSING and **hard-blocked Submit** onto a section that renders nothing. Fixed by a landing-step correction effect + `effectiveFormData` (prefill derived at the point of use, feeding skip predicate / Review gate / submit payload). Back-navigation regression added. All three RED-verified individually. Wizard-level file now 6 tests; web suite 2821 pass + 2 todo / 259 files, tsc + eslint clean. | Claude (Review) |
| 2026-07-25 | **Adversarial code-review (Claude) — 11 findings (3 H / 4 M / 4 L), ALL fixed in-pass; status → done.** Headline: **AC2 was not actually satisfied.** The all-prefilled dead-end still fired on a fresh registration's first forward pass, because `WizardPage` bootstrapped its hide-set from `prefilledQuestionNames` — a field written by the very step the skip avoids. Proven RED before fixing, fixed by extracting the Pattern C derivation to `lib/wizard-prefill.ts` and deriving in `WizardPage` (form-derived, like 13-34's geopoint half). The AC2 sign-off test could not have caught this: reverting the fix leaves it 12/12 green while the new wizard-level file fails 3/3. Also fixed: a keyboard regression in the new refocus (pointer-only now), invalid ARIA that put the Done button inside `role="listbox"`, focus dropped to `<body>` on close, duplicated close paths + custom-text leak, non-unique listbox id, over-broad Escape, a stale doc comment, and mobile/AT banner copy. Web suite 2818 pass + 2 todo / 259 files, tsc + eslint clean. Git File List was accurate (0 discrepancies) and the no-data-shape-change claim held. | Claude (Review) |
| 2026-07-25 | **Adjudication (me) — APPROVED (incl. the SECOND-pass F1/F2/F3).** Re-verified the current re-reviewed tree (the other CLI's re-review landed mid-adjudication): web tsc + eslint clean on all touched files; the 4 test files **56/56** green (ComboboxMultiSelect 30, Step4 8, section-relevance 12, WizardPage.prefilledSectionSkip **6**). **RED-verified the two load-bearing fixes myself:** (H1) neuter the form-derivation in `unreachableQuestionNames` → `WizardPage.prefilledSectionSkip` fails 3/3 while `section-relevance` stays 12/12 (the structural blind spot); (F2) revert the Review gate to the raw draft → the submit-gate test fails `data-incomplete="true"` while the 5 skip/nav tests stay green — proving the Submit hard-block regression is real and `effectiveFormData` closes it. F1 (landing-step correction) + F2 (`effectiveFormData` feeding skip/gate/submit) code-reviewed: pure `computePrefill`, no second writer, well-guarded effect (can't loop, `replace`-nav, ceiling-safe under 9-54). File List == git reality; no data-shape change (frontend-only). ⚠️ **AC5 e2e residual carried:** Playwright wizard e2e verified by inspection only — CI `smoke-e2e` (deploy-gating) closes it; F1/F2 flagged as prime suspects if it reds. Selective-commit of the File List. | Awwal (adjudication) |
| 2026-07-24 | dev-story: all 4 tasks / 5 ACs implemented. Task 1 — `ComboboxMultiSelect` sticky "Done ({n} selected)" + Escape-to-close + retain-search-focus-after-pick + combobox/listbox/option ARIA (5 new tests). Task 3 — Step4 banner reworded to positive "✓ We already have your … — no need to re-enter" (prefill mechanism untouched). Task 2 — added whole-questionnaire-all-prefilled regression (real `isSectionStepSkippable`→`advanceStep` wiring lands on Review, no dead-end); section-level case already covered by 13-29/13-34, no prod code needed. Task 4 — tsc + eslint clean, full web suite 2808 pass/0 fail. Frontend-only, no data-shape change. Status → review. | Amelia (Dev) |
