# Story 13-24: Coordinated registration-welcome send (all real registrants) + blast sequencing — no double-contact

Status: done

<!-- Authored 2026-07-10 by Bob (SM) via *create-story. EMERGENT from the 13-21 AC5 backfill dry-run (prod 2026-07-10): the pool of public respondents missing the confirmation + thank-you/referral is 141 (116 with resolvable emails), NOT the handful the AC5 wording implied ("Modupe + post-13-12 completers"). ~135 registered in a May soft-launch. Operator (Awwal) decision: a thank-you/referral is promotional, NOT time-bound, so gating out the 135 is a disservice to the programme — send the welcome to ALL real registrants. This story owns (a) the send-to-all decision + its guardrails, (b) the sequencing against the 13-11 re-engagement blast so overlapping people never get whiplash, (c) the Resend-Pro gating, and (d) straightening the docs (13-21 AC5 scope + the campaign runbook). It supersedes 13-21 AC5's narrow "recent-only" framing. -->

## Story
As **the launch operator**,
I want **the registration welcome (confirmation + thank-you/referral) to reach EVERY real registrant, sequenced so nobody also gets the re-engagement blast in the same window**,
so that **the programme acknowledges all registrants + activates the referral loop, without double-contacting people and burning goodwill/sender-reputation right at launch.**

## Context & Evidence (prod-verified 2026-07-10)
- **The backfill pool is the whole historical public cohort, not a handful.** `_backfill-registration-autosends.ts --dry-run`: **141** public respondents missing ≥1 auto-send marker; **116** have a resolvable email (0 test, 0 suppressed). Date split: **135 in May 2026** (soft-launch), 3 June, 3 July.
- **The 3 genuinely-recent organic registrants** (post-13-12, which shipped ~2026-07-01): **Kolade Apata** (bonarock01@yahoo.ca, 07-02), **Modupe Adesina** (dupsy5@gmail.com, 07-06), **Yusuf Moshood** (atandayusuf170@gmail.com, 07-07). (June's Islamiat Ige has NO email → unreachable.)
- **Operator decision (Awwal, 2026-07-10):** do NOT date-gate. The thank-you/referral is promotional and not time-bound; withholding it from the 135 May registrants is a disservice. Send the welcome to **all 116**.
- **Volume vs. capacity:** 116 × 2 = **232 emails**. Resend free tier is 100/day → the all-116 send REQUIRES **Resend Pro** (the same $20 gate the blast waits on). So this is a coordinated launch send, not a today-run. Pro is being purchased.
- **Whiplash risk:** the 116 overlap the 13-11 blast cohorts (A/B/C). The welcome backfill and the 13-11 blast are SEPARATE systems — nothing currently dedupes their audiences, so an uncoordinated pair of sends would hit the same inbox twice near launch.

## Acceptance Criteria
1. **AC1 — Send-to-all decision recorded + AC5 scope corrected.** The 13-21 AC5 "Modupe + post-13-12 completers" framing is superseded: the welcome goes to ALL 116 emailable real registrants (idempotent, suppression-honored, test-rows excluded — the existing `_backfill-registration-autosends.ts` already does this; NO date filter is added). Record the reconciliation in 13-21 + this story.
2. **AC2 — Resend-Pro gating explicit.** The all-116 run is blocked until Resend Pro is active (else the 100/day free cap truncates it). The runbook states this; the operator confirms Pro before `--apply`.
3. **AC3 — Cross-system contact-dedupe: IMPLEMENTED and INHERITED (the keystone). ⚠️ RE-SCOPED 2026-07-23 from the send-ownership triangulation** (`docs/handoff-2026-07-23-send-ownership-triangulation.md`). The dedupe is **CODE, not a manual per-run list filter** — the original 2026-07-10 framing ("a query/list filter, wherever the blast list is assembled") is INSUFFICIENT: the send is fragmented across 3 independent blast scripts (`_reengagement-email-blast.ts` / `_cohort-a-supplemental-survey-blast.ts` / `_thankyou-referral-blast.ts`), each re-deriving its own cohort SQL, so a per-script filter is re-forgotten by the next script — which is exactly how this gap arose. Requirements:
   - **(a) A durable, queryable contact record keyed by EMAIL** — a dedicated `campaign_sends` marker written on EVERY marketing send (the welcome backfill + all 3 blasts). Keyed by email (NOT respondent metadata): Cohort B is `wizard_drafts` with NO respondent row, so a `respondents.metadata` marker cannot dedupe it — the key must be the email. (Chosen over reusing the Redis meter's per-recipient frequency: clearer per-campaign semantics, transactional, queryable.)
   - **(b) A SHARED cohort filter every blast INHERITS** — exactly as all three already inherit `getSuppressedEmails()`. Every blast cohort excludes any address contacted within the gap window (AC6). Adding a new blast in future gets the dedupe for free; it is NOT re-implemented per script.
   - **(c) Closes BOTH races the triangulation found**, not just welcome↔blast: also the **auto-send↔blast race** — `_thankyou-referral-blast.ts` today WRITES `metadata.thankyou_referral_sent_at` (:361) but never READS it, so an auto-welcomed completer is still in the blast cohort. The shared filter (via the email-keyed `campaign_sends`) must exclude them.
   The dedupe is **MANDATORY, not best-effort**, and "framed as a deliberate distinct follow-up" is NO LONGER an accepted escape hatch (per the ruling: true dedupe, blast MINUS welcomed).
4. **AC4 — Dry-run #2 (email positive control) before the mass send.** A fresh public registration on `pubcore-1` sets `confirmation_email_sent_at` + `thankyou_referral_sent_at` and delivers both emails (mirror of the 13-19/20 Dry-run #1 negative control), proving the go-forward pipe works BEFORE the 116-send. Verified on the box. **PLUS (review M1) — ledger-liveness:** the same control send MUST leave a `campaign_sends` row (`SELECT count(*) FROM campaign_sends WHERE email=lower(trim('<ctrl>')) AND sent_at > now()-interval '15 min'` ≥ 1). The write is fail-soft, so a missing/unwritable table degrades the dedupe to a silent no-op — this is the positive proof the dedupe is armed, not just that the code merged. Gate documented in `pre-blast-dry-run.md` §2.
5. **AC5 — Documentation straightened + a dedicated dry-run runbook created.** (i) Update `docs/runbooks/re-engagement-campaign-launch.md`: mark the STALE 2026-06-15 send-order + the old 306-row A↔B dedupe section **SUPERSEDED**, and carry the full ordered sequence: Pro → Dry-run #2 → welcome-backfill(all 116) → GAP → deduped blast; plus the 13-21-scope correction note and the auto-send monitor signals (`registration_autosend.failure/.alert_paged`) watched during the send. (ii) **CREATE `docs/runbooks/pre-blast-dry-run.md`** — the canonical Dry-run procedure that 13-21 AC4 + this AC4 both reference but which does not exist: dry-run every script → verify counts + dedupe + suppression → ONE live positive-control send → then fire; a gate that cannot be half-done. (iii) **Counts honesty:** every doc quotes the LIVE dry-run output as the source of truth for cohort sizes — NOT the stale snapshots (which disagree: Cohort A 55 vs 63, Cohort B 101 vs 267 vs 268). Stop asserting fixed numbers ("don't assert a count you don't own").
6. **AC6 — Gap-as-data + a RED-failing regression test (no double-send survives).** The gap is enforced by the AC3 shared filter as a **named constant** (e.g. `MARKETING_CONTACT_GAP_DAYS = 5`), not operator discipline — an operator cannot skip a filter that lives in the cohort SQL. A **regression test proves the exclusion FIRES**: seed an address with a recent `campaign_sends` contact → assert it is ABSENT from every blast cohort; revert the filter → it re-appears (the test RED-fails). All three double-send scenarios in the triangulation brief §1 (auto-send↔blast, blast re-run, welcome↔blast overlap) must be closed. A dedupe with no failing test is a [[pattern-ship-a-fix-that-never-fires]] candidate and fails adjudication.

## Tasks / Subtasks
- [x] **Task 1 (AC1)** — reconcile 13-21 AC5 scope (all-116, not date-gated) in both story files; no code change to the backfill (send-to-all is already its behaviour). ✅ 13-21 AC5 text + operator-residual line + change-log entry updated; this story's AC1 already carried the correction.
- [x] **Task 2 (AC3, AC6) — CODE (the re-scope):** ✅ `campaign_sends` table (`apps/api/src/db/schema/campaign-sends.ts`, email-keyed, append-only, indexed on `(email, sent_at)`) recorded on EVERY marketing send at the ONE chokepoint (`EmailService.dispatch`, MARKETING categories) — NOT per-script, so it can't re-fragment. Shared `filterMarketingCohort()` + `MARKETING_CONTACT_GAP_DAYS=5` (`apps/api/src/services/campaign-contact.service.ts`) inherited by all 3 blast cohort builders + the welcome backfill, replacing each script's bespoke `getSuppressedEmails` call (suppression + gap in ONE pass). `thankyou_referral_sent_at`-marker exclusion added to the completer-blast cohort SQL (covers historic auto-sends predating the ledger). RED-failing regression test proven (revert the filter → contacted addresses re-appear in all 3 cohorts → 4 tests fail; restore → green).
- [x] **Task 3 (AC5) — docs:** ✅ `docs/runbooks/pre-blast-dry-run.md` CREATED 2026-07-23; its §1 dedupe-verify step flipped from MANUAL-cross-check to "read the dry-run's `excluded:` line, the guard is enforced in code." ✅ `re-engagement-campaign-launch.md` §2 replaced with the authoritative ordered sequence (Pro → Dry-run #2 → welcome → 5-day gap → blasts) + how the inherited dedupe works; §0/§1/§4/§5/§7.1 reconciled; counts-honesty rule added (quote the dry-run, not the doc). ✅ `backfill-operator-residuals.md` `_thankyou-referral-blast.ts` row flipped from `❓ check for double-send` to CLOSED-in-code + welcome-backfill row added.
- [ ] **Task 4 (AC4)** — operator: Dry-run #2 on the box (fresh reg → both markers + delivery), verified via Tailscale. **[OPERATOR RESIDUAL — post-deploy.]**
- [ ] **Task 5 (AC2)** — operator: confirm Resend Pro live, then `_backfill-registration-autosends.ts --dry-run` → review the emailable list (read the `excluded:` line) → `--apply --confirm-i-am-not-dry-running --rate-per-minute 10`. **[OPERATOR RESIDUAL — post-deploy, gated on Resend Pro.]**

### Review Follow-ups (AI) — adversarial code-review 2026-07-24 (all fixed same session)
- [x] [AI-Review][Med] **M1 — ledger-liveness gate.** Fail-soft `recordCampaignSend` silently no-ops if `campaign_sends` is missing/unwritable on prod → dedupe excludes nobody, ships looking "done" ([[pattern-ship-a-fix-that-never-fires]]). AC4/pre-blast-dry-run had no POSITIVE assertion a row was written. → Added a mandatory ledger-liveness check to `pre-blast-dry-run.md` §2 + AC4. [docs/runbooks/pre-blast-dry-run.md §2; AC4]
- [x] [AI-Review][Med] **M2 — intra-run duplicate-email double-send.** Ledger is read once at cohort-build + written per-send, so two respondent rows sharing one email (real: `duplicate-registration`) both send in ONE run. Cohort B safe (`wizard_drafts.email UNIQUE`); thank-you/Cohort-A `DISTINCT ON (r.id)` ≠ per-email. → `filterMarketingCohort` now de-dupes the surviving cohort by canonical email (keep-first) + reports `duplicatesSkipped`. [campaign-contact.service.ts:filterMarketingCohort]
- [x] [AI-Review][Med] **M3 — anti-fragmentation guard blind spot.** `blast-dedupe-inheritance.test.ts` only caught the OLD `getSuppressedEmails` half-fix; a NEW cohort script that forgot dedupe entirely passed. → Added a discovery-based check: any script referencing a MARKETING category literal must route through `filterMarketingCohort` (`_diagnose-email-usage.ts` allowlisted as a non-cohort diagnostic). [blast-dedupe-inheritance.test.ts]
- [x] [AI-Review][Low] **L1 — integration test env-isolation.** `blast-cohort-dedupe.integration.test.ts` hard-asserts `gapDays===5` without neutralising a `MARKETING_CONTACT_GAP_DAYS` override → false RED on a shell/CI that sets it. → beforeAll/afterAll now save+clear+restore the env var. [blast-cohort-dedupe.integration.test.ts]
- [x] [AI-Review][Low] **L2 — implicit disjointness invariant.** Reengagement/Cohort-A rely on the ledger (not the metadata marker) for the auto-send↔blast race; safe only because those cohorts are disjoint from auto-thanked completers. Undocumented → a future cohort tweak could silently reopen it. → Documented the invariant at the cohort-SQL sites. [_reengagement-email-blast.ts, _cohort-a-supplemental-survey-blast.ts selectCohort]

## Dev Notes
- **⚠️ 2026-07-23 RE-SCOPE — this is NOW a code story, not "ops + docs only."** The original framing ("minimal/no code; the dedupe is a query/list filter wherever the blast list is assembled") was shown INSUFFICIENT by the send-ownership triangulation: the send is 3 independent blast scripts + 2 auto-sends, and a per-run manual filter re-fragments (the very cause of the gap). The backfill's send-to-all behaviour is unchanged; what's NEW is the **inherited cross-system contact-dedupe** (AC3/AC6) — a `campaign_sends` marker + a shared filter every blast consults, closing the auto-send↔blast race the original story didn't even mention.
- **Why email-keyed, not respondent-metadata:** the auto-send guard (`respondents.metadata.thankyou_referral_sent_at`) can't dedupe **Cohort B** — those are `wizard_drafts` with NO respondent row. The dedupe key must be the EMAIL, in a shared `campaign_sends` record, so all cohorts (drafts + respondents) are covered by one filter.
- **SMS (9-27 Part B) is explicitly DEFERRED** (decision 2026-07-23) — do NOT block email launch on it. But design the shared filter so a future SMS blast inherits the dedupe for free (same as suppression), rather than re-deriving it.
- **The backfill script (13-21 AC5) already sends to all eligible** with idempotency + suppression + rate-limit — that part is untouched; this story adds the dedupe layer + sequencing + docs around it.
- **Why send-to-all is right:** promotional/thank-you email is not time-bound; the 135 May registrants got zero acknowledgement and the referral ask has value whenever it lands. The only real risks are capacity (→ Pro) and double-contact (→ AC3 dedupe) — both handled by sequencing, not by excluding people.
- **Reputation:** 232 emails on a fresh sender domain — the 10/min rate-limit + verified domain + these being opt-in registrants keeps it safe; monitor the 13-21 `registration_autosend.failure` signal + the Resend dashboard during the run.
- **Not a hard registration blocker**, but it IS the launch's re-engagement mechanism — do it as part of the coordinated launch send, gated on Pro.

### References
- [Source: apps/api/scripts/_backfill-registration-autosends.ts — the AC5 send-to-all backfill (idempotent + suppression + rate-limit)]
- [Source: 13-21 (auto-send fix + monitor); 13-11 (re-engagement blast); 13-12 (evergreen thank-you); 13-9 (suppression)]
- [Source: docs/runbooks/re-engagement-campaign-launch.md — the campaign execution hub to update]
- [Source: prod 2026-07-10 — 141 pool / 116 emailable / 135 May; recent 3 = Kolade/Modupe/Yusuf]

## Dev Agent Record

### Implementation Plan / Completion Notes (Amelia, 2026-07-23)

**The keystone (AC3): dedupe is INHERITED, written at the chokepoint, read via one shared filter.**
- **`campaign_sends`** (new table) is the cross-system contact ledger — email-keyed (the ONLY key every cohort shares: Cohort B drafts have no respondent row, so a `respondents.metadata` marker could never dedupe them), append-only (a later-round contact is real history; the dedupe question is always "within N days", never "ever"), indexed on `(email, sent_at)`.
- **WRITE is centralized** at `EmailService.dispatch()` — the single point every send already passes through for suppression-headers + metering. Gated to `MARKETING_CATEGORIES` (13-13 set) after a *successful* provider send, fail-soft (a ledger error never changes send behaviour). A new blast inherits recording for free; it cannot forget to opt in — which is exactly how the original gap was born (3 scripts each re-deriving a cohort). This directly answers John's PM ask #2 ("check the WRITE is centralized, not just the read").
- **READ is one shared filter** — `filterMarketingCohort(rows, getEmail)` applies suppression (13-9/13-13) AND the recent-contact gap in one pass, so a caller cannot inherit one and miss the other. All three blast cohort builders + the welcome backfill now call it and nothing else; each script's bespoke `getSuppressedEmails` block is gone (an inheritance-guard test forbids a bare `getSuppressedEmails` cohort filter from creeping back).

**AC3c — the auto-send↔blast race (the one the original story didn't even name).** `_thankyou-referral-blast.ts` always WROTE `metadata.thankyou_referral_sent_at` (:361) but never READ it. Now the cohort SQL excludes `metadata->>'thankyou_referral_sent_at' IS NULL`, which also covers HISTORIC auto-sends stamped on prod before the ledger existed — the ledger covers everything from this story forward; the marker check covers the back-catalogue.

**AC6 — gap as DATA + RED-failing test.** `MARKETING_CONTACT_GAP_DAYS = 5` (the resolved 3-vs-5 decision; overridable via env, but never falls back to 0 — that would silently disable the guard). The gate is `blast-cohort-dedupe.integration.test.ts`: for every cohort it asserts a PAIR — the contacted address is present in the RAW cohort and ABSENT from the SEND cohort, plus a never-contacted control that must survive both (so "the filter emptied everything" also fails). Revert-to-RED verified during dev: neutering the filter fails 4 tests across all three cohorts; restoring → green. All three §1 double-send scenarios (welcome↔blast, blast-re-run, auto-send↔blast) are covered.

**AC5 iii — counts honesty.** Every script's `--dry-run` now prints `excluded: suppressed=N, contacted-within-Nd=N`; the runbooks say that output — not any doc snapshot — is the cohort size.

**Gates.** tsc(api) clean; eslint(changed files) clean; the 12 email/blast-adjacent suites green (227 tests) incl. the new ledger + filter + regression + inheritance-guard tests. Full API suite: 3258 pass / 7 skip / 6 fail — **all 6 failures are pre-existing local test-DB parity gaps** (DB CHECK/partial-unique constraints in `respondents`, `api_consumers`, `audit_logs` that `db:push:force` doesn't create and `db:push:full:force` couldn't add over the pre-populated local `app_test`; the constraint objects are confirmed absent from the DB — `SELECT ... pg_constraint` returns 0 rows). None touch email/campaign/blast code; they pass in CI on a fresh DB. Web unchanged (API-only story).

**Operator residuals (Tasks 4–5, post-deploy):** deploy the code (the ledger write + migrate the `campaign_sends` table on prod), then Dry-run #2, then — gated on Resend Pro — the welcome backfill. **NOTE for deploy:** `campaign_sends` is a Drizzle-native table (picked up by `db:push:force`), no `migrate-*-init.ts` runner needed.

### File List
**New:**
- `apps/api/src/db/schema/campaign-sends.ts` — the `campaign_sends` cross-system contact ledger (schema)
- `apps/api/src/services/campaign-contact.service.ts` — `MARKETING_CONTACT_GAP_DAYS`, `recordCampaignSend`, `getRecentlyContactedEmails`, `filterMarketingCohort`
- `apps/api/src/services/__tests__/campaign-contact.service.test.ts` — gap constant + ledger + filter (DB)
- `apps/api/src/services/__tests__/email-campaign-ledger.test.ts` — the centralized chokepoint WRITE (DB)
- `apps/api/scripts/__tests__/blast-cohort-dedupe.integration.test.ts` — **the AC6 keystone** (revert-to-RED, all 3 cohorts + auto-send race)
- `apps/api/scripts/__tests__/blast-dedupe-inheritance.test.ts` — anti-fragmentation guard (every cohort builder inherits the shared filter)

**Modified (code):**
- `apps/api/src/db/schema/index.ts` — export `campaign-sends`
- `apps/api/src/services/email.service.ts` — record the marketing contact in `dispatch()` (chokepoint WRITE)
- `apps/api/src/services/__tests__/email-campaign-tag.test.ts` — stub the new ledger write (side-effect-free tag test)
- `apps/api/scripts/_thankyou-referral-blast.ts` — inherit `filterMarketingCohort`; `thankyou_referral_sent_at` cohort exclusion; export `selectCohort`/`selectSendCohort`; dry-run `excluded:` line
- `apps/api/scripts/_cohort-a-supplemental-survey-blast.ts` — inherit `filterMarketingCohort`; export `selectCohort`/`selectSendCohort`; dry-run `excluded:` line
- `apps/api/scripts/_reengagement-email-blast.ts` — inherit `filterMarketingCohort`; export `selectCohort`/`selectSendCohort`; dry-run `excluded:` line
- `apps/api/scripts/_backfill-registration-autosends.ts` — inherit `filterMarketingCohort`; counts-honesty in dry-run + apply headers

**Modified (docs):**
- `_bmad-output/implementation-artifacts/13-21-registration-autosend-reliability-and-visibility.md` — AC5 scope reconciled with 13-24 AC1 (all-116, not date-gated) + change-log entry
- `docs/runbooks/re-engagement-campaign-launch.md` — §2 authoritative ordered sequence + dedupe explainer; §0/§1/§4/§5/§7.1 reconciled; counts-honesty
- `docs/runbooks/pre-blast-dry-run.md` — §1 dedupe-verify flipped to code-enforced (read the `excluded:` line)
- `docs/runbooks/backfill-operator-residuals.md` — `_thankyou-referral-blast.ts` row CLOSED-in-code; welcome-backfill row added
- `_bmad-output/implementation-artifacts/sprint-status.yaml` — 13-24 → in-progress → review

## PM Validation (John, 2026-07-10)

**Validated — approved. Launch-relevant (the re-engagement mechanism), ops-led, gated on Resend Pro.**

1. **The send-to-all call is right, and AC3 is what makes it safe.** Awwal's framing is correct — a thank-you/referral isn't time-bound, so excluding 135 real registrants is lost programme value. The ONLY things that made me hesitate on the original backfill were capacity and double-contact; both are solved by *sequencing*, not exclusion. So: send to all 116, but AC3's cross-system dedupe (welcomed emails OUT of the blast list) + the ≥3–5 day gap is non-negotiable — that's the difference between "coordinated launch" and "spammed our whole list twice."
2. **Resend Pro gating (AC2) is a hard precondition** — a truncated 100/day send would silently deliver to an arbitrary 100 and look "done." Confirm Pro, then the full run.
3. **Dry-run #2 (AC4) before the mass send, not after.** Prove the go-forward auto-send pipe on one fresh registration first; if it's broken we find out on 1 person, not discover 116 half-sent.
4. **Scope correction (AC1) matters for honesty** — 13-21 AC5 said "recent completers"; reality + this decision is "all real registrants." Straighten both story files + the runbook so the next operator isn't confused.
5. **Sequence vs the other launch items:** this rides AFTER Public Core is finalized (done) and Pro is live; it's independent of 13-22/13-23 (which are the data/attribution track). Fine to run in parallel with those being dev'd.

**No AC changes.** Dev-ready (ops-led).

## PM Validation (John, 2026-07-23 — re-scope review)

**Re-scope APPROVED. The 2026-07-10 "ops-led, no-code" classification is withdrawn — this is now a code story and must go through dev + adversarial review + the RED-test before any operator send.**

1. **The re-scope is justified by evidence, not preference.** The double-send gap was verified three ways (the script's own cohort SQL, the residual tracker's `❓ check for double-send` flag, both Explore maps). The original AC3 "manual list filter" would have left the **auto-send↔blast race entirely unaddressed** — the original story didn't even name it. Correcting this before firing is exactly the point of the triangulation.
2. **Email-keyed `campaign_sends` is the correct primitive, and I want it written at the CHOKEPOINT, not per-script.** Bob's Cohort-B argument is decisive (drafts have no respondent row, so metadata can't dedupe them). BUT the marker must be recorded where suppression/metering already are (the shared send path), NOT bolted onto each of the 3 scripts — otherwise it inherits the same fragmentation it's meant to cure. Adjudication should check the WRITE is centralized, not just the read.
3. **The RED-failing test (AC6) is non-negotiable and is the real gate.** "13-24 says dedupe is mandatory" and "dedupe fires on real data" are different artifacts; only the revert-to-red test proves the second. This is the story most about not-annoying real people at launch — a silent guard here is the worst place for [[pattern-ship-a-fix-that-never-fires]].
4. **SMS deferral (Part B) endorsed** — do not block email launch on unbuilt SMS; the inherited-filter design means the future SMS blast gets dedupe for free, so deferring costs nothing.
5. **Counts-honesty (AC5 iii) is a real fix, not polish** — a truncated or stale-cohort send "looks done." Live dry-run output as the single source of truth is the honest bar.
6. **Sequencing note:** this now sits AFTER dev+review (the code) and Pro-live, and still rides after Public Core (done). It is no longer parallelizable-as-pure-ops — the code lands first, then the operator steps (Tasks 4–5).

**AC changes: AC3 re-scoped, AC5 expanded, AC6 added.** Dev-ready (code + ops).

## Senior Developer Review (AI) — 2026-07-24 (Opus, BMAD code-review workflow)

**Outcome: APPROVED (code) — 5 findings, 0 High, all fixed + independently re-verified in-session.** The keystone (email-keyed `campaign_sends` written at the one `EmailService.dispatch` chokepoint + a shared `filterMarketingCohort` inherited by all 3 blasts and the welcome backfill) is correct: WRITE is centralized (verified `thankyou-referral` auto-send passes the marketing category; confirmation classifies to `other` → correctly not ledgered), READ is one inherited filter, and the AC6 regression genuinely fires against the REAL cohort SQL (RAW-contains / SEND-excludes pairs + surviving controls). Git File List == story File List, exactly.

Independently ran (not trusting self-report): `tsc(api)` 0 · `eslint`(changed) 0 · 37 email/blast tests green vs `app_test` (incl. the AC6 integration + inheritance guard).

Findings (all fixed this session — see the Review Follow-ups (AI) checklist under Tasks):
- **M1 (Med) — no prod ledger-liveness check.** `recordCampaignSend` is fail-soft, so a missing/unwritable `campaign_sends` on prod degrades the dedupe to a silent no-op ([[pattern-ship-a-fix-that-never-fires]]) — and "no `record_failed` log" is not proof it wrote. Added a mandatory positive-control `SELECT count(*) FROM campaign_sends …` gate to `pre-blast-dry-run.md` §2 + AC4.
- **M2 (Med) — intra-run duplicate-email double-send.** The ledger dedupes across runs but is read once at cohort-build, so two respondent rows sharing an email (`duplicate-registration` is real; `DISTINCT ON (r.id)` ≠ per-email) both send in one run. `filterMarketingCohort` now de-dupes the surviving cohort by canonical email (keep-first) and reports `duplicatesSkipped` (surfaced in every dry-run `excluded:` line). +2 tests. Cohort B was already safe (`wizard_drafts.email UNIQUE`).
- **M3 (Med) — anti-fragmentation guard blind spot.** The old guard only caught the `getSuppressedEmails` half-fix; a NEW cohort script that forgot dedupe entirely passed. Added a discovery-based check: any script referencing a MARKETING category literal must route through `filterMarketingCohort` (`_diagnose-email-usage.ts` allowlisted as a read-only diagnostic).
- **L1 (Low) — integration test env-isolation.** `blast-cohort-dedupe.integration.test.ts` hard-asserts `gapDays===5`; now saves/clears/restores `MARKETING_CONTACT_GAP_DAYS` so an ambient override can't cause a false RED.
- **L2 (Low) — implicit disjointness invariant documented.** Reengagement/Cohort-A rely on the ledger (not the metadata marker) for the auto-send↔blast race, safe only because those cohorts are disjoint from auto-thanked completers; documented at both `selectCohort` sites so a future cohort tweak can't silently reopen the race.

### Post-fix deeper pass — the sequencing nuance (documented, not a code change)

After fixing the five findings I re-examined the thing a green suite cannot prove: whether "blast MINUS welcomed" actually holds under the operator's real send sequence. **Finding: the gap constant (5d) equals the sequencing wait**, so when the blast fires *after* the gap, the welcomed cohort has just fallen OUT of the `campaign_sends` exclusion window — the ledger alone would not minus them (AC6's own `contacted OUTSIDE the window is sendable again` test confirms this is intended window semantics, not a bug).

I traced every double-contact path; the guarantee is carried by **three** mechanisms, only one time-limited:

| Overlap | What actually prevents the double-send | Timing-dependent? |
|---|---|---|
| Welcome-thankyou ↔ **thank-you blast** (completers) | `metadata.thankyou_referral_sent_at` **marker** (cohort SQL) | **No — permanent** |
| Welcome ↔ **reengagement** (drafts) | Structural — pure drafts have no respondent row; Cat1 drops completed | **No — structural** |
| Welcome ↔ **Cohort A** (no-submission) | Structural — no-submission rows get only a transactional confirmation (deliberately un-ledgered), not the marketing thank-you | **No — structural** |
| Same address in **two different** marketing cohorts, or a **blast re-run** | `campaign_sends` ledger gap window | **Yes — 5-day window** |

**Conclusion — launch-safe:** the primary overlap (welcomed completers ↔ thank-you blast) is closed **permanently by the marker**, and the other cohorts are **structurally disjoint** ([[13-24]] L2 invariant); the ledger is the backstop for re-runs / same-address-two-cohorts and holds as long as those sends are within the window of each other. **Residual (narrow, documented):** two *different* marketing blasts to the same address spaced **> `MARKETING_CONTACT_GAP_DAYS` apart** would not be deduped by the ledger — mitigated by the operational rule **"fire all blasts in one session, back-to-back"** so each later cohort is built after the earlier send's ledger rows exist. It widens only if a cohort redefinition breaks the L2 disjointness invariant. Captured for the operator in `pre-blast-dry-run.md` §4 (three-mechanism explainer + the one-session rule). No code change — changing the window would contradict the ratified AC6 "window, not ever" decision + its pinning test.

Status → done (code + review complete). **AC2/AC4 remain operator residuals (Tasks 4–5, post-deploy, Resend-Pro-gated)** — including the new M1 ledger-liveness verification.

## Change Log
| Date | Change | By |
|------|--------|-----|
| 2026-07-24 | **Adversarial code-review PASSED (Opus, BMAD workflow).** 5 findings (0 High: M1/M2/M3 Med, L1/L2 Low), ALL fixed + re-verified in-session (tsc 0, eslint 0; 69 email/campaign/blast/suppression tests green vs `app_test`). M1 ledger-liveness gate → `pre-blast-dry-run.md` §2 + AC4 (fail-soft write can silently no-op the dedupe). M2 `filterMarketingCohort` intra-run canonical-email de-dupe + `duplicatesSkipped` counts-honesty across all 4 scripts (+2 tests). M3 discovery-based inheritance guard (marketing-literal ⇒ must inherit the filter). L1 integration-test env isolation. L2 disjointness invariant documented. Status → done; AC2/AC4 remain post-deploy operator residuals. File List updated (no new files — all edits to already-listed files + `campaign-contact.service.ts`/tests). | Opus (Review) |
| 2026-07-24 | **Post-fix deeper pass — sequencing nuance documented (no code change).** Traced every double-contact path under the real operator send sequence: the gap constant (5d) equals the sequencing wait, so the `campaign_sends` ledger is the time-windowed BACKSTOP, not the primary "blast MINUS welcomed" guarantee — that is carried permanently by the `thankyou_referral_sent_at` marker + cohort disjointness (L2). Launch-safe; the one narrow residual (two different marketing blasts to one address spaced > gap-days apart) is mitigated by the "fire all blasts in one session" rule now in `pre-blast-dry-run.md` §4 (three-mechanism explainer). Recorded in the Senior Developer Review (AI) section. No code change — altering the window would contradict the ratified AC6 "window, not ever" decision + its pinning test. | Opus (Review) |
| 2026-07-10 | Story drafted via *create-story — coordinated welcome send to ALL real registrants (116) + anti-whiplash blast sequencing + Resend-Pro gating + Dry-run #2 + doc straightening. Supersedes 13-21 AC5's recent-only framing (operator decision: promotional email isn't time-bound). EMERGENT from the 13-21 backfill dry-run. | Bob (SM) |
| 2026-07-23 | **RE-SCOPE from the send-ownership triangulation** (`docs/handoff-2026-07-23-send-ownership-triangulation.md`). AC3 changed from a documented manual list-filter to an IMPLEMENTED, INHERITED cross-system contact-dedupe (email-keyed `campaign_sends` marker + a shared filter every blast consults like `getSuppressedEmails`), now explicitly closing the auto-send↔blast race the original missed. AC5 expanded (create the missing `pre-blast-dry-run.md` + counts-honesty). AC6 added (gap-as-data named constant + RED-failing regression test; the 3 double-send scenarios must be closed). Dev Notes corrected ("no-code" → code story; email-keyed because Cohort-B drafts have no respondent row). SMS/9-27-Part-B deferred; 5-day gap constant; true dedupe (blast MINUS welcomed, no "distinct follow-up" escape hatch) — decisions resolved to recommendation (Awwal, "do everything here"). John PM re-validated (approved; want the marker WRITE centralized at the chokepoint + the RED-test as the gate). Authored + validated in the adjudication CLI per Awwal's instruction; DEV + adjudication still to follow. | Bob (SM) + John (PM) |
| 2026-07-23 | **dev-story COMPLETE (Amelia).** The re-scoped CODE landed: `campaign_sends` email-keyed ledger (new table) written at the ONE send chokepoint (`EmailService.dispatch`, marketing categories) + a shared `filterMarketingCohort()` (suppression + `MARKETING_CONTACT_GAP_DAYS=5` in one pass) inherited by all 3 blast cohort builders and the welcome backfill — replacing each script's bespoke `getSuppressedEmails` call. AC3c: the completer-blast cohort SQL now excludes `metadata->>'thankyou_referral_sent_at' IS NOT NULL` (closes the auto-send↔blast race, incl. historic pre-ledger auto-sends). AC6: `blast-cohort-dedupe.integration.test.ts` proves the exclusion FIRES on the real cohort queries and revert-to-REDs (neuter filter → 4 fails across all 3 cohorts). Anti-fragmentation guard test forbids a bare `getSuppressedEmails` cohort filter from returning. AC5: campaign runbook §2 rewritten to the ordered sequence + dedupe explainer, pre-blast-dry-run §1 flipped to code-enforced, residual tracker CLOSED, counts-honesty (`excluded:` dry-run line) everywhere. AC1: 13-21 AC5 scope reconciled. Gates: tsc(api)+eslint(changed) clean; 227 email/blast-adjacent tests green. Full API 3258 pass/6 fail — all 6 pre-existing local test-DB constraint-parity gaps (objects confirmed absent via pg_constraint; pass on CI's fresh DB), none touching this story's code. Status → review. **OPERATOR RESIDUALS (Tasks 4–5, post-deploy):** deploy (migrate `campaign_sends`) → Dry-run #2 → Resend-Pro-gated welcome backfill. NEXT: adversarial code-review (different LLM). | Amelia (Dev) |
