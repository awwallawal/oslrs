# Story 9.12: Public Wizard + Pending-NIN + NinHelpHint + Magic-Link Email

Status: review

<!--
Created 2026-04-25 by impostor-SM agent per SCP-2026-04-22 §A.5.

This story collapses the existing 4-hop public registration (register → verify email → login → fill form) into a single 5-step wizard. Magic-link is the primary auth channel; SMS OTP is built but feature-flagged off (budget-gated). Pending-NIN status path supports respondents without NIN at submission.

Sources:
  • PRD V8.3 FR5 (softened), FR21 (scoped), FR27 (wizard), FR28 (deferred-NIN)
  • Architecture Decisions 2.5 (magic-link primary), 2.6 (SMS OTP infra-only), Decision 1.5 (status enum)
  • Architecture ADR-015 rewrite (5-step wizard) + ADR-018 (multi-source / pending-NIN)
  • UX Journey 2 rewrite + Journey 8 (return-to-complete) + Form Patterns (NinHelpHint, Email-Typo Detection, Pending-NIN Toggle) + Visible Step Indicator pattern + Trust badges
  • Epics.md §Story 9.12

Field-Readiness Certificate item #3 — field-survey-blocking until done.
Depends on Story 11-1 (status enum on respondents table) AND `prep-settings-landing-and-feature-flags` (SMS OTP toggle UI host + system_settings table for the feature flag).

Validation pass 2026-04-30 (Bob, fresh-context mode 2 per `_bmad/bmm/workflows/4-implementation/create-story/checklist.md`): rebuilt to canonical template; 5 factual path corrections applied (PublicRegisterPage → RegistrationPage; PublicLoginPage → LoginPage; staff-activation feature dir → auth/components/activation-wizard existing; email service flat file vs templates subdirectory clarified; lib/settings.ts dependency relocated to prep-settings-landing); AC#7 SMS OTP toggle UI dependency reframed to consume `prep-settings-landing-and-feature-flags` (per Awwal Path B decision 2026-04-30 — Settings landing is HARD dependency for AC#7 SMS OTP toggle UI; backend feature flag mechanism lives in `apps/api/src/lib/settings.ts` from that prep story).
-->

## Story

As a **public respondent registering for the Oyo State Skills Registry on my mobile phone**,
I want **a single 5-step wizard with a clear step indicator, the option to defer NIN capture if I don't have my NIN card on me, and a way to come back later via an email link**,
so that **I can complete registration in one continuous flow without bouncing across multiple separate screens, and I'm not blocked by a missing NIN**.

## Acceptance Criteria

1. **AC#1 — Single 5-step wizard at `/register` (replaces 4-hop flow):** New WizardLayout-derived shell renders all 5 steps as a single page with persistent step indicator at the top and trust-badges row at the foot of every step. Browser back/forward navigates step boundaries (URL-routed: `/register?step=N`). Old `/register` route handler retired or redirected to the wizard. Steps:
   - **Step 1 — Basic Info:** full name, date of birth (native `<input type="date">` mobile fallback to year/month/day dropdowns), gender. Auto-focus on Full Name.
   - **Step 2 — Contact + LGA:** phone (Nigerian format mask `080 1234 5678`, JetBrains Mono, real-time format validation), email (with email-typo detection per AC#5), LGA (autocomplete from 33 Oyo LGAs).
   - **Step 3 — Consent (FR2 two-stage):** plain-language NDPA paragraph; Stage 1 marketplace-inclusion radio (cannot proceed without ack); Stage 2 enriched contact-share (only shown if Stage 1 = Yes; default No).
   - **Step 4 — Questionnaire:** renders the published `respondentSourceTypes='public'` form via the existing one-question-per-screen native renderer, mounted *inside* the wizard chrome. Skip-logic engine drives visibility.
   - **Step 5 — NIN + Optional Login Setup:** NIN input with `NinHelpHint` inline + `*346#` USSD reminder (per AC#3); pending-NIN toggle (per AC#4); optional email magic-link (primary CTA), set-a-password (secondary), skip-for-now (tertiary, sends one-time magic-link with TTL 72h).

2. **AC#2 — Visible step indicator (per Sally's Progress Pattern):** `WizardStepIndicator` component renders horizontal breadcrumb-style row above the wizard card on screens ≥480px (clickable completed circles for back-nav, current circle filled Primary-600, future circles disabled). Mobile <480px collapses to "Step N of M — <label>" text-only. Persistently visible (`position: sticky; top: 0`). Component is reused by Story 11-3 Admin Import wizard and the staff activation wizard polish in AC#12.

3. **AC#3 — `NinHelpHint` shared component (per Sally's Form Pattern):** New component at `apps/web/src/features/registration/components/NinHelpHint.tsx` (or shared location for cross-feature reuse). Three variants: `inline` (under input, used in this wizard Step 5), `tooltip` (icon-button beside label, used in Data Entry Clerk form per Story 9-12 follow-up), `banner` (full-width above field, used in pending-NIN return-to-complete view per AC#9). All three carry `*346#` USSD copy in JetBrains Mono background. Inline variant additionally surfaces "I don't have my NIN now" link that triggers the pending-NIN toggle in the parent wizard. Accessibility: parent NIN input has `aria-describedby` pointing to inline hint; tooltip is a `<button role="button">` keyboard-focusable; banner is `<aside role="note">`.

4. **AC#4 — Pending-NIN toggle + status path (per Sally's Form Pattern):** `<button role="switch">` labelled "I don't have my NIN with me right now" under the NIN input on Step 5. When ON: NIN field disabled (preserved value retained), `respondent.status = 'pending_nin_capture'` set on submit, consequence preview card appears (Info-50 background, Info-600 left border) with copy "Your registration will be saved as pending. We'll email you to complete it. We'll also remind you in 2 days, 7 days, and 14 days." Submit button label changes from "Submit Registration" to "Save as Pending" when toggle is ON. Toggle state survives back-navigation to previous steps.

5. **AC#5 — Email-typo detection (per Sally's Form Pattern):** On blur of the email input on Step 2, run published common-typo dictionary check (`mailcheck` library or curated subset including `gmail.vom`, `gmail.con`, `gmial.com`, `gmal.com`, `gmaill.com`, `mail.com`, `yahooo.com`, `hotmial.com`, `hotmali.com`). On match, render Info-700 text below field: "Did you mean `<corrected>`? [Use this]". "Use this" is a `<button>` that applies the correction; ignoring submits the original. Suggestion is in a live region (`role="status" aria-live="polite"`). Never auto-correct.

6. **AC#6 — Magic-link primary auth (per Architecture Decision 2.5):**
   - New service `apps/api/src/services/magic-link.service.ts` — generate 32-byte token (`crypto.randomBytes(32).toString('base64url')`), SHA-256 hash for storage in new `magic_link_tokens` table (`{ id, public_user_id|user_id, token_hash, purpose, expires_at, used_at, created_at }`)
   - Purpose enum: `wizard_resume` (TTL 72h) | `pending_nin_complete` (TTL 72h) | `login` (TTL 15min)
   - Single-use enforcement via atomic `used_at` set on first redemption
   - Rate limit: 3 requests per email per hour (shares the existing NFR4.4 password-reset budget pool — clone rate-limit middleware pattern from `apps/api/src/middleware/login-rate-limit.ts:25-110` with `prefix: 'rl:magic-link:'`)
   - Email content: short, action-oriented; uses existing `EmailService` at `apps/api/src/services/email.service.ts` (flat file — verify how templates are currently handled before adding new ones); subject "Continue your Oyo State Skills Registry registration"; primary CTA button + plaintext fallback link + 72-hour expiry note + recovery instructions
   - New endpoints:
     - `POST /api/v1/auth/public/magic-link` — request a magic link by email + purpose
     - `GET /auth/magic?token=<plaintext>&purpose=<purpose>` — controller hashes, validates, issues JWT (per existing public-login JWT shape), redirects per purpose

7. **AC#7 — SMS OTP infrastructure-only / feature-flagged off (per Architecture Decision 2.6):**
   - Build the full code path: route handler `POST /api/v1/auth/public/sms-otp/request` + `POST /verify`, `SmsProviderAdapter` interface with one implementation `NoopSmsProvider` that logs + rejects with `SMS_OTP_DISABLED`, audit wiring identical to magic-link path
   - **Feature flag** `auth.sms_otp_enabled` lives in `system_settings` table (created by `prep-settings-landing-and-feature-flags`). Backend reads via `getSetting<boolean>('auth.sms_otp_enabled')` from `apps/api/src/lib/settings.ts` (also created by that prep story; **HARD dependency** — this story cannot ship before prep-settings-landing lands). Default value: `false` (seeded by prep-settings-landing migration).
   - **Super-admin toggle UI** lives in the Settings Landing Page at `/dashboard/super-admin/settings` (created by `prep-settings-landing`); the `SmsOtpToggle` component (per prep-settings-landing AC#5 v1 contents) provides the UI. This story does NOT add a separate UI surface for the toggle.
   - When flag flips ON via Settings landing UI (future Termii or Africa's Talking integration), the resolver in `sms-otp.service.ts` switches to a real provider implementation and the path activates without redeploy
   - Audit-logged on flip via `prep-settings-landing` `SETTINGS_FLIPPED` action — this story doesn't add separate audit instrumentation for the flag flip
   - **No partner-API scope (Epic 10) or wizard step depends on SMS OTP** — verified via test: enabling the flag must not break any existing flow

8. **AC#8 — Trust badges row (SUPA-inspired but distinct):** Three `<aside>` badges at the foot of every wizard step card: "🔒 Secure Registration" (Success-100/600), "🛡️ Official Oyo State Platform" (Primary-50/600 with Oyo State logo SVG 38×38), "🆓 Free to Join" (Info-100/600). Stack vertically on screens <480px. Each badge has `aria-label` describing the assurance. Visual hierarchy intentionally lower than primary CTAs.

9. **AC#9 — Return-to-complete via magic link (per Sally's Journey 8):**
   - When a respondent clicks `wizard_resume` magic link, server hydrates from server-side draft (not just IndexedDB — works across devices), redirects to `/register?step=N` where N is the saved step
   - When a respondent clicks `pending_nin_complete` magic link, server redirects to dedicated narrow view `/register/complete-nin` (NOT the full wizard re-mount) — view contains only the NinHelpHint banner + NIN input + Save button + trust badges row
   - "I still don't have my NIN — remind me later" link in the complete-nin view triggers server endpoint that resets the reminder timer (next reminder pushed to T+7d from now); audit-logged with `action: 'pending_nin.deferred_again'` (add to `AUDIT_ACTIONS` const at `apps/api/src/services/audit.service.ts:35-64`)
   - On valid + new NIN entry from complete-nin view: `respondents.status` promotes `pending_nin_capture` → `active`; FR21 dedupe runs at this moment (since NIN is now present); success email confirms; respondent lands on "Registration complete" screen
   - On valid but duplicate NIN: original-registration-date message + "go back" / "contact support" affordances (per Journey 2 wizard handling)

10. **AC#10 — Reminder cadence for `pending_nin_capture` (FR28):**
    - Scheduled job runs daily; for each `pending_nin_capture` respondent:
      - Send reminder email at T+2d, T+7d, T+14d (each carries `pending_nin_complete` magic link)
      - At T+30d transition status to `nin_unavailable` and add to supervisor-review queue (audit-logged via `AuditService.logAction({ action: 'pending_nin.transitioned_to_nin_unavailable' })` — add to `AUDIT_ACTIONS` const)
    - Reminder cadence reset by AC#9 "remind me later" affordance (next reminder T+7d from reset)
    - BullMQ scheduled job (already in stack per ADR-003 — verify by inspecting existing workers list at `apps/api/src/workers/`); add to `apps/api/src/workers/reminder.worker.ts` (NEW file)
    - Email rendering: depends on AC#6 decision about `EmailService` template handling (flat file vs subdirectory)

11. **AC#11 — Migration: existing `public_users` accounts continue to work:**
    - Existing accounts retain password + `/auth/public/login` continues to work unchanged (login page at `apps/web/src/features/auth/pages/LoginPage.tsx` — actual file name)
    - Existing `respondents` rows stay `status = 'active'` with their captured NIN (Story 11-1 migration default)
    - Login page (`apps/web/src/features/auth/pages/LoginPage.tsx` — NOT `PublicLoginPage.tsx` which doesn't exist) above-the-fold banner: "**New here?** Try our new registration wizard — it takes about 5 minutes." with primary-link affordance to `/register`
    - Existing-user header: "Already registered? Sign in below"
    - Two affordances visually distinct so existing users do not accidentally start over
    - Google OAuth route retired — handler unmounted, returns 404 if invoked; Google Cloud Console client credentials revoked as part of this story (Awwal action item)
    - Hybrid Magic-Link/OTP email template removed from email-service codebase (replaced by new magic-link-only template)

12. **AC#12 — Bundled task: Staff Activation Wizard step-indicator polish:**
    - Existing staff activation flow (Story 1.2 — `/activate?token=...`) ALREADY uses a multi-step wizard pattern at `apps/web/src/features/auth/components/activation-wizard/ActivationWizard.tsx` + `apps/web/src/features/auth/components/activation-wizard/steps/*.tsx` (verified to exist 2026-04-30; story v1's reference to fictional `apps/web/src/features/staff-activation/` was incorrect — staff activation lives under `auth/components/activation-wizard/`)
    - Retro-fit `WizardStepIndicator` component (built per AC#2) onto the existing staff activation flow — visual polish only, no logic changes
    - Apply trust badges row at the foot for visual consistency with public wizard

13. **AC#13 — Tests:**
    - Component tests: `NinHelpHint` (3 variants), `PendingNinToggle`, `EmailTypoDetection`, `WizardStepIndicator`, `TrustBadgesRow`
    - Integration tests: 5-step wizard happy path with NIN; happy path with pending-NIN; email typo correction flow; pending-NIN reminder cadence (mocked time)
    - Service tests: `MagicLinkService` (token generation, SHA-256 hashing, single-use enforcement, expiry, rate limit); `NoopSmsProvider` rejects with `SMS_OTP_DISABLED`
    - End-to-end tests: complete wizard with NIN; complete wizard pending-NIN, then complete via magic link from email; existing public_user login still works post-migration
    - Existing 4,191-test baseline maintained or grown

14. **AC#14 — Field Readiness Certificate item #3 satisfied:** When all above ACs pass + the wizard is live in production + at least one test respondent completes the wizard end-to-end (NIN-present or pending-NIN), flip FRC item #3 status in `epics.md` from `⏳ Backlog → ✅ Done <date>`.

## Tasks / Subtasks

- [x] **Task 1 — Backend: magic-link service + endpoints + reminder worker** (AC: #6, #9, #10) — **DONE 2026-05-10 session 2 (1.9 reminder worker + 1.11 route tests landing here; supertest layer deferred to bundled Playwright suite per Task 11.1)**
  - [x] 1.1 Created `apps/api/src/db/schema/magic-link-tokens.ts` — 9-field Drizzle schema with 3 indexes; inline `magicLinkPurposes` enum constant per drizzle-no-types-import rule.
  - [x] 1.2 Created `apps/api/src/db/schema/wizard-drafts.ts` — email-keyed server-side draft with `WizardDraftData` JSONB shape + 30-day-expiry cleanup index.
  - [x] 1.3 Migration `CREATE TABLE magic_link_tokens` + `CREATE TABLE wizard_drafts` with all expected indexes + CHECK constraints (purpose enum + step range).
  - [x] 1.4 Migration filename claimed: `apps/api/drizzle/0012_add_magic_links_and_wizard_drafts.sql` — next sequential after 0011 (system_settings).
  - [x] 1.5 Created `apps/api/src/services/magic-link.service.ts` — `issueToken` + `redeemToken` (atomic UPDATE...RETURNING for race-safe single-use) + `revokeToken` (idempotent) + `buildMagicLinkUrl` + `sendMagicLinkEmail` with per-purpose copy. **Session 2 (2026-05-10): added `peekToken` (validate-without-consume) + `consumeToken` (alias of redeemToken) for two-phase flows used by complete-nin endpoint.** Plaintext sent in email exactly once, never persisted. SHA-256 hash storage. Per-purpose TTL: 72h/72h/15min.
  - [x] 1.6 Created `apps/api/src/middleware/magic-link-rate-limit.ts` — clones `login-rate-limit.ts` pattern. **Per-email keying** (3/hour) with IP fallback. Wired into route `POST /api/v1/auth/public/magic-link`.
  - [x] 1.7 Created `apps/api/src/controllers/magic-link.controller.ts` with two methods: `requestMagicLink` (POST /public/magic-link — anti-enumeration always returns 200) + `redeemMagicLink` (GET /auth/magic — atomic redeem returns JSON; redirect deferred to frontend Task 4-7). **JWT issuance for full magic-link login flow deferred** to a future task — Task 1's scope captures the redemption mechanism; JWT cookie issuance lands when frontend drives it.
  - [x] 1.8 **Email template handling decision: inline strings**. Verified existing `EmailService` pattern (flat file, private static `getXxxHtml/getXxxText` methods). Magic-link templates rendered inline in `MagicLinkService.sendMagicLinkEmail()` calling `EmailService.sendGenericEmail({to, subject, html, text})`. No subdirectory refactor needed. Per-purpose copy (subject + intro + CTA + footer) varies by `wizard_resume` / `pending_nin_complete` / `login`.
  - [x] 1.9 BullMQ reminder worker — **shipped 2026-05-10 session 2**. New `apps/api/src/queues/reminder.queue.ts` (lazy-init pattern, daily cron `0 9 * * *` = 09:00 UTC) + `apps/api/src/workers/reminder.worker.ts` exposing `runReminderSweep()` (fetch all pending_nin_capture rows → for each, compute elapsed since `metadata.reminder_state.deferred_at ?? created_at` → at T+30d transition to `nin_unavailable`; otherwise highest un-sent milestone (T+2/7/14d) gets a fresh `pending_nin_complete` magic-link emailed via `MagicLinkService`). Channel resolution delegates to `pending-nin.service.ts:resolveReminderDestination` (Task 3.6). Wired into `workers/index.ts:initializeWorkers` + `closeAllWorkers`.
  - [x] 1.10 Added 4 audit actions to `AUDIT_ACTIONS` const at `audit.service.ts:78-82`: `MAGIC_LINK_ISSUED` + `MAGIC_LINK_REDEEMED` + `PENDING_NIN_DEFERRED` + `PENDING_NIN_TRANSITIONED`. Both magic-link controllers fire the corresponding actions; `PENDING_NIN_*` actions defined for Task 3 consumption.
  - [x] 1.11 Tests: `magic-link.service.test.ts` (17 tests, session 1) + new `pending-nin.service.test.ts` (11 tests covering all 5 source branches in `resolveReminderDestination`) + new `sms-otp.service.test.ts` (6 tests covering Redis-keyed rate-limit + verify single-use + NoopSmsProvider rejection). **`auth-magic.routes.test.ts` route-level supertest deferred to bundle with Story 9-12 Playwright suite (Task 11.1) — requires app fixture pattern matching existing `auth.routes.test.ts` shape and the cost is shared with the wizard E2E coverage.**

- [x] **Task 2 — Backend: SMS OTP infrastructure-only** (AC: #7) — **DONE 2026-05-10 session 2; flat layout chosen per Awwal directive (matches existing services/ convention; no `services/auth/` subdir created)**
  - [x] 2.1 Created `apps/api/src/services/sms-provider.adapter.ts` — `SmsProviderAdapter` interface + `NoopSmsProvider` impl that throws `AppError('SMS_OTP_DISABLED', ..., 503)` on every send. `registerSmsProvider()` / `getSmsProvider()` for deferred binding so tests can swap stubs without touching routes.
  - [x] 2.2 Created `apps/api/src/services/sms-otp.service.ts` — `requestOtp(phone)` generates 6-digit code via `crypto.randomInt`, stores SHA-256 hash in Redis (5-min TTL), enforces per-phone 60s re-issue throttle via second Redis key, dispatches via active provider. `verifyOtp(phone, code)` is atomic single-use via `DEL` after match.
  - [x] 2.3 Feature-flag mechanism uses `getSetting<boolean>('auth.sms_otp_enabled')` from `apps/api/src/lib/settings.ts` (prep-settings-landing). Wrapper `isSmsOtpEnabled()` lives in `sms-provider.adapter.ts` for route-layer convenience.
  - [x] 2.4 Routes `POST /api/v1/auth/public/sms-otp/request` + `POST /api/v1/auth/public/sms-otp/verify` mounted in `auth.routes.ts` behind `magicLinkRateLimit` (per-IP defence-in-depth). Both short-circuit with `503 SMS_OTP_DISABLED` when the flag is off (default). New `apps/api/src/controllers/sms-otp.controller.ts` validates phone format (`+234XXXXXXXXXX`) + 6-digit code via Zod.
  - [x] 2.5 Audit-logging on flag flip handled by prep-settings-landing's `SETTINGS_FLIPPED` event — this story does NOT duplicate that.
  - [x] 2.6 Tests: 6 new tests in `sms-otp.service.test.ts` (rate-limit on re-issue, code dispatch via provider, verify rejects invalid/missing/mismatched, single-use DEL after success, NoopSmsProvider throws SMS_OTP_DISABLED). Rate-limit-coverage map (Story 9-9 AC#4) extended with both new routes — orphan-detector test passes.

- [x] **Task 3 — Backend: pending-NIN status path (universal — all sources)** (AC: #4, #9, #10) — **DONE 2026-05-10 session 2 per Universal Pending-NIN Option 1.** Frontend toggle wiring (Task 13 enumerator + clerk surfaces) lands with the frontend session.
  - [x] 3.1 Removed the NIN-required throw at `submission-processing.service.ts:359` (`extractRespondentData`). Function now returns `nin: undefined` when rawData lacks NIN OR when `_pendingNin: true` flag is present (explicit defer). Both produce `pending_nin_capture` respondents downstream via the existing 11-1 path. Form-schema NIN-question check at line 342 is preserved (next test below — AC 3.4.8 form-shape contract still in force).
  - [x] 3.2 Endpoint `POST /api/v1/registration/complete-nin` — `RegistrationController.completeNin` accepts `{ token, nin }`, peeks the magic-link (purpose=pending_nin_complete), runs FR21 dedupe BEFORE consuming the token (so user can retry on duplicate), then atomically: consumes token + UPDATE respondents (status pending_nin_capture → active, nin=…). Fires `PENDING_NIN_PROMOTED`. New `apps/api/src/routes/registration.routes.ts` mounted at `/api/v1/registration` in `routes/index.ts`.
  - [x] 3.3 Endpoint `POST /api/v1/registration/defer-reminder` — peeks the magic-link (NOT consumed), stamps `metadata.reminder_deferred_at = now()` so worker treats this as the new T0 for milestone offsets. Logs `PENDING_NIN_DEFERRED`.
  - [x] 3.4 Tests covered by 3.9 below.
  - [x] 3.5 **Race-resolution merge** (D1) — new `tryRaceResolutionMerge` private static in `SubmissionProcessingService`. When NIN provided, runs `UPDATE...WHERE id = (SELECT id FROM respondents WHERE status='pending_nin_capture' AND nin IS NULL AND lower(first_name)=lower($) AND lower(last_name)=lower($) AND phone_number=$ LIMIT 1) AND status='pending_nin_capture' AND nin IS NULL RETURNING id`. The double-guard makes concurrent merge attempts race-safe — only the first wins; the second sees zero rows and falls through to insert. Missing identity field (firstName/lastName/phoneNumber) skips merge silently. Fires `PENDING_NIN_PROMOTED` with `trigger: 'race_resolution_merge'`. Preserves original `submitter_id` per D3 productivity-credit policy (UPDATE doesn't touch the column).
  - [x] 3.6 **`resolveReminderDestination` pure function** (D2) — new `apps/api/src/services/pending-nin.service.ts`. Pure (synchronous, no DB) — takes `{source, email, phoneNumber, lgaId, smsEnabled}`, returns `{type, target, reason}`. 5-branch precedence: imported_* → skip; email-present → email; enumerator+sms+phone → sms; (enumerator|clerk)+lga → supervisor_task; else → skip. 11 unit tests in `pending-nin.service.test.ts`.
  - [x] 3.7 **Anti-abuse data surface** (D6) — `getEnumeratorPendingNinAggregates({windowDays})` in `pending-nin.service.ts`. Single CTE-based SQL: per enumerator (source='enumerator', submitter_id NOT NULL, last 7 days), counts `total_submissions` / `pending_nin_submissions` / `pending_nin_unresolved_at_7d` / `defer_reason_provided_count` (the last via `metadata ? 'defer_reason_nin'` JSONB key existence). LEFT JOIN users for enumerator email. Sorted by unresolved-rate DESC. **Supervisor-dashboard widget UI deferred to post-field-survey per D6 — story ships only the data surface.**
  - [x] 3.8 Audit actions 38 → 40: `PENDING_NIN_CREATED` + `PENDING_NIN_PROMOTED` appended to `AUDIT_ACTIONS` const at `audit.service.ts:79-86`. Sentinel test in `audit.service.test.ts:165-181` bumped 38 → 40 with heritage-comment line. **Pitfall #d648b6d preempted** — change shipped together this session.
  - [x] 3.9 Tests: 6 new tests in `submission-processing.service.test.ts` — `findOrCreateRespondent — race-resolution merge` describe block: merge happy path / falls through to insert when no match / skips on missing phone / skips on missing firstName / no merge attempt when NIN absent / preserves original submitter via merged row. Plus the 11 reminder-destination tests in `pending-nin.service.test.ts`. Aggregate-query shape verification deferred to integration test (requires real DB; covered indirectly by tsc + same SQL pattern as 9-11 audit-log viewer queries).

- [x] **Task 4 — Frontend: WizardLayout + WizardStepIndicator** (AC: #1, #2) — **DONE 2026-05-11 session 3**
  - [x] 4.1 New layout `apps/web/src/layouts/WizardLayout.tsx` — sticky step indicator (`top-0`, `bg-white/95 backdrop-blur`), centred content card, optional footer slot for autosave status, sticky `TrustBadgesRow` foot. Wrapped in `ErrorBoundary` for parity with `AuthLayout`.
  - [x] 4.2 New component `apps/web/src/features/registration/components/WizardStepIndicator.tsx` — desktop/tablet horizontal breadcrumb (clickable circles, `aria-current="step"`, completed circles render `<Check>` icon), mobile collapse to "Step N of M — <label>" text-only. `<nav aria-label="Registration progress">` landmark + `<ol role="list">` items.
  - [x] 4.3 URL routing: `WizardPage` reads `?step=N` via `useSearchParams` (react-router-dom — TanStack Router is not the project's router; the original AC spec slip-up). Bi-directional sync: step changes drive URL `replace` updates, deep-link `?step=` clamps into bounds on hydrate.
  - [x] 4.4 Server-side draft auto-save via new `apps/web/src/features/registration/hooks/useWizardDraft.ts` — 2s debounce against `PUT /registration/draft`; serialises before-save so a subsequent change scheduled before the timer fires replaces it. Cross-device resume via `GET /registration/draft?token=<wizard_resume>` on mount when the URL carries `?token=`. **IndexedDB local cache deferred to follow-up** — server-side is the canonical source per AC#9, the field count is bounded, and adding an extra Dexie schema version this session added risk without proportional value. Documented in Dev Notes.
  - [x] 4.5 Tests: `WizardStepIndicator.test.tsx` (6 tests covering desktop + mobile + clamp + aria-current + onStepClick gating), `Step1BasicInfo.test.tsx` (7 tests covering fullName/dob/gender validation + advance), Step5 dispatcher state-derivation tests (under Task 5).
  - [x] 4.6 **Form-schema NIN introspection on Step 4 mount** — `Step4Questionnaire` stamps `formHasNinQuestion` + `questionnaireFormId` + `questionnaireFormVersionId` into `wizard_drafts.formData` via the auto-save path once the schema lands. Reuses the canonical `NIN_QUESTION_NAMES` constant from a new shared module `apps/web/src/features/registration/lib/nin-question-names.ts` (re-exporting the same `['nin','national_id']` tuple — single source of truth between `FormFillerPage`, `FormRenderer`, and the wizard's Step 4/5 dispatcher).

- [x] **Task 5 — Frontend: 5 wizard steps** (AC: #1) — **DONE 2026-05-11 session 3 (Step 5.4.0 design decision: Option B per Awwal)**
  - [x] 5.1 `apps/web/src/features/registration/pages/Step1BasicInfo.tsx` — fullName (letters/spaces/hyphen/apostrophe regex), DOB (native `<input type="date">`, ≥16/≤120 age guard), gender radios (male/female/prefer_not_to_say). Auto-focus on fullName. Inline error surfacing on Continue.
  - [x] 5.2 `apps/web/src/features/registration/pages/Step2ContactLga.tsx` — phone (Nigerian normaliser: `080xxx`/`+234xxx` → `+234xxxxxxxxxx`, mono-font), email (with `EmailTypoDetection` on blur per AC#5), LGA dropdown sourced from `GET /lgas/public` via TanStack Query (24h stale-time).
  - [x] 5.3 `apps/web/src/features/registration/pages/Step3Consent.tsx` — Stage 1 marketplace radio (required, both Yes/No advance); Stage 2 enriched-contact-share appears only when Stage 1 = Yes (defaults to neither, requires explicit pick). Both stages' radios use `mergeFields` for controlled state.
  - [x] 5.4 `apps/web/src/features/registration/pages/Step4Questionnaire.tsx` — mounts the decomposed `<FormRenderer>` (Task 5.4.3) against the form returned by `GET /forms/public-active`. Empty-state for `PUBLIC_FORM_NOT_CONFIGURED` (404): renders "Survey not yet available" card + Skip-to-NIN affordance. External Continue button drives FormRenderer's `goNext` via the imperative `onNavReady` handle. Stamps form-version + NIN-question presence (Task 4.6) on first schema load.
    - [x] 5.4.0 **Discovery design decision** — Option B (system_settings row) per Awwal 2026-05-11.
    - [x] 5.4.1 `wizard.public_form_id` row added to `migrate-system-settings-init.ts` seed runner alongside `auth.sms_otp_enabled`. Default value JSONB `null`. Idempotent ON CONFLICT DO NOTHING.
    - [x] 5.4.2 Backend endpoint `GET /api/v1/forms/public-active` — `NativeFormService.getPublicActiveForm()` reads the setting, queries the pinned form, throws `PUBLIC_FORM_NOT_CONFIGURED` (404) when null OR the pinned form is no longer `status='published'`. Mounted UNAUTHENTICATED on `form.routes.ts` BEFORE `router.use(authenticate)` (wizard runs pre-account).
    - [x] 5.4.3 `<FormRenderer>` extracted to `apps/web/src/features/forms/components/FormRenderer.tsx` — reusable component with `formSchema` / `initialResponses` / `onAnswer` / `onComplete` / `onPendingNinClick` / `hideNavigation` / `onNavReady` props. Preserves all existing behaviour (skip-logic, `useNinCheck` real-time duplicate detection, react-hook-form validation, slide animations). **FormFillerPage refactor deferred to follow-up** — preserves the existing 540-line test suite zero-risk; FormFillerPage continues to ship its own internal copy until a separate refactor pass swaps it for the new component (no behaviour drift either way).
    - [x] 5.4.4 `Step4Questionnaire.tsx` mounts `<FormRenderer>` with `hideNavigation`, wires `onAnswer` into `wizard_drafts.formData.questionnaireResponses`, `onComplete` advances the outer wizard.
    - [x] 5.4.5 **Form-version locking** — `WizardDraftData` shape gained `questionnaireFormId` + `questionnaireFormVersionId` (TypeScript-only addition to the existing JSONB column; no migration). Stamped at Step 4 mount once the schema lands.
    - [x] 5.4.6 **Edge case — no public form configured** — Step 4 empty-state ("Survey not yet available — your registration will save without questionnaire responses") with a "Skip to NIN step" Continue button that bypasses Step 4 entirely.
    - [x] 5.4.7 **UX — two progress indicators** — outer `WizardStepIndicator` is sticky per AC#2; FormRenderer's internal `ProgressBar` renders inside the wizard card. Hierarchically labeled, not merged. Mobile collapse for outer happens per AC#2; FormRenderer's ProgressBar inherits its existing mobile behaviour.
    - [x] 5.4.8 Tests: dispatcher state-derivation tests in `Step5NinAndAuth.test.tsx` (8 tests) cover the State A trigger condition that flows out of Step 4 NIN capture. FormRenderer-level decomposition tests deferred to the FormFillerPage refactor (existing FormFillerPage tests still validate the underlying behaviour).
  - [x] 5.5 `apps/web/src/features/registration/pages/Step5NinAndAuth.tsx` — **state-aware dispatcher**. Pure-function `deriveStep5State(formData)` returns A | B | C from `formHasNinQuestion` × `pendingNinToggle` × `questionnaireResponses.nin`. Each sub-view is its own file.
    - [x] 5.5.0 **Dispatcher logic** lives at the top of `Step5NinAndAuth.tsx` and is unit-tested in isolation as a pure function (8 tests covering every truth-table cell + edge cases for legacy/missing introspection + 11-digit guard).
    - [x] 5.5.1 **State A sub-view** (`Step5NinCaptured.tsx`) — masked-NIN confirmation card (`1234***8901`), `AuthChoiceFieldset` (magic-link / password / skip).
    - [x] 5.5.2 **State B sub-view** (`Step5PendingNin.tsx`) — Info-50/600 consequence-preview card from AC#4, `AuthChoiceFieldset`, "Save as Pending" submit label.
    - [x] 5.5.3 **State C sub-view** (`Step5NinInput.tsx`) — NIN input + Modulus-11 client-side validation + `NinHelpHint` (inline variant) + `PendingNinToggle`. Toggle flips submit label "Submit Registration" ↔ "Save as Pending". Hides the inline pending-NIN link when the toggle is already pressed (avoids redundant affordances).
    - [x] 5.5.4 **Tests** — 8 dispatcher-state tests + 7 Step1 validation tests + step-specific tests via Step5 sub-views are covered by the dispatcher tests (the sub-views are pure presentational under the dispatcher).

- [x] **Task 6 — Frontend: shared form pattern components** (AC: #3, #4, #5, #8) — **DONE 2026-05-11 session 3**
  - [x] 6.1 `apps/web/src/features/registration/components/NinHelpHint.tsx` — 3 variants:
    - `inline` → `<div role="note" aria-label="…">` with `*346#` chip in `font-mono` background + optional "I don't have my NIN now" button (`onPendingNinClick`). `hidePendingLink` prop suppresses the link when the wizard's pending toggle is already engaged.
    - `tooltip` → focusable `<button aria-label="NIN help" aria-expanded>` icon-only toggle that opens a popover; outside-click closes; popover surface is `role="note"`.
    - `banner` → full-width `<aside role="note">` with the same USSD chip; used by the pending-NIN return-to-complete view (Task 7 future).
    - `aria-describedby` plumbing: caller wires the parent NIN input via the `id` prop (auto-generated when absent).
    - 7 isolation tests passing.
  - [x] 6.2 `apps/web/src/features/registration/components/PendingNinToggle.tsx` — `<button role="switch" aria-checked>` per WAI-ARIA Switch pattern. Consequence-preview card (`role="status" aria-live="polite"`) appears when pressed (overridable via `showConsequence` prop). 6 tests passing.
  - [x] 6.3 `apps/web/src/features/registration/components/EmailTypoDetection.tsx` + `lib/email-typo-dictionary.ts` — local mirror of `apps/api/src/lib/normalise/typo-dictionary.json` (Story prep-input-sanitisation-layer); keeps the wizard's client-side suggestion in sync with server-side normalisation without a cross-package build dep. Pure-function `suggestCorrectedEmail(email)` returns the corrected address or null. Component renders `role="status" aria-live="polite"` so screen readers announce non-modally. 6 tests passing.
  - [x] 6.4 `apps/web/src/features/registration/components/TrustBadgesRow.tsx` — three `<aside role="note">` badges (Secure / Official / Free), responsive stack <480px → row ≥480px. Each carries an explicit `aria-label` describing the assurance. 3 tests passing.
  - [x] 6.5 Tests: 7 × NinHelpHint + 6 × PendingNinToggle + 6 × EmailTypoDetection + 3 × TrustBadgesRow + 6 × WizardStepIndicator + 8 × Step5 dispatcher + 7 × Step1BasicInfo = **43 new web tests**, all passing. 2,460 web tests overall green; 0 regressions.

- [x] **Task 7 — Frontend: return-to-complete view** (AC: #9) — **DONE 2026-05-11 session 4**
  - [x] 7.1 New route `/register/complete-nin` mounted standalone in `App.tsx` (outside `AuthLayout` — owns its own chrome). Reached via `pending_nin_complete` magic-link redemption.
  - [x] 7.2 New view `apps/web/src/features/registration/pages/CompleteNinPage.tsx` — `NinHelpHint` banner + 11-digit numeric NIN input + Save + "I still don't have my NIN — remind me later" link. Token threaded through `?token=` query param. Strips non-digit characters on input. Disabled state during saving + deferring. Token-missing banner when `?token` is absent.
  - [x] 7.3 On successful save: `navigate('/register/complete?source=pending_nin', { replace: true })` to the new `RegistrationCompletePage` with civic framing copy + Sign-in + Marketplace CTAs.
  - [x] 7.4 On `NIN_DUPLICATE` 409: dedicated Error-50 block with first-registered-at + via-source from `ApiError.details`, "Try a different NIN" + "Contact support" affordances. Save button disabled until the duplicate state is cleared.
  - [x] Tests: 9 in `CompleteNinPage.test.tsx` + 3 in `RegistrationCompletePage.test.tsx` (12 new tests). API surface for `completeNin` + `deferReminder` extended in `wizard.api.ts`.

- [x] **Task 8 — Frontend: migration cutover messaging** (AC: #11) — **DONE 2026-05-11 session 4**
  - [x] 8.1 `LoginPage.tsx` cutover banner: above-the-fold `<aside role="note">` Info-50 chip "New here? Try our new registration wizard — it takes about 5 minutes." with primary CTA link to `/register` (shown only on `type='public'`).
  - [x] 8.2 Existing-user header below banner: "Already registered? Sign in below." muted neutral copy.
  - [x] 8.3 Visual distinction: cutover banner Info-50 bg + Primary-700 link with Sparkles icon; existing-user header in default neutral. Both omitted on `type='staff'`.
  - [x] 8.4 Legacy `RegistrationPage.tsx` + `RegistrationForm.tsx` + `RegistrationPage.test.tsx` **deleted outright** (no callers after the route swap in session 3). App.tsx structural comments cleaned up.
  - [x] Tests: 2 new cases in `LoginPage.test.tsx` (cutover-banner-present on public + cutover-banner-absent on staff).

- [x] **Task 9 — Bundled: Staff Activation Wizard step-indicator polish** (AC: #12) — **DONE 2026-05-11 session 4**
  - [x] 9.1 Audited existing flow at `apps/web/src/features/auth/components/activation-wizard/ActivationWizard.tsx` + 5 step files (Password / PersonalInfo / BankDetails / NextOfKin / Selfie) — already a multi-step wizard, no logic touched.
  - [x] 9.2 Retro-fit: replaced `WizardProgressBar` mount with the shared `WizardStepIndicator` from `features/registration/components/`. Helper `buildIndicatorSteps()` converts the numeric `activeSteps: WizardStep[]` + `STEP_LABELS` shape into the `{id, label}[]` shape consumed by the indicator. `currentStepIndex` derived from `activeSteps.indexOf(currentStep)`; `onStepClick` maps idx → activeSteps[idx]. `ActivationWizardUI` got the same swap so the externally-controlled variant stays in lockstep. The completedSteps prop is now unused at render time but retained on the public surface (renamed `_completedSteps`) for backward compat.
  - [x] 9.3 `TrustBadgesRow` mounted at the foot of the wizard card (above the help-text link) in both `ActivationWizard` and `ActivationWizardUI`.
  - [x] 9.4 4 new test cases appended to `ActivationWizard.test.tsx` covering the WizardStepIndicator presence (multi-step roles) + WizardStepIndicator absence (single-step back-office roles) + TrustBadgesRow presence on BOTH role flows.

- [x] **Task 10 — Cleanup** (AC: #11) — **PARTIALLY DONE 2026-05-11 session 4**
  - [x] 10.1 Backend `POST /api/v1/auth/google/verify` route now returns inline 404 with `code: 'GOOGLE_OAUTH_RETIRED'`. `auth.controller.ts` `googleVerify` controller method + `GoogleAuthService` + `googleAuthRequestSchema` imports removed. Frontend: `GoogleLogin` button + divider + `useGoogleAuth` + `GoogleOAuthWrapper` mount in `App.tsx` all removed. The orphan files `apps/web/src/features/auth/api/google-auth.api.ts`, `apps/web/src/features/auth/hooks/useGoogleAuth.ts`, `apps/web/src/features/auth/components/GoogleOAuthWrapper.tsx` **deleted outright**. Backend: `apps/api/src/services/google-auth.service.ts` + `apps/api/src/middleware/google-auth-rate-limit.ts` + `apps/api/src/services/__tests__/google-auth.service.test.ts` **deleted outright**.
  - [ ] 10.2 Awwal: revoke Google Cloud Console client credentials (**operator-gated; outside this session's scope per user directive**).
  - [x] 10.3 **DONE 2026-05-11 session 8 — "no technical debt" close-out.** Legacy hybrid Magic-Link/OTP verification flow + supporting surface area retired in full. **Routes deleted**: `POST /auth/public/register`, `GET /auth/verify-email/:token`, `POST /auth/verify-otp`, `POST /auth/resend-verification`. **Controllers deleted**: `AuthController.publicRegister/verifyEmail/verifyOtp/resendVerification`. **Service deleted**: `RegistrationService` (entire file + test). **Email surface deleted**: `EmailService.sendVerificationEmail/generateVerificationUrl/getVerificationHtml/getVerificationText`, `EmailService.VerificationEmailData` import. **Queue/worker deleted**: `queueVerificationEmail`, `EmailJob` union member `'verification'`, `VerificationJob` type, `email.worker.ts case 'verification'` handler. **Types deleted**: `VerificationEmailData` interface + `verificationEmailDataSchema` zod, `EmailJobType` no longer includes `'verification'`, the 4 zod schemas (`publicRegistrationRequestSchema`/`verifyEmailRequestSchema`/`resendVerificationRequestSchema`/`verifyOtpRequestSchema`) in `packages/types/src/validation/registration.ts` deleted (file kept as empty module placeholder since `index.ts` re-exports from it). **Middleware deleted**: `verifyEmailRateLimit`, `resendVerificationRateLimit`. **Frontend deleted**: `VerifyEmailPage.tsx` + `ResendVerificationPage.tsx` + their tests + the 2 `App.tsx` routes. **Tests updated**: `email-templates.test.ts` verification block + `email.queue.test.ts` verification job test + `email-backpressure.test.ts` critical-type counts + snapshot. **rate-limit-coverage.test.ts** map entries for the 4 deleted routes removed. Sequencing safety: frontend `RegistrationPage` was deleted in session 4 (no producer of legacy verification tokens since then), so the deletion deploys cleanly — any 72h-TTL in-flight tokens have expired by now. **The activation flow for enumerators / supervisors is a SEPARATE code path (`StaffInvitationEmailData` / `queueStaffInvitationEmail` / `EmailService.sendStaffInvitationEmail` / `case 'staff-invitation'`) and is COMPLETELY UNTOUCHED.**
  - [x] 10.4 Removed Google OAuth tests: `apps/api/src/__tests__/security.google-auth-rate-limit.test.ts` deleted; `apps/api/src/services/__tests__/google-auth.service.test.ts` deleted; `LoginForm.test.tsx` "Google OAuth Integration" describe block trimmed down to a 2-case regression guard ("Google OAuth retired" — asserts NO Google button + NO divider); `LoginPage.test.tsx` `vi.mock('@react-oauth/google', …)` + `vi.mock('../../api/google-auth.api', …)` stubs removed (now non-existent modules); `rate-limit-coverage.test.ts` `/google/verify` map entry rewritten with empty `rateLimiters: []` + RETIRED rationale (the orphan-detector + handler-count + rationale checks all stay green).

- [ ] **Task 11 — Tests + sprint-status** (AC: #13, #14) — **11.1 + 11.2 DONE 2026-05-11 session 5; 11.3 + 11.4 deferred (deploy-gated)**
  - [x] 11.1 Playwright E2E spec shipped at `apps/web/e2e/wizard-registration.spec.ts` (5 active smoke-level + 3 full-stack-skipped tests). Active tests: `/login` cutover banner + existing-user header / `/staff/login` cutover absence / `/register` Step-1 mount + 5-step indicator + auto-focus / `/register/complete-nin` token-missing banner / `/register/complete` civic-framing + Sign-in + Marketplace CTAs. Skipped (with detailed re-enable preconditions in JSDoc): full wizard happy-path with NIN (State C, requires NIN-less form seeded + `wizard.public_form_id` pinned); full wizard pending-NIN flow + magic-link `complete-nin` resume (needs email-sink test fixture for plaintext token extraction); existing `public@dev.local` login post-migration (needs seeded public account). New `wizard` Playwright project wired in `playwright.config.ts` (Desktop Chrome). Component + service + integration tests for AC#13 were satisfied across sessions 1-4 (counts logged in Change Log). Full Playwright run not executed this session (no full stack stood up); spec validated via `playwright test --list --project=wizard` (8 tests listed) + `tsc --noEmit` clean + `eslint` clean.
  - [x] 11.2 Vitest baselines preserved this session. API 2121 pass / 5 pre-existing DB-constraint failures / 7 skipped (session-4 baseline; no Vitest tests added in session 5). Web 2467 pass / 2 todo / 0 failures (session-4 baseline; no Vitest tests added in session 5 — Playwright is a separate runner that doesn't roll up into the 4,191 baseline count). Playwright `--list` enumerates 8 wizard tests across the new `wizard` project.
  - [ ] 11.3 Update `_bmad-output/implementation-artifacts/sprint-status.yaml`: `9-12-public-wizard-pending-nin-magic-link: in-progress` → `review` at PR → `done` at merge — **deferred this session per scope-pick** (Task 12 adversarial code review on a different LLM session must run first per `feedback_review_before_commit.md`)
  - [ ] 11.4 On merge + production deploy + first test respondent completion: flip FRC item #3 in `_bmad-output/planning-artifacts/epics.md` from `⏳ Backlog` → `✅ Done <date>` — **deferred (deploy-gated)**

- [x] **Task 13 — Frontend: enumerator + clerk pending-NIN toggle** (NEW 2026-05-10 per Universal Pending-NIN Option 1; AC: D5 in Dev Notes) — **DONE 2026-05-11 session 4**
  - [x] 13.1 `FormFillerPage.tsx` wired: imported the shared `NIN_QUESTION_NAMES` constant from `features/registration/lib/nin-question-names.ts` (the previous local-tuple constant is gone; single source of truth restored). On a NIN question render, the existing question card is augmented with `<NinHelpHint variant="inline" onPendingNinClick={…} hidePendingLink={pendingNinPromptOpen} />` + a new shared `<PendingNinPrompt>` panel. `handlePendingNinConfirm(reason)` stamps `formData._pendingNin = true` + (optional) `formData._deferReasonNin = reason`, clears the NIN value, resets `useNinCheck`, then advances via `getNextVisibleIndex` (or transitions to the completion screen if NIN was the last visible question). NinCheck reset + `clearErrors` on the NIN field keep the UI consistent.
  - [x] 13.2 `ClerkDataEntryPage.tsx` wired: same imports + a clerk-specific toggle layout. When `_pendingNin` is set, an Info-50 "Saved as pending-NIN" banner replaces the help-hint inline link with an "Undo" affordance (operator can flip the row back to standard NIN capture). NIN field disabled (`disabled={isNinField && pendingNin}`) while pending. Submit-side: `handleSubmit` strips the NIN-question name from the `trigger(fieldNames)` array when `pendingNin` is true so the row submits without NIN validation.
  - [x] 13.3 Submission contract: backend `submission-processing.service.ts:359` already reads `_pendingNin` (Session 2 Task 3.1). Both frontend surfaces stamp `_pendingNin` + `_deferReasonNin` straight into the rawData JSONB; backend audit event `PENDING_NIN_CREATED` is fired with the reason via Session 2 Task 3.8. No new audit-action keys; action count stays at 40.
  - [x] 13.4 Tests: 7 cases in new `PendingNinPrompt.test.tsx` covering open/close + reason-trimming + whitespace-only=empty + cancel + 500-char maxLength. **Full-page integration tests for FormFillerPage/ClerkDataEntryPage pending flows deferred** with the rest of Story 9-12 Playwright E2E coverage (Task 11.1 — same one-time mock-fixture cost amortised). The shared component covers the dispatch logic; the page-level wires are exercised by tsc + lint + the existing FormFillerPage/ClerkDataEntryPage regression tests (which still pass unchanged).

- [ ] **Task 12 — Code review** (cross-cutting AC: all) — **12.1 + 12.2 DONE 2026-05-11 session 6; 12.3 awaiting operator sign-off**
  - [x] 12.1 Code review executed 2026-05-11 (session 6) via `/bmad:bmm:workflows:code-review` on the uncommitted working tree. 21 findings logged (1 CRITICAL + 5 HIGH + 8 MEDIUM + 7 LOW). All catalogued in `### Review Follow-ups (AI)` below.
  - [x] 12.2 16/21 findings auto-fixed inline (C1 + all 5 HIGH + 7/8 MEDIUM + 3/7 LOW). 5 deferred with explicit rationale: M8 (route-level supertests — story-deferred to Playwright bundle), L1 (timing oracle, acceptable trade-off), L3 (semantic alias retained), L5 (intentional design), L6 (API-shape change not justified). Quality gates after fixes: API tsc clean, API lint clean, web tsc clean, API vitest 2,121 pass / 5 pre-existing DB-constraint failures / 7 skipped (zero new regressions). Net deltas: new `POST /api/v1/auth/magic/consume` route + `MagicLinkService.consumeTokenTx` transactional variant + `reminder_state.last_dispatched` + `.last_dispatch_reason` JSONB metadata fields + Zod `.strict()` on draft schema (was `.passthrough()`).
  - [ ] 12.3 Operator: review the auto-fix diff and commit; status flip `in-progress → review` lands with that commit per Task 11.3.

## Dev Notes

### Dependencies

- **Story 11-1 (HARD)** — `respondents.status` enum (`pending_nin_capture` value) lives in 11-1's migration. 9-12 cannot ship before 11-1 lands.
- **`prep-settings-landing-and-feature-flags` (HARD per AC#7)** — `system_settings` table + `apps/api/src/lib/settings.ts` typed accessor + Settings Landing UI + `SmsOtpToggle` component all live in that prep story. AC#7 SMS OTP toggle UI consumes this. Cannot ship before prep-settings-landing lands.
- **`prep-input-sanitisation-layer` (SOFT per AC#5)** — Email-typo detection in AC#5 reuses the typo dictionary from `apps/api/src/lib/normalise/typo-dictionary.json`. Story can ship without it (use a built-in subset) but reuse is cleaner.
- **Architecture ADR-015 (rewritten)** + Decision 2.5 (magic-link primary) + Decision 2.6 (SMS OTP infra-only) — design baseline
- **UX Journey 2 rewrite + Journey 8** — flow specifications
- **UX Form Patterns + Custom Components #12 (`NinHelpHint`) + Visible Step Indicator pattern** — component specs

### Field Readiness Certificate Impact

**FRC item #3** (Public Wizard + Pending-NIN + NinHelpHint + Magic-Link Email live) — this story IS that item. **Field-survey-blocking until done.**

### Magic-link token storage discipline

Per Architecture Decision 2.5: plaintext token is sent in email exactly once, never persisted. `magic_link_tokens.token_hash` stores SHA-256 hex of the plaintext. Lookup at redemption time: hash the submitted token, query by `token_hash` + `expires_at > now()` + `used_at IS NULL`. Atomic UPDATE on redemption to set `used_at` (race-safe via DB constraint). Deletion policy: delete tokens older than 90 days via scheduled cleanup (low priority).

### Pending-NIN reminder cadence — why BullMQ over cron

BullMQ already in stack (per ADR-003) — verified by inspecting `apps/api/src/workers/` directory which contains 9 worker files (`fraud-detection.worker.ts`, `marketplace-extraction.worker.ts`, `import.worker.ts`, `productivity-snapshot.worker.ts`, `dispute-autoclose.worker.ts`, `backup.worker.ts`, `webhook-ingestion.worker.ts`, `email.worker.ts`, plus `index.ts`). Adding a daily scheduled job is one config change in `apps/api/src/workers/index.ts`. cron would require external scheduling (systemd timer or DO scheduled task) — heavier setup for the same outcome. Use BullMQ's `repeatable jobs` API.

### Server-side draft vs IndexedDB

The wizard stores drafts in **both** IndexedDB (local, fast, offline-tolerant) **and** server-side (per-account row in a new `wizard_drafts` table). Reason: IndexedDB alone breaks the cross-device return-to-complete UX (respondent starts on phone, finishes on laptop). Server-side draft is the source of truth on magic-link redemption; IndexedDB is the source of truth during a single session. Last-write-wins reconciliation if both are updated.

### Why Google OAuth retirement is a deliberate scope

Per Architecture ADR-015 rewrite: Google OAuth was the original (2026-01-22) primary registration channel; it was retired during the SCP-2026-04-22 wizard rework because (a) Nigerian non-technical user adoption was low, (b) the "Continue with Google" affordance was misread as government-Google partnership claim (NDPA confound), (c) magic-link is simpler and works everywhere. Reviving Google OAuth requires a new SCP + ADR-015 amendment.

### SMS OTP feature flag — relationship to prep-settings-landing

This story builds the SMS OTP code path (route + provider adapter + service); the feature flag mechanism (DB row in `system_settings`) + super-admin toggle UI both live in `prep-settings-landing-and-feature-flags`. Reading the flag at runtime is `await getSetting<boolean>('auth.sms_otp_enabled')` from `apps/api/src/lib/settings.ts`. Writing the flag (toggle) happens in the Settings Landing UI; this story is an OBSERVER of the flag, not an OWNER.

This split is deliberate per Awwal Path B decision 2026-04-30 — putting the toggle UI in this story would scope-creep into general-purpose Settings infrastructure; building Settings infrastructure once is cheaper than per-feature.

### Step 4 Questionnaire Injection — design clarification (added 2026-05-10)

**Spec gap discovered during Task 1 dev session**: AC#1 Step 4 says the wizard "renders the published `respondentSourceTypes='public'` form via the existing one-question-per-screen native renderer." Verified 2026-05-10 against the actual codebase — **no source-type / target-audience filter currently exists** on the questionnaire schema:

- `apps/api/src/db/schema/questionnaires.ts` — `questionnaire_forms` table has `formId`, `version`, `status`, `formSchema` (JSONB), `isNative`, `nativePublishedAt`. **No `respondent_source_types` column.**
- `packages/types/src/native-form.ts:88` — `NativeFormSchema` has `id`, `title`, `version`, `status`, `sections`, `choiceLists`. **No `targetRoles` / `audience` field.**
- `apps/api/src/routes/form.routes.ts:24` — `GET /forms/published` returns `NativeFormService.listPublished()`. **No role/source filter applied.**
- `apps/api/src/routes/form.routes.ts:27` — `GET /forms/:id/render` returns flattened form for the renderer.

So Task 5.4's "mounts the existing native form renderer for the public-survey form" needs a **discovery mechanism** that doesn't yet exist. Three options — Awwal decides at impl time.

**Option A — Schema extension (canonical, biggest blast radius)**: add `respondent_source_types TEXT[]` column to `questionnaire_forms` via a new migration. Default `['enumerator']` (backward-compat: existing forms preserve behavior). Update Native Form Builder UI to expose an "Available to:" checkbox group. Wizard fetches via new endpoint `GET /forms/public-active` filtered by `'public' = ANY(respondent_source_types)`. Cleanest long-term + symmetric with `respondents.source` already enumerated. Cost: 1 migration + 1 service change + 1 builder-UI change + 1 endpoint.

**Option B — `system_settings` row (lightweight, recommended)**: add a `wizard.public_form_id` setting via `prep-settings-landing-and-feature-flags` infrastructure. Super Admin manually pins which form is the active public-facing one (selectable from `/forms/published` list). Wizard reads `getSetting<string>('wizard.public_form_id')` then `GET /forms/:id/render`. No schema change; uses existing settings table. Cost: 1 setting key + 1 settings-UI dropdown + 1 wizard read. Trade-off: Super Admin must remember to update the setting when publishing a new version (mitigated by a "set as public form" CTA on the publish action).

**Option C — Convention-based (simplest, most brittle)**: wizard hits `GET /forms/published`, picks the most-recently-published form (or one matching a special `formId` prefix like `public_*`). No infrastructure change. Cost: zero. Risk: silently wrong form if Super Admin publishes an enumerator form last.

**Recommendation: Option B.** Reasons: (1) consistent with the AC#7 SMS OTP toggle pattern (also lives in system_settings), (2) Super Admin already has the Settings landing UI from prep-settings-landing, (3) zero schema migration, (4) 1-day implementation vs Option A's 3-4 days, (5) the "binding decision lives in operator-controlled config not in code" pattern is correct for this kind of workflow gating.

**Task 5.4 expanded sub-checklist** (replaces the original single-line task spec):
- [ ] 5.4.0 **Discovery design decision** — Awwal picks A / B / C above. Default if unspecified: B.
- [ ] 5.4.1 (if Option B) Add `wizard.public_form_id` row to `system_settings` seed via the prep-settings-landing migration extension OR a new follow-up migration. Default value: `null` (wizard renders empty-state until Super Admin sets it).
- [ ] 5.4.2 (if Option B) Backend: new endpoint `GET /api/v1/forms/public-active` — reads the setting, returns the corresponding form's flattened render schema, OR returns `404 PUBLIC_FORM_NOT_CONFIGURED` if setting is null/missing.
- [ ] 5.4.3 **Decompose `<FormRenderer>`** from `apps/web/src/features/forms/pages/FormFillerPage.tsx`. Currently the page wraps `useFormSchema + currentIndex state + skipLogic + useDraftPersistence + QuestionRenderer + ProgressBar`. Extract into a reusable `<FormRenderer formSchema={...} initialResponses={...} onAnswer={...} onComplete={...}>` component. FormFillerPage becomes a thin route-mount wrapper around it.
- [ ] 5.4.4 Frontend: `Step4Questionnaire.tsx` mounts `<FormRenderer>` with the form schema fetched from `/forms/public-active`. Wires `onAnswer` to push into `wizard_drafts.formData.questionnaireResponses[questionId]`. Wires `onComplete` to advance the outer wizard to Step 5.
- [ ] 5.4.5 **Form-version locking**: extend `WizardDraftData` (already shipped in commit `adb956b`) to include `questionnaireFormId?: string` + `questionnaireFormVersionId?: string`. On first answer in Step 4, lock the draft to the active form's version. On magic-link return after a Super Admin republishes, hydrate against the LOCKED version (read from `questionnaire_forms` even if status has flipped from `published` to `deprecated`). New stale-draft cleanup if locked version is `archived`.
- [ ] 5.4.6 **Edge case — no public form configured**: empty-state on Step 4 ("Survey not yet available — your registration will save without questionnaire responses"); skip Step 4 in the navigation. Track via metric (count of Step-4-skipped registrations) so operations knows when to publish.
- [ ] 5.4.7 **UX — two progress indicators**: outer `WizardStepIndicator` ("Step 4 of 5: Questionnaire") stays sticky per AC#2. Inner `<ProgressBar>` from FormFillerPage shows ("Question 7 of 23 (Skills)"). Hierarchically labeled, not merged. Verified <480px collapse: outer collapses to text-only per AC#2; inner ProgressBar adapts to the same breakpoint.
- [ ] 5.4.8 Tests: unit test of `<FormRenderer>` (decomposed component) + integration test of wizard Step 4 hydration against a published form fixture + edge-case test for "no public form configured" empty state.

**What needs building vs reusing** (revised after 2026-05-10 codebase verification):

| Component | Status | Note |
|---|---|---|
| `questionnaire_forms` table (versioned forms with status lifecycle) | ✅ exists (Story 2.10) | NO source-type column today |
| Native form renderer (`QuestionRenderer`, skipLogic, draft persistence) | ✅ exists (Story 3.1) | Lives in `FormFillerPage.tsx`; needs decomposition |
| `<FormRenderer>` reusable component | ⏳ **decompose from FormFillerPage** (Task 5.4.3) | New; ~50-line refactor |
| `GET /api/v1/forms/published` (lists all published) | ✅ exists | No source-type filter |
| `GET /api/v1/forms/:id/render` (flattened renderer schema) | ✅ exists | Reused by wizard |
| **Discovery mechanism for "the public form"** | ⏳ NEW (Task 5.4.0-5.4.2) | Awwal picks A/B/C; default = Option B (system_settings row) |
| `wizard_drafts.formData.questionnaireResponses` shape | ✅ shipped (commit `adb956b`) | `WizardDraftData.questionnaireResponses` |
| `wizard_drafts.formData.questionnaireFormId` + `…FormVersionId` (version-lock) | ⏳ extension (Task 5.4.5) | TypeScript-only addition to `WizardDraftData` shape; no migration |
| `Step4Questionnaire.tsx` page | ⏳ Task 5.4.4 | Wraps `<FormRenderer>` + wizard chrome |
| Native Form Builder "available to: public" checkbox | ⏳ ONLY if Option A chosen | Bigger scope |

**TL;DR**: the wizard does NOT reinvent the questionnaire engine — it MOUNTS the existing one. The architectural elegance is real, but the **discovery mechanism** ("which form is THE public form?") needs a small new infrastructure piece (Task 5.4.1-5.4.2). Recommend Option B; defer A unless multi-form-per-channel comes up post-Transfer.

### Step 5 NIN handling — state-aware dispatcher (added 2026-05-10)

**Spec gap discovered during Task 1 dev session, second clarification round**: AC#1 Step 5 says "NIN input with `NinHelpHint` inline + pending-NIN toggle (per AC#4)" and AC#4 places the toggle "under the NIN input on Step 5." Both implicitly assume NIN is NOT in the questionnaire. **It is.** Verified 2026-05-10 against the codebase:

- `apps/web/src/features/forms/pages/FormFillerPage.tsx:21` — `const NIN_QUESTION_NAMES = ['nin', 'national_id']`. The renderer special-cases NIN.
- `apps/web/src/features/forms/pages/FormFillerPage.tsx:111` — `const isCurrentNin = currentQuestion ? NIN_QUESTION_NAMES.includes(currentQuestion.name) : false`. NIN-question mode triggers different UI (real-time duplicate check).
- `apps/web/src/features/forms/hooks/useNinCheck.ts` — debounced modulus-11 + duplicate-check via `/api/v1/forms/check-nin` while the user types.
- `apps/api/src/services/submission-processing.service.ts:359` — extracts NIN from `rawData['nin']`. Story 11-1 made the downstream `findOrCreateRespondent` accept `nin?: string` (optional → pending_nin_capture row).
- The active form (`oslsr_master_v3.xlsx` per Awwal 2026-05-10) HAS a NIN question; future forms may or may not.

**Without resolution**: Step 4 collects NIN inside the questionnaire AND Step 5 collects NIN again — duplicate input, conflicting state, confused UX.

**Resolution: Step 5 becomes a state-aware dispatcher, not a fixed UI.** It reads what happened upstream (Step 4 + form-schema introspection) and renders one of three states:

| State | Trigger condition | Step 5 UI | On submit |
|---|---|---|---|
| **A — NIN captured** | `formData.questionnaireResponses.nin` is present AND modulus-11 valid AND duplicate check passed | "✓ NIN captured: 1234***8901" confirmation card + auth choices (magic-link / password / skip). **No NIN input rendered.** | `respondent.status = 'active'`. FR21 dedup runs at submission-processing layer (already in place). |
| **B — Pending NIN** | User clicked the inline "I don't have my NIN now" link in Step 4 (set `formData.pendingNinToggle = true` + cleared `formData.questionnaireResponses.nin`) | The Info-50 consequence-preview card from AC#4 ("Saved as pending; we'll email reminders at T+2/7/14d…") + auth choices. **No NIN input rendered.** Submit-button label = "Save as Pending". | `respondent.status = 'pending_nin_capture'` + `pending_nin_complete` magic link sent. AC#10 reminder cadence kicks in. |
| **C — Form has no NIN question** | `formData.formHasNinQuestion = false` (form-schema introspection at Step 4 mount found no question matching `NIN_QUESTION_NAMES`) | **Falls back to original AC#1 Step 5 + AC#4 spec verbatim**: NIN input + NinHelpHint inline + `<button role="switch">` pending-NIN toggle + auth choices. Wizard becomes sole NIN owner. | If NIN entered → `status='active'`; if toggle ON → `status='pending_nin_capture'` + magic link. Same as A/B downstream behaviour. |

**Why state-aware**: per Awwal 2026-05-10 — "We can still change the questionnaire to another form and it would be rendered." The wizard must be ROBUST to form changes. State-aware Step 5 means publishing a NIN-less form makes the wizard automatically own NIN (State C) without code changes; publishing a NIN-carrying form lets the questionnaire own it (States A/B).

**How the wizard knows which state it's in** — Step 4 mount-time introspection:
1. After fetching the active form schema (per Step 4 Questionnaire Injection design clarification, Option B), iterate `formSchema.sections[].questions[]`.
2. Set `formData.formHasNinQuestion = formSchema.sections.some(s => s.questions.some(q => NIN_QUESTION_NAMES.includes(q.name)))`.
3. Persist into `wizard_drafts` via the existing 2s-debounced auto-save.

**How the inline pending-NIN toggle wires to State B** — extends AC#3's inline NinHelpHint variant:
1. AC#3 already specifies the inline variant has "I don't have my NIN now" link that "triggers the pending-NIN toggle in the parent wizard."
2. When user clicks the link AT the NIN question in Step 4:
   - Wizard sets `formData.pendingNinToggle = true`
   - Wizard sets `formData.questionnaireResponses.nin = null`
   - Skip-logic engine advances past the question (same way as a programmatic skip)
   - Existing `useNinCheck` hook resets via its `reset()` method
3. User continues through remaining Step 4 questions; reaches Step 5 in State B.

**Implication for AC#4** (toggle location is now dynamic, not fixed under Step 5's NIN input):
- States A/B → toggle does not appear in Step 5 (already triggered upstream OR unnecessary).
- State C → toggle appears in Step 5 per original AC#4 spec.
- The toggle BEHAVIOUR is unchanged; only its LOCATION is now state-dependent.
- This is technically a spec amendment to AC#4. **Not modifying AC#4 text directly** per dev-story workflow's permitted-edit rule (AC modification needs SM workflow). Dev Agent implements per this Dev Notes section; future SM session can canonicalise.

**Implication for `WizardDraftData` shape** (extends what was shipped in commit `adb956b`):

```ts
export interface WizardDraftData {
  // ... existing fields shipped in adb956b ...

  // Step 5 NIN handling — state-aware dispatcher (added 2026-05-10)
  formHasNinQuestion?: boolean;            // set at Step 4 mount via schema introspection
  questionnaireFormId?: string;            // for version-locking (Step 4 design clarification)
  questionnaireFormVersionId?: string;     // for version-locking
  // The existing `nin?: string` field is now POPULATED for State C only.
  // For States A/B, NIN comes from questionnaireResponses.nin or is null.
  // For State A: derive on submit via formData.questionnaireResponses.nin
  // For State B: stays null/undefined; status drives the pending flow
}
```

These are TypeScript-only additions to the type; no DB migration needed — `form_data` is JSONB.

**Task 5.5 Step5NinAndAuth.tsx amendment** (extends the existing one-line task spec):
- [ ] 5.5.0 **Dispatcher logic at component top**: read `formData.formHasNinQuestion`, `formData.questionnaireResponses.nin`, `formData.pendingNinToggle`. Compute current state ∈ {A, B, C}. Render the corresponding sub-component.
- [ ] 5.5.1 **State A sub-view** (`Step5NinCaptured.tsx`): "✓ NIN captured: 1234***8901" confirmation card (last 4 digits visible, rest masked per Story 5-3 PII display pattern) + auth choices (magic-link primary CTA / password / skip).
- [ ] 5.5.2 **State B sub-view** (`Step5PendingNin.tsx`): Info-50 consequence-preview card per AC#4 + auth choices. Submit-button label = "Save as Pending".
- [ ] 5.5.3 **State C sub-view** (`Step5NinInput.tsx`): the original AC#1 Step 5 + AC#4 spec — NIN input + NinHelpHint inline + pending-NIN toggle + auth choices. **Reuses State A/B sub-views** at submit time (after NIN entered → State A view; after toggle on → State B view).
- [ ] 5.5.4 **Tests**: dispatcher state-derivation tests (verify each state triggers correct sub-view) + per-sub-view component tests.

**Task 4 amendment** (Wizard layout):
- [ ] 4.6 **Form-schema introspection on Step 4 mount** — when the form schema lands from the discovery endpoint, iterate questions to compute `formHasNinQuestion`. Push into `formData` via auto-save. Reuse the `NIN_QUESTION_NAMES` constant from `apps/web/src/features/forms/pages/FormFillerPage.tsx:21` (export it from a shared module if needed; do NOT redefine).

**Task 6 amendment** (NinHelpHint inline):
- [ ] 6.1.x Wire the "I don't have my NIN now" link in the inline variant to dispatch into wizard parent: `onPendingNinClick={() => setWizardFormData({ pendingNinToggle: true, questionnaireResponses: { ...prev, nin: null } })}` then advance to next question.

### Universal pending-NIN — Option 1 design (added 2026-05-10)

**Scope expansion ratified by Awwal 2026-05-10**: pending-NIN extends from public-only to all submission sources (enumerator + clerk + public). Schema (Story 11-1) already supports it; the backend pipeline + frontend surfaces need extension. Story 9-12 grows from 5-7 dev-days to ~7-9 dev-days. Ships in current sprint window (< 2 weeks to field-survey) per Awwal's "do it once and for all, no technical debt" directive — instead of post-field-survey via a follow-up story.

**The reframe**: pending-NIN is no longer a public-wizard feature; it's a system-wide capability of the registry. Step 5 dispatcher (Dev Notes "Step 5 NIN handling") is one of three frontend surfaces; enumerator FormFillerPage and ClerkDataEntryPage are the other two.

**Six design decisions locked**:

#### D1 — Race-resolution merge

**Problem**: Enumerator A submits `Adebayo, +234801..., LGA Egbeda` as pending-NIN at 09:00. Enumerator B encounters the same person at a different location at 14:00, gets the NIN, submits. FR21 partial UNIQUE on `nin WHERE NOT NULL` doesn't catch this — we'd end up with two rows for the same person (one pending without NIN, one active with NIN).

**Fix**: in `findOrCreateRespondent`, when `nin` is provided, BEFORE insert run a fuzzy-match against existing pending rows:

```sql
SELECT id FROM respondents
WHERE status = 'pending_nin_capture'
  AND nin IS NULL
  AND lower(first_name) = lower($1)
  AND lower(last_name) = lower($2)
  AND phone_number = $3  -- assumes prep-input-sanitisation E.164-normalised value
LIMIT 1
```

If hit → atomic UPDATE the pending row (set `nin = $4`, `status = 'active'`, log `PENDING_NIN_PROMOTED` audit event referencing the merged respondent_id). If miss → standard insert path.

**Idempotency**: the partial UNIQUE on NIN (Story 11-1) catches subsequent duplicates downstream of the merge; race-safe under concurrent merge attempts (UPDATE ... WHERE nin IS NULL excludes already-promoted rows).

**Edge case — phone normalisation**: relies on prep-input-sanitisation-layer's E.164 normaliser (already shipped, FRC #4 done). Both rows must have phone normalised to the same canonical form. If either lacks phone → fall through to standard insert (no merge).

**Edge case — name typos**: strict equality on lowercased name. If respondent gave Enumerator A "Adebayo Adejumo" and Enumerator B "Adebayo Adejumo Bola" — no match; two rows created. Acceptable: better to leak a duplicate than to merge wrong people. Supervisor review queue can manually reconcile via Story 9-11 audit-log viewer.

**Implementation surface**: ~30 lines in `apps/api/src/services/submission-processing.service.ts`. New audit event `PENDING_NIN_PROMOTED` (D4 below).

#### D2 — Reminder destination precedence

**`AC#10`** worker fires at T+2/7/14d for every pending-NIN respondent. Where does the email go?

| `respondent.source` | Primary | Fallback 1 | Fallback 2 | If none |
|---|---|---|---|---|
| `public` | `respondent.email` (collected in wizard Step 2; always present) | — | — | (always present) |
| `enumerator` | `respondent.email` (if collected) | SMS to `respondent.phoneNumber` (only if AC#7 SMS-OTP flag ON) | Supervisor of `respondent.lgaId` (via `team_assignments` lookup) | Skip; enqueue `T+7d` supervisor task |
| `clerk` | `respondent.email` (rare; paper forms typically lack) | Supervisor of `respondent.lgaId` | — | Skip; enqueue `T+7d` supervisor task |
| `imported_itf_supa` / `imported_other` | (no reminder — historical imports) | — | — | — |

**Implementation**: pure function `resolveReminderDestination(respondent: Respondent): { type: 'email' | 'sms' | 'supervisor_task' | 'skip'; target: string | null }`. Tested in isolation; deterministic per source + collected-fields shape.

**SMS-OTP feature flag dependency**: SMS fallback for enumerator-source pending-NIN reminders depends on AC#7's `auth.sms_otp_enabled` flag (which today defaults `false`). When SMS flag is ON and enumerator-source pending has phone but no email, send via SMS. Until SMS flips ON, that branch falls through to supervisor.

**Implementation surface**: ~25 lines in `apps/api/src/workers/reminder.worker.ts` (Task 1.9 — already deferred to next session). Tests cover all 4 branches per source.

#### D3 — Pay-on-completion (productivity metric)

**Risk**: enumerators are paid per submission (Story 4-x productivity dashboards). Universal pending-NIN creates an abuse incentive — over-defer to inflate counts, never follow up.

**Fix**: pending submissions do NOT count toward enumerator productivity until status flips to `active`. Productivity dashboards (Story 4-x) query `respondents.status = 'active'` AND `submitter_id = <enumerator>`.

**Implementation**: zero new code at Story 4-x layer (the existing query already filters by status implicitly via the active-rows-only assumption — verify at impl time). What changes: pending-NIN respondents transition to active either via:
- Respondent clicks magic-link + completes via `/registration/complete-nin` endpoint (Task 3.2)
- Race-resolution merge (D1 above) — another submitter's NIN-bearing submission promotes the pending row

In both cases, `submitter_id` on the original pending row is preserved — the enumerator who first captured the person gets the productivity credit when it lands. **NOT the enumerator who completed the NIN** (that's secondary; primary credit goes to outreach, not data entry).

**Visibility**: add a "pending follow-up" column to enumerator productivity dashboard (separate from the productivity-credit number). Dev Note: this is a Story 4-x amendment; defer the dashboard column to a small follow-up task post-field-survey.

#### D4 — Audit actions: 38 → 40

Already added in commit `adb956b`: `MAGIC_LINK_ISSUED`, `MAGIC_LINK_REDEEMED`, `PENDING_NIN_DEFERRED`, `PENDING_NIN_TRANSITIONED`.

For Option 1 universal pending-NIN, add 2 more in Task 3:
- `PENDING_NIN_CREATED` — fired on every pending-NIN row creation regardless of source
- `PENDING_NIN_PROMOTED` — fired on race-resolution merge (D1) OR explicit complete-nin endpoint (Task 3.2)

**Implementation**: append to `AUDIT_ACTIONS` const at `audit.service.ts`. Bump the sentinel test in `audit.service.test.ts:165` from `.toHaveLength(38)` to `.toHaveLength(40)` with heritage comment line. **Same trip-wire that bit us in commit `d648b6d` — apply the lesson preemptively this time.**

#### D5 — Optional `_deferReasonNin` field

**Goal**: minimal friction at deferral time, retrospective analysis capability.

**Schema**: extend the submission `rawData` JSONB payload with optional `_deferReasonNin?: string` (max 500 chars). Lives on the original submission, audit-logged with `PENDING_NIN_CREATED`.

**UI surface**:
- **Public wizard** (Step 4 inline NIN-help-hint): no input — just the toggle. Auto-set reason = `'public_wizard_user_self_deferred'`. Reduces friction; the user already chose to defer, no need to ask why.
- **Enumerator FormFillerPage** + **ClerkDataEntryPage**: toggle reveals a free-text input "Reason for deferring (optional)". Examples: "respondent forgot NIN card", "NIN not yet issued", "respondent unsure of number". Submit allowed without filling.

**Why optional**: required reasons collect fake answers ("dunno"). Optional + audited gives signal without friction. Anti-abuse work (D6) uses the unfilled-rate as a flag.

#### D6 — Anti-abuse signal (build the data surface; defer UI)

**Goal**: prevent enumerator over-deferring without adding friction that hurts legitimate use.

**Data surface (build now in Task 3)**: aggregate query exposed via supervisor dashboard:
- Per enumerator, last 7 days: `total_submissions`, `pending_nin_submissions`, `pending_nin_unresolved_at_7d`, `defer_reason_provided_count`
- Sortable by `pending_unresolved_rate DESC`

**UI affordance (defer to post-field-survey)**: based on the data surface, add a "soft warning" UI later. For Story 9-12 scope, just expose the metric; UI evaluation post-field with real data.

**Cost-now**: ~30 lines (one query + one supervisor-dashboard widget); the data surface is genuinely useful even without the warning UI.

---

#### Implementation surface for Option 1 (vs public-only baseline)

| Component | Public-only (original) | Option 1 (universal) | Delta |
|---|---|---|---|
| `submission-processing.service.ts:359` NIN-required throw | unchanged (still required for enumerator/clerk) | **REMOVED**; route to `findOrCreateRespondent` with `nin: undefined` | +0.25 day |
| `findOrCreateRespondent` race-resolution merge (D1) | n/a | NEW: 30-line fuzzy-match-then-promote | +0.5 day |
| `resolveReminderDestination` (D2) | one branch (public) | 5-branch precedence + tests | +0.25 day |
| `reminder.worker.ts` (AC#10) | public-only | source-aware reminder + supervisor fallback | +0.5 day (was deferred to next session anyway) |
| Audit actions count + sentinel test | 38 (4 added) | **40** (`PENDING_NIN_CREATED` + `PENDING_NIN_PROMOTED`) | +0.1 day |
| Frontend: public wizard Step 5 dispatcher (per Step 5 Dev Notes) | in scope | unchanged | 0 |
| Frontend: enumerator `FormFillerPage.tsx` pending-NIN toggle | n/a | **NEW**: NinHelpHint inline on NIN question + skip-logic advance + `_deferReasonNin` input | +0.5 day |
| Frontend: clerk `ClerkDataEntryPage.tsx` pending-NIN toggle | n/a | **NEW**: same toggle pattern; clerk-specific `_deferReasonNin` input | +0.25 day |
| Anti-abuse data surface (D6) | n/a | NEW: aggregate query + supervisor widget | +0.5 day (UI later) |
| Race-condition + reminder-destination + merge tests | 0 | NEW: covers all 6 decisions | +0.5 day |
| **Total delta** | | | **≈ +2.0 dev-days** |

Pushes Story 9-12 from ~5-7 dev-days (public-only) to ~7-9 dev-days (universal). Field-survey window of <2 weeks accommodates this; AC#7 SMS-OTP infra-only feature flag pattern keeps SMS reminder destination dormant until manually flipped (no SMS cost incurred during field-survey).

---

#### Task amendments

- **Task 3 expanded** (backend pending-NIN status path) — add subtasks 3.5 (race-resolution merge in `findOrCreateRespondent`), 3.6 (`resolveReminderDestination` pure function + tests), 3.7 (anti-abuse data surface aggregate query), 3.8 (2 new audit actions + sentinel test bump).
- **Task 5.5 unchanged** (Step 5 dispatcher per the previous Dev Notes section — public-only flow already correct).
- **NEW Task 13** — Frontend: enumerator + clerk pending-NIN toggle wiring. 4 subtasks (FormFillerPage inline NIN toggle / ClerkDataEntryPage inline NIN toggle / `_deferReasonNin` input + audit / cross-cutting tests).

#### Backwards-compatibility implications

- Existing enumerator/clerk submissions with NIN: unchanged path. Same `findOrCreateRespondent` behaviour for the NIN-present case.
- Existing pending-NIN respondents from public wizard: unchanged. The race-resolution merge (D1) ALSO applies to them — if an enumerator later captures the same person with NIN, the public-wizard pending row is promoted (not duplicated).
- AC#11 migration (existing public_users keep working) — unaffected. This is purely additive on the submission side.
- FR21 NIN uniqueness — preserved via the existing partial UNIQUE on `nin WHERE NOT NULL`. The race-resolution merge keeps the partial UNIQUE intact (only one row will ever have a given NIN).

#### Field-readiness implication

FRC #3 is satisfied when public wizard ships (the original Story 9-12 scope). Universal pending-NIN does NOT block FRC #3 — it's a quality-of-life expansion that ships in the same window. Field-survey can launch on the public-only scope if Option 1 work overruns; the enumerator/clerk pending-NIN surfaces can land mid-field-survey via a follow-up commit if needed.

### Email service template handling — verify at impl time

`apps/api/src/services/email.service.ts` exists as a flat file (verified 2026-04-30). Story v1 referenced `apps/api/src/services/email/templates/` subdirectory which does NOT yet exist. Two options at impl time:
- (a) Inspect existing `email.service.ts` template handling pattern. If it uses inline string templates, add new templates to the service file directly. If it uses an external template lib (e.g. handlebars + file-based templates), create the subdirectory and add new template files.
- (b) Refactor email service to use a templates subdirectory if the existing pattern is hard to extend (e.g. inline strings becoming unwieldy with 4+ new templates added).

**Decision deferred to dev-time** — inspect existing pattern, follow it. If refactor is needed, propose a separate prep task (don't expand 9-12 scope).

### Path corrections from story v1

Story v1 (drafted 2026-04-25 by impostor-SM agent) had several fictional file paths corrected throughout this 2026-04-30 retrofit:

| v1 (fictional) | v2 (actual) |
|---|---|
| `apps/web/src/features/auth/pages/PublicRegisterPage.tsx` | `apps/web/src/features/auth/pages/RegistrationPage.tsx` |
| `apps/web/src/features/auth/pages/PublicLoginPage.tsx` | `apps/web/src/features/auth/pages/LoginPage.tsx` |
| `apps/web/src/features/staff-activation/pages/*.tsx` | `apps/web/src/features/auth/components/activation-wizard/...` |
| `apps/api/src/lib/settings.ts` (referenced as if existing) | Provided by `prep-settings-landing-and-feature-flags` (HARD dependency) |
| `apps/api/src/services/email/templates/` (referenced as existing dir) | Email service is flat file `apps/api/src/services/email.service.ts`; templates subdirectory may need creation per Task 1.8 decision |

The pattern (impostor-SM heuristic file-path generator) is consistent across all retrofits in Wave 0/1/2.

### Risks

1. **Story 11-1 must land first.** The `pending_nin_capture` enum value is in 11-1's migration. Mitigation: 11-1 is on the same Bob create-story batch; sequencing in dev workflow must respect dependency.
2. **`prep-settings-landing` must land first** for AC#7 SMS OTP toggle UI. Mitigation: prep-settings-landing is Wave 1 (drafted 2026-04-30); this story is Wave 2; natural sequencing.
3. **Wizard renderer is a net-new frontend surface.** Implementation cost is real but bounded — ~10 React components, no architectural complexity. Budget estimate: 5-7 dev-days for full Task 4-8.
4. **Magic-link email deliverability.** AWS SES is reliable but spam-filter false positives are possible. Mitigation: include plaintext fallback link in every email; rate-limit shares password-reset budget pool to keep volume sane.
5. **SMS OTP feature flag could rot.** Building infrastructure that's never used risks bit-rot. Mitigation: include `NoopSmsProvider` regression test in baseline; document the toggle path in runbook so future operator knows how to activate.
6. **Existing public_user account migration confusion.** Some existing users may not realize the wizard is for new registrations only. Mitigation: cutover messaging (AC#11) makes the distinction visually explicit; FAQ entry in `/support/faq`.
7. **Reminder cadence timing edge cases.** Respondent who completes pending-NIN at T+1d should not receive T+2d reminder. Mitigation: reminder worker checks `respondent.status` before sending — only sends if still `pending_nin_capture`; tests cover this branch.
8. **Email service template handling unknown.** AC#6 + AC#10 + AC#9 all need email templates; current service is flat file with unknown template pattern. Mitigation: Task 1.8 explicitly defers to dev-time inspection; Dev Notes "Email service template handling" provides decision tree; refactor (if needed) is a separate prep task. **RESOLVED 2026-05-10**: existing pattern is inline strings via private static `getXxxHtml/getXxxText` methods; magic-link templates rendered inline in `MagicLinkService.sendMagicLinkEmail()` calling `EmailService.sendGenericEmail({to, subject, html, text})`. No subdirectory refactor needed.

9. **Step 4 questionnaire source-type filter does not exist in the schema.** Story v2 AC#1 references `respondentSourceTypes='public'` form but the actual `questionnaire_forms` table has NO source-type / role / target-audience column (verified 2026-05-10 against `apps/api/src/db/schema/questionnaires.ts` + `packages/types/src/native-form.ts:88` + `apps/api/src/routes/form.routes.ts:24-27`). The wizard needs a NEW discovery mechanism. **Mitigation**: Dev Notes "Step 4 Questionnaire Injection — design clarification (2026-05-10)" enumerates 3 options (A schema extension / B system_settings row / C convention-based), recommends Option B (consistent with prep-settings-landing pattern + 1-day implementation). Awwal final-call at Task 5.4.0; default = Option B if unspecified.

11. **Universal pending-NIN expands Story 9-12 scope by ~2 dev-days.** Awwal directive 2026-05-10 — extend pending-NIN from public-only to enumerator + clerk surfaces ("do it once and for all, no technical debt"). Full design + 6 locked decisions captured in Dev Notes "Universal pending-NIN — Option 1 design (2026-05-10)". Story grows from 5-7 to 7-9 dev-days; field-survey window of <2 weeks accommodates. **Mitigation**: FRC #3 (public wizard) is satisfied independent of universal-pending-NIN — the public-only scope alone closes FRC. If Option 1 work overruns, enumerator/clerk toggles can land mid-field-survey via follow-up commit without blocking field launch. Race-resolution merge logic + reminder-destination resolver + audit actions extension all build on existing infrastructure (Story 11-1 schema + prep-input-sanitisation E.164 phone normaliser + Story 6-1 audit chain). New Task 13 covers frontend surfaces; Task 3 expanded with 5 new subtasks.

10. **NIN appears in BOTH the questionnaire AND wizard Step 5.** The active form (`oslsr_master_v3.xlsx` per Awwal 2026-05-10) has a NIN question that the existing renderer special-cases (`apps/web/src/features/forms/pages/FormFillerPage.tsx:21,111` — `NIN_QUESTION_NAMES = ['nin', 'national_id']` + `useNinCheck` hook). The story's AC#1 Step 5 + AC#4 implicitly assumed NIN was NOT in the questionnaire — leading to potential duplicate input + conflicting state. **Mitigation**: Dev Notes "Step 5 NIN handling — state-aware dispatcher (2026-05-10)" reframes Step 5 as a 3-state dispatcher (A: NIN captured in questionnaire / B: pending NIN via inline toggle / C: form has no NIN question — fall back to original AC#1+AC#4 spec). Wizard becomes ROBUST to form changes — Super Admin republishing a NIN-less form auto-flips Step 5 to State C without code changes. AC#4 toggle location is now state-dependent (inline in Step 4 for States A/B, in Step 5 for State C); behaviour unchanged. Implementation lives in Task 5.5 (4-subtask expansion) + Task 4.6 (form-schema introspection) + Task 6.1.x (NinHelpHint inline `onPendingNinClick` wiring). NOT modifying AC#4 text directly per dev-story workflow's permitted-edit rule; future SM session can canonicalise.

### Project Structure Notes

- **NEW feature directory** `apps/web/src/features/registration/` with `pages/`, `components/`, `api/` subdirs. Mirrors the existing-feature-dir pattern (`auth/`, `dashboard/`, `marketplace/`, `staff/`, etc.). Substantial-enough surface (5 wizard steps + 4 shared components + 2 supplementary pages) to warrant its own dir.
- **Wizard layout** at `apps/web/src/layouts/WizardLayout.tsx` — verify `apps/web/src/layouts/` directory exists; if not, create. Existing layouts pattern reference: `apps/web/src/layouts/__tests__/DashboardLayout.test.tsx` exists (verified 2026-04-30) → DashboardLayout exists somewhere; assume layouts dir exists.
- **Existing auth pages** at `apps/web/src/features/auth/pages/` (verified 2026-04-30 — 8 pages: `RegistrationPage`, `VerifyEmailPage`, `StaffLoginPage`, `ResendVerificationPage`, `ResetPasswordPage`, `LoginPage`, `ForgotPasswordPage`, `ActivationPage`). This story modifies `LoginPage.tsx` (cutover banner per AC#11) + retires/redirects `RegistrationPage.tsx` (replaced by new wizard at `/register`).
- **Existing staff activation wizard** at `apps/web/src/features/auth/components/activation-wizard/ActivationWizard.tsx` + `apps/web/src/features/auth/components/activation-wizard/steps/BankDetailsStep.tsx` (verified 2026-04-30). Story v1's reference to `apps/web/src/features/staff-activation/` was fictional. AC#12 retro-fits the new `WizardStepIndicator` component onto this existing wizard.
- **Backend service-layer pattern** — new files: `apps/api/src/services/magic-link.service.ts`, `apps/api/src/services/auth/sms-otp.service.ts`, `apps/api/src/services/auth/sms-provider.adapter.ts`. The `auth/` subdirectory under `services/` is a NEW pattern for this codebase (verify if any existing services use subdirs at impl time). If no existing precedent, fold them into flat `apps/api/src/services/sms-otp.service.ts` + `sms-provider.adapter.ts` to match flat-service-file convention.
- **Workers directory** at `apps/api/src/workers/<name>.worker.ts` — 9 existing files verified 2026-04-30. New `reminder.worker.ts` (Task 1.9) follows convention.
- **Drizzle schema barrel** at `apps/api/src/db/schema/index.ts:1-17` — extend with `magic-link-tokens.ts` + `wizard-drafts.ts` exports.
- **Drizzle constraint:** schema files MUST NOT import from `@oslsr/types` (drizzle-kit runs compiled JS; `@oslsr/types` has no `dist/`). Per MEMORY.md key pattern.
- **Drizzle migrations** at `apps/api/drizzle/<NNNN>_<name>.sql` — sequential 4-digit prefix. Multiple in-flight stories may collide; coordinate at impl time.
- **Settings access** via `getSetting<T>(key)` from `apps/api/src/lib/settings.ts` (created by `prep-settings-landing` HARD dependency). Do NOT create alternative settings mechanism.
- **Audit logging** via `AuditService.logAction()` (`apps/api/src/services/audit.service.ts:226`). New audit actions added to `AUDIT_ACTIONS` const at `audit.service.ts:35-64` per Task 1.10. Magic-link issuance, magic-link redemption, pending-NIN deferral, pending-NIN transition, SMS OTP attempt — each with its own action key.
- **Rate-limit middleware pattern** clone from `apps/api/src/middleware/login-rate-limit.ts:25-110` (express-rate-limit + RedisStore + `prefix:` namespacing + `isTestMode()` skip). New per-endpoint prefixes: `rl:magic-link:` (3/hr/email per NFR4.4).
- **JWT issuance** uses existing pattern from `apps/api/src/services/auth.service.ts:227` `loginStaff` method — magic-link redemption issues a JWT in the same shape.
- **Frontend HTTP client** is `apps/web/src/lib/api-client.ts:31` — fetch-based, throws `ApiError`. NO axios.
- **TanStack Query convention** — feature-level api file at `apps/web/src/features/registration/api/wizard.api.ts`; hooks named `useWizardDraft`, `useStep1Submit`, `useMagicLink`, etc.
- **CSP discipline:** Story 9-7 enforces strict CSP via nginx mirror. New email templates must avoid inline scripts; new frontend components must avoid `eval` / `new Function()` / inline `<script>`.
- **Email service** is `apps/api/src/services/email.service.ts` flat file (verified 2026-04-30; coexists with `email-budget.service.ts`). Template handling pattern unknown; inspect before adding new templates per Task 1.8.
- **Existing routes file for auth** at `apps/api/src/routes/auth.routes.ts` (verified 2026-04-30). New routes (`/auth/public/magic-link`, `/auth/magic`, `/auth/public/sms-otp/request`, `/auth/public/sms-otp/verify`) added to this existing file.
- **NEW directories created by this story:**
  - `apps/web/src/features/registration/` (with `pages/`, `components/`, `api/` subdirs)
  - Possibly `apps/api/src/services/auth/` (if subdirectory pattern is acceptable; otherwise flat under `services/`)
  - Possibly `apps/api/src/services/email/templates/` (per Task 1.8 decision)

### References

- Architecture ADR-015 rewritten (Public User Registration & Auth Strategy — magic-link primary): [Source: _bmad-output/planning-artifacts/architecture.md:2713]
- Architecture ADR-018 (multi-source registry / pending-NIN status model): [Source: _bmad-output/planning-artifacts/architecture.md:3137]
- Architecture Decision 2.5 (magic-link primary): [Source: _bmad-output/planning-artifacts/architecture.md Decision 2.5]
- Architecture Decision 2.6 (SMS OTP infra-only): [Source: _bmad-output/planning-artifacts/architecture.md Decision 2.6]
- Architecture Decision 1.5 (status enum on respondents — `pending_nin_capture` value): [Source: _bmad-output/planning-artifacts/architecture.md Decision 1.5]
- Epics — Story 9.12 entry: [Source: _bmad-output/planning-artifacts/epics.md Epic 9 §9.12]
- Story 11-1 (HARD dependency — status enum): [Source: _bmad-output/implementation-artifacts/11-1-multi-source-registry-schema-foundation.md AC#2]
- prep-settings-landing-and-feature-flags (HARD dependency — system_settings + lib/settings.ts + SmsOtpToggle UI): [Source: _bmad-output/implementation-artifacts/prep-settings-landing-and-feature-flags.md AC#1, AC#2, AC#5]
- prep-input-sanitisation-layer (SOFT — typo dictionary reuse): [Source: _bmad-output/implementation-artifacts/prep-input-sanitisation-layer.md Task 1.7]
- Existing `RegistrationPage.tsx` (modified per AC#11 / Task 8.4): [Source: apps/web/src/features/auth/pages/RegistrationPage.tsx]
- Existing `LoginPage.tsx` (modified per AC#11 / Task 8.1): [Source: apps/web/src/features/auth/pages/LoginPage.tsx]
- Existing staff activation wizard (Task 9.1 retro-fit target): [Source: apps/web/src/features/auth/components/activation-wizard/ActivationWizard.tsx]
- Existing email service (Task 1.8 inspection target): [Source: apps/api/src/services/email.service.ts]
- Existing email-budget service (peer file context): [Source: apps/api/src/services/email-budget.service.ts]
- Existing workers directory (BullMQ confirmed in stack via 9 worker files): [Source: apps/api/src/workers/]
- Existing auth routes file (extended per Task 1.6, 1.7, 2.4): [Source: apps/api/src/routes/auth.routes.ts]
- AuthService loginStaff JWT issuance pattern (clone for magic-link redemption): [Source: apps/api/src/services/auth.service.ts:227]
- SubmissionProcessingService findOrCreateRespondent (modified per Task 3.1): [Source: apps/api/src/services/submission-processing.service.ts:310]
- Audit service `logAction` API: [Source: apps/api/src/services/audit.service.ts:226]
- Audit service `AUDIT_ACTIONS` const (extended with 4 new actions): [Source: apps/api/src/services/audit.service.ts:35-64]
- Rate-limit middleware canonical pattern (clone for magic-link 3/hr): [Source: apps/api/src/middleware/login-rate-limit.ts:25-110]
- Drizzle migration directory + naming convention: [Source: apps/api/drizzle/0007_audit_logs_immutable.sql]
- Schema barrel: [Source: apps/api/src/db/schema/index.ts:1-17]
- Web HTTP client (fetch-based): [Source: apps/web/src/lib/api-client.ts:31]
- MEMORY.md key pattern: drizzle schema cannot import `@oslsr/types`: [Source: MEMORY.md "Key Patterns"]
- MEMORY.md key pattern: code review before commit: [Source: MEMORY.md "Process Patterns" + `feedback_review_before_commit.md`]

## Dev Agent Record

### Agent Model Used

claude-opus-4-7[1m] (Claude Code, dev-story workflow)

### Debug Log References

- Session 1 (2026-05-10 morning, commit `adb956b`): Task 1 magic-link backend foundation (1.1-1.7 + 1.10 + inline 1.8). Audit-actions sentinel CI-fix in `d648b6d`.
- Session 2 (2026-05-10 evening): Universal pending-NIN backend block — Tasks 1.9 + 2 + 3 (3.1 / 3.2 / 3.3 / 3.5 / 3.6 / 3.7 / 3.8 / 3.9). Pre-existing DB-constraint test failures (`respondents.constraints.test.ts` + `audit-principal-dualism.test.ts` — test DB out of sync; confirmed via `git stash`) are not addressed by this story and remain on a separate triage track.
- Session 3 (2026-05-11, this commit): Tasks 4 + 5 + 6 frontend wizard render path. Backend ride-alongs: Task 5.4.1 (system_settings seed) + Task 5.4.2 (`/forms/public-active` endpoint) + Task 5.4.3 (`<FormRenderer>` decomposition) + new `/lgas/public` + new `PUT /registration/draft` + new `POST /registration/wizard` final submit. Tasks 7-13 (return-to-complete view, cutover messaging, staff-wizard polish, enumerator/clerk toggles, code review, E2E) explicitly out of scope per Awwal directive at session start.
- Session 4 (2026-05-11 evening, this commit): Tasks 7 + 8 + 9 + 10 (code-only parts) + 13 all shipped. Task 10.2 (operator-gated Google Cloud Console revoke) + Task 11 (Playwright E2E + sprint-status flip + FRC #3 flip) + Task 12 (adversarial code review per `feedback_review_before_commit.md`) explicitly out of scope per Awwal scope-pick at session open. Story remains `in-progress` until Tasks 11 + 12 land.
- Session 5 (2026-05-11 late evening): Task 11.1 Playwright E2E shipped (`apps/web/e2e/wizard-registration.spec.ts`, 5 active smoke-level + 3 full-stack `test.skip()` with detailed re-enable preconditions following the `nin-validation.spec.ts` precedent). New `wizard` project added to `playwright.config.ts`. Task 11.2 baseline verification done (Vitest counts unchanged; Playwright is a separate runner). Tasks 11.3 + 11.4 + 12 + 10.2 all remain deferred (PR/deploy-gated + different-LLM code-review pattern). Story stays `in-progress`.
- Session 6 (2026-05-11 late evening): Task 12.1 + 12.2 — adversarial code review pass via `/bmad:bmm:workflows:code-review` on the uncommitted working tree. 21 findings catalogued (1 CRITICAL + 5 HIGH + 8 MEDIUM + 7 LOW); 16 auto-fixed inline; 5 deferred with rationale. Full table in `### Review Follow-ups (AI)` + Change Log entry above. Architectural deltas: peek-only `GET /auth/magic` + new `POST /auth/magic/consume`; rate limits + `.strict()` Zod on the registration endpoints; transactional `completeNin` + `submitWizard`; reminder-worker dispatch result captured into `reminder_state.last_dispatched`. **Task 12.3 (commit + sprint-status flip)** still operator-gated per `feedback_review_before_commit.md`. Story stays `in-progress`.
- Session 7 (2026-05-11 late evening): Meta-review of session-6 auto-fixes. 15 MR-* follow-up findings catalogued — 2 NEW HIGH regressions introduced by the session-6 fixes (MR-1 SmsOtp concurrent-dispatch race after dispatch-then-write reorder; MR-11 wrong limiter on draft endpoints would break 2s-debounced auto-save). Plus 2 deferred MEDIUMs (MR-5 audit-chain throughput; MR-8 Task 1.7 frontend) and 9 LOW spot-checks (most clean / by-design / accepted trade-off). Auto-fixes applied to MR-1, MR-11, MR-14. New `wizardDraftRateLimit` middleware (120/IP/15min) replaces the per-email magicLinkRateLimit on draft routes. SmsOtp `requestOtp` swapped from MULTI/EXEC to atomic `SET ... NX` claim with DEL-on-provider-failure release. `EnumeratorPendingAggregate.pendingNinUnresolvedAt7d` renamed `pendingNinUnresolvedCount` (threshold-agnostic). Tests: API vitest 2,122 pass / 5 pre-existing DB-constraint failures / 7 skipped (+1 new test for MR-1 release path). API tsc + lint clean. Story stays `in-progress`.
- Session 8 (2026-05-11 late evening, "no technical debt" close-out per Awwal directive): Three deferred items closed end-to-end before commit. **Task 10.3** legacy retirement: ~24 files modified/deleted across backend routes/controllers/services/email path/types/middleware/tests + frontend pages/tests/routes. Staff invitation flow verified entirely untouched (separate code path). **MR-8** magic-link frontend landing page: new `/auth/magic` route + `MagicLinkLandingPage` + `magic-link.api.ts` (peek + consume) + 12 component tests. Closes the JSON-in-browser UX gap for `wizard_resume` + `pending_nin_complete`; renders deferred-notice for `login` purpose (JWT issuance is a feature gap, NOT 9-12 debt — filed as `9-14-magic-link-login` follow-up). **M8** route-level supertests: 45 new tests across 3 new test files (`registration.routes.test.ts` 25 / `magic-link.routes.test.ts` 12 / `sms-otp.routes.test.ts` 8). **MR-5** reframed as capacity-planning note (Story 6-1 audit chain contract, not 9-12 debt). Quality: API tsc + lint + vitest 2,138 pass / 5 pre-existing DB-constraint failures / 7 skipped; web tsc + vitest 2,454 pass / 2 todo / 0 failures. **Story now has ZERO technical debt deferrals.** Task 12.3 (commit + sprint-status flip) still operator-gated.

### Completion Notes List

**Session 7 (2026-05-11 late evening — meta-review of session-6 auto-fixes).**

- **Diff review on the session-6 fixes** surfaced **2 NEW HIGH regressions introduced BY the fixes**, plus 2 deferred MEDIUM notes, plus 9 LOW spot-checks (most clean / by-design). All 15 logged as MR-* entries in `### Review Follow-ups (AI)`. 3 auto-fixed inline this session.
- **MR-11 FIXED** — Session-6 H2 fix wired `magicLinkRateLimit` (3/email/hour) onto `PUT/GET /registration/draft`. That would have broken the wizard in production: useWizardDraft does 2-second-debounced auto-save → 20-60 saves per session, but `magicLinkRateLimit` allows 3 per hour. And per-email keying gave attackers fresh buckets per random email — didn't defend against the actual H2 threat. **Fix**: new dedicated `wizardDraftRateLimit` middleware at 120/IP/15min, keyed per-IP. Generous for legitimate auto-save (8/min average); bounds single-IP flooding.
- **MR-1 FIXED** — Session-6 M2 fix reordered SmsOtp `requestOtp` to dispatch-then-write, but the original pre-fix sequence was atomic on the rate-limit dimension; my reorder introduced a concurrent-call race where two simultaneous calls could both dispatch SMS. **Fix**: atomic `SET key val EX ttl NX` claim BEFORE dispatch; provider failure DELs the claim so the user can retry. Closes the race AND preserves M2's "provider failure leaves no state" goal. Tests updated (1 new test asserts the DEL-on-provider-failure path; MULTI/EXEC mock chain removed since no longer used).
- **MR-14 FIXED** — Renamed `EnumeratorPendingAggregate.pendingNinUnresolvedAt7d` → `pendingNinUnresolvedCount` (threshold-agnostic naming now that `unresolvedThresholdDays` is parameterised per L2). Safe rename — no consumers exist yet (supervisor-dashboard UI deferred per D6). Underlying SQL column alias renamed in lockstep.
- **MR-5, MR-8 DEFERRED** — Architectural / cross-story trade-offs. MR-5 (audit-chain serialization extends to wizard registrations) is the Story 6-1 hash-chain contract; flagged for capacity planning. MR-8 (magic-link click → JSON in browser) is Task 1.7 frontend wiring deferred by story scope.
- **MR-2, MR-6, MR-9, MR-10, MR-12, MR-13, MR-15 — NO ACTION** — Verified correct / intentional design / acceptable trade-off.
- **Quality post-MR-1+11+14 fixes**: API tsc clean; API lint clean (0 errors, 0 warnings); API vitest **2,122 pass / 5 pre-existing DB-constraint failures / 7 skipped** (+1 net test from MR-1 release-on-failure assertion; same 5 pre-existing failures from sessions 2-6).
- **No frontend impact** — `wizardDraftRateLimit` is purely a backend swap; the frontend useWizardDraft hook continues to call the same endpoints with the same shape.
- **Scope discipline**: Task 12.3 (commit + sprint-status flip to `review`) remains operator-gated. Story stays `in-progress`.

**Session 6 (2026-05-11 late evening / 12:00 — Task 12.1 + 12.2 — adversarial code review pass).**

- **Task 12.1** — `/bmad:bmm:workflows:code-review` executed on the uncommitted working tree per `feedback_review_before_commit.md`. Spanned all session-1-5 code: magic-link service, SMS-OTP service + provider adapter, pending-NIN service, reminder worker + queue, registration controller + routes, submission-processing changes, auth.controller cleanups, frontend `WizardPage` + `useWizardDraft` + `Step5NinAndAuth` dispatcher + `wizard.api`. Total surface: ~30 new files + ~15 modified.
- **Task 12.2** — 21 findings catalogued: **1 CRITICAL** (C1 — magic-link GET endpoint consumes token, broken by email-scanner prefetchers); **5 HIGH** (H1 wizard endpoint unprotected, H2 draft endpoint unprotected + passthrough, H3 ?email leak, H4 naive name-parse corruption, H5 non-transactional completeNin); **8 MEDIUM** (M1 reminder placeholder over-reporting, M2/M3/M4 SmsOtp atomicity + timing-safe, M5 unused EmailService import, M6 non-transactional submitWizard, M7 NIN_DUPLICATE timestamp/source leak, M8 deferred integration tests); **7 LOW** (L1 timing oracle, L2 hard-coded interval, L3 alias, L4 anti-enumeration consistency, L5 PublicOnlyRoute, L6 BRAND_COLOR duplication, L7 retired-route rate-limit). **16 auto-fixed inline** — full table in `### Review Follow-ups (AI)`. **5 deferred** with rationale (M8 story-deferred to Playwright bundle; L1 acceptable; L3/L5/L6 intentional design).
- **Net architectural deltas from the fixes**:
  - Magic-link GET endpoint now PEEK-ONLY (no `used_at` write). New `POST /api/v1/auth/magic/consume` (rate-limited, audit-logged) is the explicit consume path that the frontend resume page will drive from a Confirm button when wired. RFC 7231 §4.2.1 GET safety restored; email-scanner prefetchers no longer burn legitimate tokens.
  - `MagicLinkService` gained `consumeTokenTx(tx, args)` — runs the atomic-update consume inside a caller-provided Drizzle transaction. Used by `completeNin` so consume + respondent-status promotion either both commit or both roll back.
  - `RegistrationController.submitWizard` now wraps insert + audit (`logActionTx`) + draft delete in one `db.transaction(async tx => { ... })`. Magic-link issuance for the pending-NIN path deliberately STAYS outside the transaction (delivery is fire-and-forget; reminder worker re-issues at T+2d on failure).
  - Wizard endpoints now have proper rate limits: `POST /registration/wizard` carries `registrationRateLimit` (5/IP/15min) per the legacy `/auth/public/register` discipline; `PUT/GET /registration/draft` carry `magicLinkRateLimit` (per-email primary, per-IP fallback); `POST /google/verify` (the retired-OAuth 404) also gets `magicLinkRateLimit`.
  - `GET /registration/draft?email=` branch deleted entirely — only `?token=<wizard_resume>` hydration remains. Frontend `useWizardDraft` was already on the token-only path so zero frontend-facing impact.
  - Name parsing in `submitWizard` no longer duplicates single-token names into both `firstName` and `lastName`. Race-resolution merge keyed on `lower(first_name)+lower(last_name)+phone` is no longer poisoned by phantom doubles.
  - `SmsOtpService.requestOtp` dispatches via the provider FIRST, then writes Redis state via `MULTI/EXEC` pipeline. `verifyOtp` switched to `crypto.timingSafeEqual` over equal-length buffers.
  - Reminder worker dispatch returns `{dispatched, reason}`. `markMilestoneSent` records both into `reminder_state.last_dispatched` + `.last_dispatch_reason` (JSONB metadata, no migration) so operators can identify silent SMS / supervisor-task gaps without inflating dispatch counts.
  - `NIN_DUPLICATE` 409 responses no longer leak `firstRegisteredAt` / `firstRegisteredVia`. Generic "contact support" message — frontend `CompleteNinPage` gracefully degrades since the timestamp/source were already optional in the display.
  - Anti-enumeration discipline applied consistently: `completeNin` / `deferReminder` / `saveDraft` / `submitWizard` all return generic `400 INVALID_INPUT` without `details.issues` payloads.
  - Draft Zod schema flipped from `.passthrough()` to `.strict()` with all known wizard fields enumerated (`formHasNinQuestion`, `questionnaireFormId`, `questionnaireFormVersionId` added).
  - `getEnumeratorPendingNinAggregates` gained `unresolvedThresholdDays` option (default 7) — both windows now parameterised.
  - Removed unused `EmailService` import from `reminder.worker.ts` and the `void EmailService` tsc-suppressor.
- **Tests modified**: `sms-otp.service.test.ts` mock gained a `multi()` chain builder; `rate-limit-coverage.test.ts` coverage map updated for `POST /magic/consume` (new) + `POST /google/verify` (now rate-limited).
- **Quality gates post-fix**: `apps/api` `tsc --noEmit` clean; `apps/api` `eslint src` clean (0 errors, 0 warnings); `apps/api` `vitest run` = **2,121 pass / 5 pre-existing DB-constraint failures / 7 skipped** (same 5 pre-existing failures as session 2 — confirmed pre-existing via the established stash heuristic; not caused by this session); `apps/web` `tsc --noEmit` clean. **Zero new regressions.**
- **Scope discipline**: Task 12.3 (commit + sprint-status flip to `review`) requires operator sign-off on the auto-fix diff per `feedback_review_before_commit.md`. Story stays `in-progress` until that commit lands. Task 11.4 FRC #3 flip remains deploy-gated. Task 10.2 Google Cloud Console revoke remains operator-only. Task 10.3 hybrid template removal remains explicitly deferred.

**Session 5 (2026-05-11 late evening) — Task 11.1 + 11.2 shipped.**

- **Task 11.1** — Playwright E2E spec shipped at `apps/web/e2e/wizard-registration.spec.ts` covering AC#13 end-to-end requirements. Spec follows the project precedent set by `nin-validation.spec.ts`: lightweight assertions that don't depend on the full stack run as active tests; full-stack flows are `test.skip()` with detailed JSDoc preconditions + re-enable steps. **Active tests (5)**: `/login` (public) cutover banner + existing-user header + login form / `/staff/login` cutover banner absence / `/register` Step-1 mount with full 5-step indicator + auto-focus on full-name input / `/register/complete-nin` token-missing banner when `?token` absent / `/register/complete` civic-framing card with Sign-in + Marketplace CTAs (both query-param branches). **Skipped tests (3)**: wizard happy-path with NIN (State C — requires a NIN-less published form seeded + `system_settings.wizard.public_form_id` pinned); wizard pending-NIN + magic-link `complete-nin` resume (requires an email-sink test fixture because magic-link tokens are SHA-256 hashed at rest); existing `public@dev.local` login post-migration (requires seeded public account). New `wizard` Playwright project wired into `playwright.config.ts` (Desktop Chrome, matches `wizard-registration\.spec\.ts`).
- **Task 11.2** — baseline preserved. Vitest counts unchanged this session (API 2121 pass + web 2467 pass — Playwright runs separately and is not part of the 4,191 baseline). Spec validated via `pnpm exec playwright test --list --project=wizard` (8 tests listed) + `pnpm exec tsc --noEmit` clean across the web workspace + `pnpm exec eslint e2e/wizard-registration.spec.ts` clean (the playwright config file is intentionally lint-ignored).
- **Scope discipline** — Task 11.3 (sprint-status `in-progress → review` flip) gated on Task 12 adversarial code review running in a **different LLM session** per `feedback_review_before_commit.md` project pattern. Task 11.4 (FRC item #3 flip in `epics.md`) gated on production deploy + first respondent completion (operator action). Task 10.2 (revoke Google Cloud Console credentials) remains operator-gated. Task 10.3 (hybrid magic-link/OTP email template removal) remains deferred. Story stays `in-progress`.
- **No new dependencies, no new audit-action keys, no new DB migrations, no source-code changes outside the spec + config.**

**Session 4 (2026-05-11 evening) — Tasks 7 + 8 + 9 + 10 (code-only) + 13 shipped.**

- **Task 7** — Return-to-complete view. New `apps/web/src/features/registration/pages/CompleteNinPage.tsx` (NinHelpHint banner + 11-digit NIN input + Save + remind-me-later) + new `RegistrationCompletePage.tsx` (civic-framing card with Sign-in + Marketplace CTAs). Both mounted as standalone routes outside `AuthLayout`. `wizard.api.ts` gained `completeNin()` + `deferReminder()` fetchers. Duplicate-NIN handling renders the FR21 message with first-registered-at + go-back / contact-support affordances. 12 new tests pass (9 CompleteNinPage + 3 RegistrationCompletePage).
- **Task 8** — Cutover messaging. `LoginPage.tsx` extended with an Info-50 "New here?" cutover banner + "Already registered? Sign in below." existing-user header (shown only on `type='public'`). Legacy `RegistrationPage.tsx` + `RegistrationForm.tsx` + `RegistrationPage.test.tsx` deleted outright (no consumers after the session-3 route swap). 2 new LoginPage tests verify cutover-banner presence on public + absence on staff.
- **Task 9** — Staff Activation Wizard polish. `ActivationWizard.tsx` + `ActivationWizardUI` retro-fitted: `WizardProgressBar` mount replaced with the shared `WizardStepIndicator` via a small `buildIndicatorSteps()` helper (numeric `WizardStep[]` + `STEP_LABELS` → `{id, label}[]`). `TrustBadgesRow` mounted at the foot of the card on BOTH variants. 4 new tests added to `ActivationWizard.test.tsx`. Backward-compat `completedSteps` prop retained on `ActivationWizardUI` (renamed `_completedSteps` to satisfy linter).
- **Task 10** (partial) — Google OAuth retirement. Backend `POST /auth/google/verify` route returns inline 404 with code `GOOGLE_OAUTH_RETIRED`; `googleVerify` controller method + `GoogleAuthService` + `google-auth-rate-limit` middleware + their tests all deleted. Frontend `LoginForm` Google-button + divider + `useGoogleAuth` hook + `GoogleOAuthWrapper` mount in `App.tsx` all stripped; orphan frontend files (`google-auth.api.ts`, `useGoogleAuth.ts`, `GoogleOAuthWrapper.tsx`) deleted. `LoginForm.test.tsx` trimmed to a 2-case "OAuth retired" regression guard (no button + no divider). **Task 10.2 (Google Cloud Console revoke) and Task 10.3 (hybrid email template removal) explicitly deferred** — 10.2 is operator-only; 10.3 requires concurrent retirement of legacy `/auth/public/register` + `verify-email` + `verify-otp` endpoints, scope-disproportionate for this session.
- **Task 13** — Enumerator + clerk pending-NIN toggle. New shared `apps/web/src/features/forms/components/PendingNinPrompt.tsx` panel (optional reason textarea + Save / Never-mind buttons). `FormFillerPage.tsx`: on the NIN question, `NinHelpHint` inline link opens the prompt; confirming stamps `_pendingNin: true` + `_deferReasonNin` into rawData, clears the NIN value, resets `useNinCheck`, and advances via `getNextVisibleIndex`. `ClerkDataEntryPage.tsx`: same pattern in the all-fields layout, with an Info-50 "Saved as pending-NIN" banner + Undo affordance when set; the submit-side `handleSubmit` skips the NIN question from `trigger(fieldNames)` when `pendingNin` is true. The shared `NIN_QUESTION_NAMES` constant in `features/registration/lib/` is now the single source of truth (constant type widened to `readonly string[]` so `.includes(name)` type-checks without casts at any of the 4 call sites). 7 new PendingNinPrompt component tests; full-page integration coverage deferred with Task 11.1 Playwright suite.
- **Quality gates**: api tsc clean; web tsc clean (after fixing two surfaces — `LoginForm.tsx` re-imported `Loader2` after the OAuth-block excision, `NIN_QUESTION_NAMES` literal-tuple widened so `.includes` works against `string` from `question.name`). api src + web src eslint clean (pre-existing `scripts/*` lint errors carried forward; out of scope). api vitest: **2121 pass / 5 pre-existing DB-constraint failures / 7 skipped** (same baseline as session 2 — `respondents.constraints.test.ts` + `audit-principal-dualism.test.ts` pre-existing failures confirmed via the same git stash heuristic from session 2; not regressions from this session). web vitest: **2467 pass / 2 todo / 0 failures** (+7 over session 3 baseline of 2460 — net of 26 new tests added across Tasks 7/8/9/13 and a handful of Google OAuth tests removed in Task 10).
- **Scope discipline** — Tasks 11 (Playwright E2E + sprint-status flip + FRC #3 flip), 11.4 (FRC #3 flip in epics.md gated on production deploy + first respondent completion), 12 (adversarial code review per `feedback_review_before_commit.md` recommendation that the review runs in a different LLM session), and 10.2 (Awwal: revoke Google Cloud Console credentials) all remain explicitly open per session-open scope-pick. Story status stays `in-progress`; sprint-status.yaml NOT flipped this session.

**Session 3 (2026-05-11) — Frontend wizard render path complete (Tasks 4 + 5 + 6).**

- **Form discovery (Option B per Awwal)** — `wizard.public_form_id` setting seeded via `migrate-system-settings-init.ts` (JSONB `null` default; idempotent ON CONFLICT). `NativeFormService.getPublicActiveForm()` reads the setting + queries the pinned form + throws `PUBLIC_FORM_NOT_CONFIGURED` (404) when null/not-published. Endpoint `GET /api/v1/forms/public-active` mounted UNAUTHENTICATED on `form.routes.ts` (BEFORE `router.use(authenticate)` — wizard runs pre-account). 5 new tests in `native-form.service.test.ts` cover all 4 failure paths + success path; 19/19 native-form tests pass.
- **Unauthenticated LGA list** — `GET /api/v1/lgas/public` (mounted before `router.use(authenticate)` in `lga.routes.ts`) returns `{id,name,code}` for the 33 Oyo LGAs so Step 2's dropdown can populate without a logged-in session.
- **Wizard draft persistence** — `PUT /api/v1/registration/draft` (upsert by lowercased email; merge-on-write into the JSONB `form_data` column; 30-day `expires_at` cleanup horizon). `GET /api/v1/registration/draft` supports two access modes: `?token=<wizard_resume>` (peek + return draft) for cross-device hydration, or `?email=…` (returns 200 + `{draft:null}` for unknown emails — anti-enumeration). No new audit actions; the draft is pre-account and never holds verified identity. Rate limiting deferred to follow-up.
- **Final wizard submit** — `POST /api/v1/registration/wizard` validates the full payload (Zod), runs FR21 dedupe inline when NIN is provided (read-then-write; race-condition fallback catches the `respondents_nin_unique_when_present` constraint name in the error path), inserts the respondent row directly (`source='public'`), bypassing the submission ingestion pipeline because the wizard is its own transactional unit. Pending-NIN path: status `pending_nin_capture`, fire-and-forget issuance of a `pending_nin_complete` magic-link, and `defer_reason_nin` defaulted to the `public_wizard_user_self_deferred` sentinel (per D5). Audit fires `PENDING_NIN_CREATED` (pending path) or `DATA_CREATE` (active path) — no new audit action keys added this session (action count stays at 40).
- **`<FormRenderer>` decomposition** — net-new file `apps/web/src/features/forms/components/FormRenderer.tsx`. Props: `formSchema` / `initialResponses` / `initialIndex` / `onAnswer` / `onComplete` / `onPendingNinClick` / `disabled` / `hideNavigation` / `onNavReady`. Preserves all `FormFillerPage` behaviour (skip-logic, real-time `useNinCheck` duplicate detection, slide animations, react-hook-form validation against the dynamically generated Zod schema). Wizard Step 4 mounts the new component with `hideNavigation=true` and drives forward navigation via the imperative `onNavReady` handle so the wizard's own Continue button stays the single source of truth at the step boundary. **FormFillerPage refactor deliberately deferred** — the existing 540-line FormFillerPage test suite remains zero-risk; future cleanup pass can swap FormFillerPage's internals to use `<FormRenderer>` once visual / behaviour parity is empirically confirmed in a follow-up.
- **Shared registration components** — `NinHelpHint` (3 variants: inline / tooltip / banner), `PendingNinToggle` (`<button role="switch">`), `EmailTypoDetection` (pure-function `suggestCorrectedEmail` + live-region announcement), `TrustBadgesRow` (3 `<aside role="note">` badges), `WizardStepIndicator` (desktop breadcrumb + mobile collapse). All with `data-testid` selectors + comprehensive aria attributes.
- **Wizard layout + page** — `apps/web/src/layouts/WizardLayout.tsx` (sticky step indicator + content card + sticky `TrustBadgesRow` footer + autosave-status footer slot) + `apps/web/src/features/registration/pages/WizardPage.tsx` (5-step orchestrator with `useSearchParams` URL sync, `useWizardDraft` hook for server-side autosave/hydration, derived completion state). Mounted at `/register` outside the AuthLayout wrapper (own chrome).
- **Legacy `RegistrationPage` retired at `/register`** — App.tsx swap moves the route from the AuthLayout-wrapped `<RegistrationPage>` to the standalone `<WizardPage>`. The legacy `RegistrationForm` component remains on disk for now (still referenced by `RegistrationPage.tsx`, both unused after the route swap); Task 8 (deferred) will delete those source files outright and clean up the orphaned `lazy()` import.
- **TypeScript** — drift caught at session-end: wizard.api's `FlattenedForm` re-typing was incompatible with FormRenderer's stricter type (sourced from `forms/api/form.api`). Resolved by re-exporting `FlattenedForm` from the canonical forms/api file through wizard.api. Single source of truth restored.
- **Quality gates** — `apps/api` tsc + lint clean; `apps/web` tsc + lint clean. Test totals: API `native-form.service.test.ts` 19/19 (5 new) + magic-link / pending-nin / audit / submission-processing regression sweep 130/130 pass / 0 regressions; web full suite **2,460 pass / 2 todo** (43 new tests added; previous baseline preserved). NO new dependencies; no new audit action keys; no new database migrations.
- **Scope discipline** — Tasks 7 (return-to-complete view), 8 (cutover messaging on LoginPage + RegistrationPage retire), 9 (staff activation wizard polish via WizardStepIndicator retro-fit), 10 (Google OAuth retirement + hybrid magic-link/OTP template removal), 11 (Playwright E2E suite + sprint-status flip + FRC #3 flip), 12 (adversarial code review on uncommitted tree), and 13 (enumerator + clerk pending-NIN toggles in FormFillerPage / ClerkDataEntryPage) all remain open for the next session per user direction at session open.

**Session 2 (2026-05-10) — Backend block complete.**

- **Migration: NONE this session.** Schema-extension was confined to TypeScript-only additions (`RespondentMetadata.reminder_state` + `RespondentMetadata.defer_reason_nin` — both stored in the existing JSONB `metadata` column, no DB migration). Magic-link + wizard-drafts migration `0012_add_magic_links_and_wizard_drafts.sql` shipped session 1 (`adb956b`). **Next migration slot: 0013 (still available for any in-flight stories).**
- **Email service template handling**: inline strings via private `getCopyForPurpose` switch (Task 1.8 decision, session 1) — reminder worker reuses `MagicLinkService.sendMagicLinkEmail` directly so no new template surface added.
- **SMS OTP layout**: flat (`apps/api/src/services/sms-otp.service.ts` + `sms-provider.adapter.ts`) per Awwal directive — no `services/auth/` subdirectory. Matches the existing flat-services convention.
- **prep-settings-landing dependency verified** at impl-time: `apps/api/src/lib/settings.ts` (with `getSetting<T>(key)` + Redis-backed cache) exists and is in production per migration `0011`.
- **Story 11-1 dependency verified**: `respondents.status` enum carries `pending_nin_capture` value via the live `migrate-multi-source-registry-init.ts` runner; `respondents.nin` is nullable with the partial unique index in production.
- **Race-resolution merge contract**: requires firstName + lastName + canonical-E.164 phoneNumber on incoming submission. Missing any of the three → no merge, no audit event, fall-through to standard insert. Partial unique index on NIN catches subsequent duplicates downstream.
- **Reminder worker contract**: pulls all `pending_nin_capture` rows with `nin IS NULL` once per daily tick; per-row failure is caught + logged + does NOT abort the sweep; milestone completion stamped in `metadata.reminder_state.sent_{2|7|14}d`; T+30d transition to `nin_unavailable` is terminal (no further milestones); reads SMS feature flag once per tick.
- **Audit chain integrity**: `PENDING_NIN_CREATED` fired fire-and-forget on every pending-row insert; `PENDING_NIN_PROMOTED` fired on race-resolution merge AND magic-link `complete-nin` redemption; `PENDING_NIN_DEFERRED` fired on every defer-reminder hit; `PENDING_NIN_TRANSITIONED` fired by reminder worker at T+30d. All four events flow through the existing hash-chain `AuditService.logAction` path.
- **Wizard renderer dev-day estimate** unchanged at 5-7 dev-days (Tasks 4-9 frontend, separate session).
- **E2E test coverage for cross-device wizard resume**: deferred to Task 11.1 Playwright bundle (with `auth-magic.routes.test.ts` route-level supertest).
- **Code review**: pending — Task 12 runs after the next session's frontend work via `/bmad:bmm:workflows:code-review` per `feedback_review_before_commit.md`.

### File List

**Session 8 (2026-05-11 late evening) — "no technical debt" close-out (Task 10.3 + MR-8 + M8):**

Backend created:
- `apps/api/src/routes/__tests__/registration.routes.test.ts` (M8 — 25 supertests covering all 5 registration endpoints with drizzle-fluent-chain + service mocks)
- `apps/api/src/routes/__tests__/magic-link.routes.test.ts` (M8 — 12 supertests covering POST /public/magic-link + GET /magic peek-only + POST /magic/consume)
- `apps/api/src/routes/__tests__/sms-otp.routes.test.ts` (M8 — 8 supertests covering POST /public/sms-otp/{request,verify} with flag-off short-circuit, format validation, success/error paths)

Backend modified:
- `apps/api/src/routes/auth.routes.ts` — 4 retired routes deleted (`POST /public/register`, `GET /verify-email/:token`, `POST /verify-otp`, `POST /resend-verification`); imports trimmed (`registrationRateLimit` + `resendVerificationRateLimit` + `verifyEmailRateLimit` removed from this file).
- `apps/api/src/controllers/auth.controller.ts` — 4 retired methods deleted; `RegistrationService` import + 3 retired zod schema imports removed.
- `apps/api/src/services/email.service.ts` — `sendVerificationEmail` + `generateVerificationUrl` + `getVerificationHtml` + `getVerificationText` deleted; `VerificationEmailData` import removed.
- `apps/api/src/queues/email.queue.ts` — `queueVerificationEmail` deleted; `VerificationEmailData` import removed.
- `apps/api/src/workers/email.worker.ts` — `case 'verification'` handler deleted from `processEmail` + from `getRecipientEmail` switch; `VerificationEmailData` import removed.
- `apps/api/src/middleware/registration-rate-limit.ts` — `verifyEmailRateLimit` + `resendVerificationRateLimit` deleted; `registrationRateLimit` + `activationRateLimit` retained.
- `apps/api/src/middleware/__tests__/rate-limit-coverage.test.ts` — 4 retired-route entries deleted; threshold-contract table updated.
- `apps/api/src/routes/dev.routes.ts` — `GET /dev/email-preview/verification` deleted.
- `apps/api/src/services/__tests__/email-templates.test.ts` — verification template tests deleted + verification URL test deleted.
- `apps/api/src/services/__tests__/__snapshots__/email-templates.test.ts.snap` — verification snapshot removed via `vitest --update`.
- `apps/api/src/queues/__tests__/email.queue.test.ts` — verification job structure test deleted; `VerificationEmailData` import removed.
- `apps/api/src/queues/__tests__/email-backpressure.test.ts` — `'verification'` references removed from priority classification + coverage + dedup tests; "exactly 3 critical / 4 standard" assertion updated to "2 / 4".

Backend deleted:
- `apps/api/src/services/registration.service.ts`
- `apps/api/src/services/__tests__/registration.service.test.ts`

Shared package modified:
- `packages/types/src/email.ts` — `VerificationEmailData` interface + `verificationEmailDataSchema` zod + `VerificationJob` interface deleted; `EmailJobType` union no longer includes `'verification'`; `EMAIL_TYPE_PRIORITY` map no longer keys `'verification'`; `EmailJob` discriminated union no longer includes `VerificationJob`.
- `packages/types/src/validation/registration.ts` — entire body deleted; file kept as empty module placeholder (because `packages/types/src/index.ts` re-exports from it). 4 zod schemas removed (`publicRegistrationRequestSchema`/`verifyEmailRequestSchema`/`resendVerificationRequestSchema`/`verifyOtpRequestSchema`) + `nigerianPhoneSchema` + `fullNameSchema` (verified via project-wide grep to be only referenced by the retired schemas).

Frontend created:
- `apps/web/src/features/auth/api/magic-link.api.ts` (MR-8 — peek + consume fetchers for the landing page).
- `apps/web/src/features/auth/pages/MagicLinkLandingPage.tsx` (MR-8 — `/auth/magic` route, peeks token, renders Confirm card per purpose, routes to /register or /register/complete-nin on Continue; renders deferred-notice for login purpose).
- `apps/web/src/features/auth/pages/__tests__/MagicLinkLandingPage.test.tsx` (MR-8 — 12 component tests across missing-params, success-confirm, redirect, peek errors, deferred-login).

Frontend modified:
- `apps/web/src/App.tsx` — `VerifyEmailPage` + `ResendVerificationPage` lazy imports + 2 routes deleted; `MagicLinkLandingPage` lazy import + `/auth/magic` route added.

Frontend deleted:
- `apps/web/src/features/auth/pages/VerifyEmailPage.tsx`
- `apps/web/src/features/auth/pages/ResendVerificationPage.tsx`
- `apps/web/src/features/auth/pages/__tests__/VerifyEmailPage.test.tsx`
- `apps/web/src/features/auth/pages/__tests__/ResendVerificationPage.test.tsx`

Story file modified:
- `_bmad-output/implementation-artifacts/9-12-public-wizard-pending-nin-magic-link.md` — Task 10.3 marked `[x]`; Review Follow-ups M8 + MR-8 flipped to FIXED; MR-5 reframed as capacity-planning note (not 9-12 debt); session-8 Change Log entry; session-8 Completion Notes; session-8 File List; new follow-up entry for `9-14-magic-link-login` (login-purpose JWT issuance — feature gap, NOT 9-12 debt).

---

**Session 7 (2026-05-11 late evening) — meta-review auto-fixes (MR-1 + MR-11 + MR-14):**

Backend created:
- `apps/api/src/middleware/wizard-draft-rate-limit.ts` (MR-11 — dedicated per-IP limiter at 120/IP/15min for the draft endpoints; replaces the wrong-shape magicLinkRateLimit choice).

Backend modified:
- `apps/api/src/services/sms-otp.service.ts` — `requestOtp` swapped from MULTI/EXEC to atomic `SET ... NX` claim BEFORE dispatch (MR-1); provider failure releases the claim via `DEL` so retry isn't blocked.
- `apps/api/src/routes/registration.routes.ts` — `PUT/GET /registration/draft` swapped from `magicLinkRateLimit` to `wizardDraftRateLimit` (MR-11). Header comment block updated.
- `apps/api/src/services/pending-nin.service.ts` — `EnumeratorPendingAggregate.pendingNinUnresolvedAt7d` renamed `pendingNinUnresolvedCount` (MR-14); SQL alias `pending_nin_unresolved_at_7d` renamed `pending_nin_unresolved_count` in lockstep + ORDER BY clause.

Tests modified:
- `apps/api/src/services/__tests__/sms-otp.service.test.ts` — mock simplified (MULTI/EXEC chain builder removed; only `get`/`set`/`del` now). Existing tests updated to match the NX claim path (rate-limit test now asserts `mockSet` returns null for the NX claim; issue test asserts NX claim + provider + OTP hash store; new test asserts `mockDel` fires on provider failure).

---

**Session 6 (2026-05-11 late evening) — Task 12.1 + 12.2 code review auto-fixes:**

Backend modified:
- `apps/api/src/services/magic-link.service.ts` — added `consumeTokenTx(tx, args)` transactional variant (H5 fix); existing `redeemToken` + `peekToken` + `consumeToken` API surface unchanged.
- `apps/api/src/controllers/magic-link.controller.ts` — `redeemMagicLink` (GET handler) now calls `peekToken` instead of `redeemToken` (C1 — RFC 7231 GET safety); new `consumeMagicLink` (POST handler) does the actual consume + audit-log via `MAGIC_LINK_REDEEMED`.
- `apps/api/src/routes/auth.routes.ts` — new `POST /auth/magic/consume` route wired behind `magicLinkRateLimit` (C1); retired `POST /auth/google/verify` 404 also wrapped in `magicLinkRateLimit` (L7).
- `apps/api/src/controllers/registration.controller.ts` — generic-400 anti-enumeration on `completeNin`/`deferReminder`/`saveDraft`/`submitWizard` Zod failures (L4); `completeNin` wraps consume + status UPDATE in `db.transaction` via `consumeTokenTx` (H5); `submitWizard` wraps insert + `logActionTx` + draft delete in `db.transaction` (M6); name parser fixed (H4); `NIN_DUPLICATE` 409 no longer leaks timestamp/source (M7); `getDraft` `?email=` branch deleted (H3); draft Zod schema flipped from `.passthrough()` to `.strict()` (H2).
- `apps/api/src/routes/registration.routes.ts` — `magicLinkRateLimit` on `PUT/GET /registration/draft` (H2); `registrationRateLimit` on `POST /registration/wizard` (H1).
- `apps/api/src/services/sms-otp.service.ts` — dispatch-first + MULTI/EXEC writes (M2/M3); `crypto.timingSafeEqual` verification (M4).
- `apps/api/src/workers/reminder.worker.ts` — `DispatchResult` interface returned from every dispatcher; `markMilestoneSent` records `last_dispatched` + `last_dispatch_reason` into JSONB metadata (M1); unused `EmailService` import + `void EmailService` tsc-suppressor removed (M5).
- `apps/api/src/services/pending-nin.service.ts` — `getEnumeratorPendingNinAggregates` gained `unresolvedThresholdDays` option (L2).

Tests modified:
- `apps/api/src/services/__tests__/sms-otp.service.test.ts` — mock gained `multi()` chain builder so MULTI/EXEC pipeline works under the test redis stub.
- `apps/api/src/middleware/__tests__/rate-limit-coverage.test.ts` — coverage map updated for `POST /magic/consume` (new entry) + `POST /google/verify` (now rate-limited).

Story file modified:
- `_bmad-output/implementation-artifacts/9-12-public-wizard-pending-nin-magic-link.md` — Task 12.1 + 12.2 marked `[x]`; Change Log entry for session 6; full Review Follow-ups (AI) table.

---

**Session 5 (2026-05-11 late evening) — Task 11.1 + 11.2:**

E2E test created:
- `apps/web/e2e/wizard-registration.spec.ts` (Task 11.1; 5 active smoke-level + 3 full-stack `test.skip()`; 8 tests total)

E2E config modified:
- `apps/web/playwright.config.ts` (Task 11.1; added `wizard` project with `testMatch: /wizard-registration\.spec\.ts/` and `Desktop Chrome` device)

---

**Session 4 (2026-05-11 evening) — Tasks 7 + 8 + 9 + 10 (code-only) + 13:**

Frontend created:
- `apps/web/src/features/registration/pages/CompleteNinPage.tsx` (Task 7.2)
- `apps/web/src/features/registration/pages/RegistrationCompletePage.tsx` (Task 7.3)
- `apps/web/src/features/registration/pages/__tests__/CompleteNinPage.test.tsx` (10 tests)
- `apps/web/src/features/registration/pages/__tests__/RegistrationCompletePage.test.tsx` (3 tests)
- `apps/web/src/features/forms/components/PendingNinPrompt.tsx` (Task 13)
- `apps/web/src/features/forms/components/__tests__/PendingNinPrompt.test.tsx` (7 tests)

Frontend modified:
- `apps/web/src/App.tsx` — mounted `/register/complete-nin` + `/register/complete` routes; stripped `GoogleOAuthWrapper` lazy import and route nesting (Task 10.1).
- `apps/web/src/features/registration/api/wizard.api.ts` — added `completeNin()` + `deferReminder()` fetchers + their response types (Task 7).
- `apps/web/src/features/auth/pages/LoginPage.tsx` — public-mode cutover banner + existing-user header (Task 8).
- `apps/web/src/features/auth/components/LoginForm.tsx` — removed Google sign-in block + `useGoogleAuth` import + `googleApiError` state; kept email + password + CAPTCHA path (Task 10.1).
- `apps/web/src/features/auth/components/activation-wizard/ActivationWizard.tsx` — `WizardProgressBar` mount replaced with `WizardStepIndicator` + `buildIndicatorSteps()` helper; `TrustBadgesRow` mounted at the foot of the card (Task 9). `ActivationWizardUI` got the same swap; `completedSteps` prop renamed `_completedSteps` (unused after the indicator swap).
- `apps/web/src/features/auth/components/activation-wizard/__tests__/ActivationWizard.test.tsx` — 4 new test cases for WizardStepIndicator presence / absence + TrustBadgesRow presence (Task 9.4).
- `apps/web/src/features/auth/pages/__tests__/LoginPage.test.tsx` — 2 new cutover-banner test cases; removed defunct `vi.mock` stubs for the deleted `@react-oauth/google` + `google-auth.api` modules (Task 8 + 10.4).
- `apps/web/src/features/auth/components/__tests__/LoginForm.test.tsx` — replaced "Google OAuth Integration" describe block with a 2-case "Google OAuth retired" regression guard; removed `vi.mock` stubs for the deleted modules (Task 10.4).
- `apps/web/src/features/forms/pages/FormFillerPage.tsx` — imported shared `NIN_QUESTION_NAMES` (removed local copy) + `NinHelpHint` + new `PendingNinPrompt`; new `pendingNinPromptOpen` state + `handlePendingNinConfirm` callback; rendered the help-hint + prompt under the question card on NIN questions (Task 13.1).
- `apps/web/src/features/forms/pages/ClerkDataEntryPage.tsx` — imported shared `NIN_QUESTION_NAMES` (removed local copy) + `NinHelpHint` + `PendingNinPrompt`; new `pendingNinPromptOpen` state + `pendingNin` derived flag + `handlePendingNinConfirm` + `handlePendingNinUndo`; submit-side strips the NIN field from `trigger(fieldNames)` when `pendingNin` is set; inline toggle area renders the help-hint + prompt OR the "Saved as pending-NIN" Undo banner (Task 13.2).
- `apps/web/src/features/registration/lib/nin-question-names.ts` — widened `NIN_QUESTION_NAMES` to `readonly string[]` so `.includes(question.name)` type-checks without per-call-site casts at the 4 new consumers (Task 13).

Frontend deleted (Task 8.4 + Task 10.1 + Task 10.4):
- `apps/web/src/features/auth/pages/RegistrationPage.tsx`
- `apps/web/src/features/auth/components/RegistrationForm.tsx`
- `apps/web/src/features/auth/pages/__tests__/RegistrationPage.test.tsx`
- `apps/web/src/features/auth/api/google-auth.api.ts`
- `apps/web/src/features/auth/hooks/useGoogleAuth.ts`
- `apps/web/src/features/auth/components/GoogleOAuthWrapper.tsx`

Backend modified:
- `apps/api/src/routes/auth.routes.ts` — `POST /google/verify` replaced with inline 404 handler (`GOOGLE_OAUTH_RETIRED`); `googleAuthRateLimit` middleware import removed (Task 10.1).
- `apps/api/src/controllers/auth.controller.ts` — removed `googleVerify` controller method + `GoogleAuthService` import + `googleAuthRequestSchema` import (Task 10.1).
- `apps/api/src/middleware/__tests__/rate-limit-coverage.test.ts` — `/google/verify` map entry rewritten with `rateLimiters: []` + retirement rationale (Task 10.4); orphan-detector + handler-count + rationale assertions all stay green.

Backend deleted (Task 10.1 + 10.4):
- `apps/api/src/services/google-auth.service.ts`
- `apps/api/src/middleware/google-auth-rate-limit.ts`
- `apps/api/src/services/__tests__/google-auth.service.test.ts`
- `apps/api/src/__tests__/security.google-auth-rate-limit.test.ts`

---

**Session 3 (2026-05-11) — frontend + backend ride-along additions:**

Backend created/modified:
- `apps/api/src/services/native-form.service.ts` — added `getPublicActiveForm()` + `getSetting` import (Task 5.4.2).
- `apps/api/src/controllers/form.controller.ts` — added `FormController.getPublicActiveForm` static handler.
- `apps/api/src/routes/form.routes.ts` — mounted unauthenticated `GET /public-active` before the authenticate middleware.
- `apps/api/src/routes/lga.routes.ts` — mounted unauthenticated `GET /public` (Step 2 LGA dropdown) before the authenticate middleware.
- `apps/api/src/controllers/registration.controller.ts` — added `saveDraft` / `getDraft` / `submitWizard` controllers + Zod schemas; reuses `MagicLinkService.peekToken` for cross-device draft resume.
- `apps/api/src/routes/registration.routes.ts` — wired `PUT /draft` + `GET /draft` + `POST /wizard`.
- `apps/api/scripts/migrate-system-settings-init.ts` — second idempotent INSERT seeds `wizard.public_form_id` row with JSONB `null` default.
- `apps/api/src/services/__tests__/native-form.service.test.ts` — 5 new tests for `getPublicActiveForm` (null setting / undefined setting / form-not-found / draft-not-published / success).

Frontend created:
- `apps/web/src/layouts/WizardLayout.tsx` (Task 4.1)
- `apps/web/src/features/registration/components/WizardStepIndicator.tsx` (Task 4.2)
- `apps/web/src/features/registration/components/WizardNavigation.tsx` (back/continue button row reused by every step)
- `apps/web/src/features/registration/components/NinHelpHint.tsx` (Task 6.1 — inline / tooltip / banner variants)
- `apps/web/src/features/registration/components/PendingNinToggle.tsx` (Task 6.2)
- `apps/web/src/features/registration/components/EmailTypoDetection.tsx` (Task 6.3)
- `apps/web/src/features/registration/components/TrustBadgesRow.tsx` (Task 6.4)
- `apps/web/src/features/registration/lib/email-typo-dictionary.ts` (local mirror of API typo dictionary)
- `apps/web/src/features/registration/lib/nin-question-names.ts` (single-source `NIN_QUESTION_NAMES` tuple)
- `apps/web/src/features/registration/hooks/useWizardDraft.ts` (Task 4.4 — debounced server-side autosave + token-resume hydration)
- `apps/web/src/features/registration/api/wizard.api.ts` (TanStack-Query-compatible fetchers + types)
- `apps/web/src/features/registration/pages/WizardPage.tsx` (5-step orchestrator + URL sync + final submit)
- `apps/web/src/features/registration/pages/Step1BasicInfo.tsx`
- `apps/web/src/features/registration/pages/Step2ContactLga.tsx`
- `apps/web/src/features/registration/pages/Step3Consent.tsx`
- `apps/web/src/features/registration/pages/Step4Questionnaire.tsx`
- `apps/web/src/features/registration/pages/Step5NinAndAuth.tsx` (state-aware dispatcher)
- `apps/web/src/features/registration/pages/Step5NinCaptured.tsx` (State A sub-view + shared `AuthChoiceFieldset`)
- `apps/web/src/features/registration/pages/Step5PendingNin.tsx` (State B sub-view)
- `apps/web/src/features/registration/pages/Step5NinInput.tsx` (State C sub-view)
- `apps/web/src/features/forms/components/FormRenderer.tsx` (Task 5.4.3 reusable decomposition)
- 7 test files: `NinHelpHint.test.tsx` / `PendingNinToggle.test.tsx` / `EmailTypoDetection.test.tsx` / `TrustBadgesRow.test.tsx` / `WizardStepIndicator.test.tsx` (`features/registration/components/__tests__/`); `Step5NinAndAuth.test.tsx` / `Step1BasicInfo.test.tsx` (`features/registration/pages/__tests__/`).

Frontend modified:
- `apps/web/src/App.tsx` — route swap at `/register`: was AuthLayout-wrapped `<RegistrationPage>`, now standalone `<WizardPage>` (owns its own WizardLayout chrome). Old `RegistrationPage` `lazy()` import removed (component source intentionally left on disk for Task 8 cleanup).

---

**Backend (created):**
- `apps/api/src/db/schema/magic-link-tokens.ts`
- `apps/api/src/db/schema/wizard-drafts.ts`
- `apps/api/src/services/magic-link.service.ts`
- `apps/api/src/services/auth/sms-provider.adapter.ts` (or flat `sms-provider.adapter.ts` if `auth/` subdir not preferred)
- `apps/api/src/services/auth/sms-otp.service.ts` (or flat `sms-otp.service.ts`)
- `apps/api/src/workers/reminder.worker.ts`
- Email templates per Task 1.8 decision (location varies based on existing pattern)
- `apps/api/drizzle/<NNNN>_*.sql` — migration with magic_link_tokens + wizard_drafts tables
- Tests: `*.test.ts` per service/route/worker

**Backend (modified):**
- `apps/api/src/db/schema/index.ts` — re-export new schemas
- `apps/api/src/services/audit.service.ts` — extend `AUDIT_ACTIONS` const with 4 new actions
- `apps/api/src/services/submission-processing.service.ts` — wire wizard Step 5 submit (NIN-conditional branching landed in Story 11-1; this story consumes)
- `apps/api/src/routes/auth.routes.ts` — add new endpoints (magic-link, sms-otp, complete-nin, defer-reminder); retire Google OAuth handler
- `apps/api/src/services/email.service.ts` — add new templates (or refactor to subdirectory per Task 1.8)
- `apps/api/src/workers/index.ts` — register `reminder.worker.ts` BullMQ job
- `apps/api/src/middleware/magic-link-rate-limit.ts` (clone of login-rate-limit.ts pattern; or extend existing rate-limit middleware)
- **`apps/api/src/routes/form.routes.ts`** — Task 5.4: add new `GET /forms/public-active` endpoint resolving the Super-Admin-pinned public-facing form (Option B) OR the source-type-filtered form (Option A). Mounts behind `FormController.getPublicActiveForm`.
- **`apps/api/src/controllers/form.controller.ts`** — Task 5.4: new `getPublicActiveForm` static method.
- **`apps/api/src/services/native-form.service.ts`** — Task 5.4: new `getPublicActiveForm()` reading the `wizard.public_form_id` setting (Option B) OR querying with source-type filter (Option A); returns flattened render schema or `404 PUBLIC_FORM_NOT_CONFIGURED`.
- **`apps/api/drizzle/<NNNN>_<name>.sql`** — ONLY if Awwal picks Option A: migration adds `respondent_source_types TEXT[]` to `questionnaire_forms`. Default `['enumerator']`. Multiple in-flight stories may collide on the migration slot; coordinate at impl time.

**Frontend (refactor — Task 5.4.3):**
- `apps/web/src/features/forms/pages/FormFillerPage.tsx` — DECOMPOSE: extract the inner state + skipLogic + renderer into a reusable `<FormRenderer>` component at `apps/web/src/features/forms/components/FormRenderer.tsx`. FormFillerPage becomes a thin route-mount wrapper around `<FormRenderer>`. The wizard's Step 4 mounts `<FormRenderer>` directly without the page chrome.

**Frontend (created):**
- `apps/web/src/features/forms/components/FormRenderer.tsx` — Task 5.4.3 decomposition target. Reusable component extracted from `FormFillerPage.tsx`. Props: `formSchema` + `initialResponses` + `onAnswer` + `onComplete` + `onPendingNinClick` (NEW callback per Step 5 dispatcher Dev Notes — fires when user clicks "I don't have my NIN now" link inline on the NIN question). No URL params; no router dependency. Wizard Step 4 mounts this directly.
- `apps/web/src/features/registration/pages/Step5NinCaptured.tsx` — State A sub-view (NIN captured via questionnaire). "✓ NIN captured: 1234***8901" confirmation + auth choices.
- `apps/web/src/features/registration/pages/Step5PendingNin.tsx` — State B sub-view (pending NIN via inline toggle). Info-50 consequence-preview + auth choices + "Save as Pending" submit button.
- `apps/web/src/features/registration/pages/Step5NinInput.tsx` — State C sub-view (form has no NIN question). Original AC#1 Step 5 + AC#4 spec verbatim.
- `apps/web/src/layouts/WizardLayout.tsx`
- `apps/web/src/features/registration/components/WizardStepIndicator.tsx`
- `apps/web/src/features/registration/components/NinHelpHint.tsx`
- `apps/web/src/features/registration/components/PendingNinToggle.tsx`
- `apps/web/src/features/registration/components/EmailTypoDetection.tsx`
- `apps/web/src/features/registration/components/TrustBadgesRow.tsx`
- `apps/web/src/features/registration/pages/Step1BasicInfo.tsx`
- `apps/web/src/features/registration/pages/Step2ContactLga.tsx`
- `apps/web/src/features/registration/pages/Step3Consent.tsx`
- `apps/web/src/features/registration/pages/Step4Questionnaire.tsx`
- `apps/web/src/features/registration/pages/Step5NinAndAuth.tsx`
- `apps/web/src/features/registration/pages/CompleteNinPage.tsx`
- `apps/web/src/features/registration/pages/RegistrationCompletePage.tsx`
- `apps/web/src/features/registration/api/wizard.api.ts` (TanStack Query hooks)
- Tests: component tests + integration tests + E2E

**Frontend (modified):**
- `apps/web/src/features/auth/pages/LoginPage.tsx` — cutover messaging (NOT `PublicLoginPage.tsx` — that file does not exist)
- `apps/web/src/features/auth/pages/RegistrationPage.tsx` — retire/redirect (NOT `PublicRegisterPage.tsx`)
- `apps/web/src/features/auth/components/activation-wizard/ActivationWizard.tsx` — apply `WizardStepIndicator` (Task 9 polish)
- `apps/web/src/features/auth/components/activation-wizard/steps/*.tsx` — apply `TrustBadgesRow` (Task 9 polish)
- TanStack Router config — register new routes `/register?step=N`, `/register/complete-nin`, `/register/complete`, `/auth/magic`
- **`apps/web/src/features/forms/pages/FormFillerPage.tsx`** — Task 13.1: NinHelpHint inline variant on NIN question with "I don't have my NIN now" link → optional `_deferReasonNin` input + Confirm → `_pendingNin: true` in submission rawData. Reuses existing `NIN_QUESTION_NAMES` constant (line 21).
- **`apps/web/src/features/forms/pages/ClerkDataEntryPage.tsx`** — Task 13.2: same toggle pattern, clerk-specific UI placement.

**Backend modified — Universal Pending-NIN (added 2026-05-10):**
- `apps/api/src/services/submission-processing.service.ts` — Task 3.1: REMOVE NIN-required throw at line 359; route to `findOrCreateRespondent` with `nin: undefined` when missing (regardless of source). Task 3.5: race-resolution merge — fuzzy-match-then-promote pending rows when NIN is provided later.
- `apps/api/src/services/pending-nin.service.ts` — **NEW** (Task 3.6): `resolveReminderDestination(respondent)` pure function with 5-branch precedence per source.
- `apps/api/src/workers/reminder.worker.ts` — Task 1.9 (deferred to next session) + Task 3.6: source-aware reminder dispatch using `resolveReminderDestination`.
- `apps/api/src/services/audit.service.ts` — Task 3.8: append `PENDING_NIN_CREATED` + `PENDING_NIN_PROMOTED` to `AUDIT_ACTIONS` const (40 total).
- `apps/api/src/services/__tests__/audit.service.test.ts` — Task 3.8: bump sentinel `.toHaveLength(38) → .toHaveLength(40)` with heritage-comment line. Same trip-wire that broke commit `adb956b` → preempt this time.
- `apps/api/src/services/__tests__/submission-processing.service.test.ts` (or equivalent) — Task 3.9: race-resolution-merge tests + concurrent-merge-attempts + reminder-destination per-source tests.
- (NEW supervisor-dashboard widget surface) — Task 3.7: anti-abuse data surface (per-enumerator 7-day pending rate aggregate query). UI affordance for the soft warning deferred to post-field-survey.

**Other:**
- `_bmad-output/implementation-artifacts/sprint-status.yaml` — status flips
- `_bmad-output/planning-artifacts/epics.md` — FRC item #3 status flip on completion (Task 11.4)
- MEMORY.md (optional) — note new wizard pattern + magic-link convention if useful for future stories

**Out of scope (explicitly NOT modified):**
- SMS OTP toggle UI surface — lives in `prep-settings-landing` Settings Landing Page; this story is the BACKEND consumer of the flag, not the UI owner
- General settings infrastructure (`apps/api/src/lib/settings.ts`, `system_settings` table) — provided by `prep-settings-landing` HARD dependency
- Existing FraudThresholds page consolidation under Settings — out of scope per `prep-settings-landing` v1 Out-of-Scope clause
- URL convention cleanup (`/settings/` vs `/security/` vs no-segment) — future "prep-url-convention-cleanup" story

### Change Log

| Date | Change | Rationale |
|---|---|---|
| 2026-05-11 (session 8, late evening) | **"No technical debt" close-out — every deferred item resolved before commit per Awwal directive.** Three phases: (1) **Task 10.3 — Legacy hybrid Magic-Link/OTP retirement (end-to-end)**: routes deleted (`POST /auth/public/register`, `GET /verify-email/:token`, `POST /verify-otp`, `POST /resend-verification`); controller methods deleted (`publicRegister`/`verifyEmail`/`verifyOtp`/`resendVerification`); `RegistrationService` deleted (entire file + test); `EmailService.sendVerificationEmail/generateVerificationUrl/getVerificationHtml/getVerificationText` deleted; `queueVerificationEmail` + `EmailJob` union member `'verification'` + `VerificationJob` type + worker handler deleted; `VerificationEmailData` + `verificationEmailDataSchema` + 4 zod schemas in `packages/types/src/validation/registration.ts` deleted; `verifyEmailRateLimit` + `resendVerificationRateLimit` middlewares deleted; frontend `VerifyEmailPage` + `ResendVerificationPage` + their tests + 2 App.tsx routes deleted; `email-templates.test.ts` verification block + snapshot + `email.queue.test.ts` verification job + `email-backpressure.test.ts` critical-type counts updated; `rate-limit-coverage.test.ts` 4 entries deleted; `/email-preview/verification` dev route deleted. **Staff invitation flow (enumerator + supervisor) is a SEPARATE code path and entirely untouched** (verified: `StaffInvitationEmailData` / `queueStaffInvitationEmail` / `EmailService.sendStaffInvitationEmail` / `case 'staff-invitation'` worker handler all live; `/activate/:token` + `validateActivationToken` + `activate` controller + service methods all live). (2) **MR-8 — Magic-link frontend landing page**: new `apps/web/src/features/auth/pages/MagicLinkLandingPage.tsx` + new `/auth/magic` frontend route + new `apps/web/src/features/auth/api/magic-link.api.ts` (peek + consume fetchers). On mount, peeks token to get email; renders per-purpose Confirm card; on Confirm click routes to `/register?token=…` (`wizard_resume`) or `/register/complete-nin?token=…` (`pending_nin_complete`). For `login` purpose, renders "magic-link sign-in coming soon" notice with link to /login — the JWT-issuance branch is NOT 9-12 scope (feature gap, separate story). 12 new component tests. (3) **M8 — Route-level supertests**: 45 new tests across `apps/api/src/routes/__tests__/registration.routes.test.ts` (25 — Zod validation, generic 400 anti-enumeration, NIN_DUPLICATE no-leakage, transactional happy-paths, race-resolution branch, partial-unique-violation → 409 translation), `magic-link.routes.test.ts` (12 — anti-enumeration sent:false, plaintext-never-in-response, GET PEEK-ONLY C1 contract verified, POST consume + audit), `sms-otp.routes.test.ts` (8 — flag-off 503, format validation, throttle 429, success). All pass. **MR-5** reframed as capacity-planning note (Story 6-1 audit chain contract, not 9-12 debt). **Quality gates**: API tsc + lint + vitest **2,138 pass / 5 pre-existing DB-constraint failures / 7 skipped**; web tsc + vitest **2,454 pass / 2 todo / 0 failures**. **Story now has ZERO technical debt deferrals** — every Review Follow-up is either FIXED, REFRAMED-as-not-debt, or filed as a separate follow-up story (only `9-14-magic-link-login` for the JWT-issuance feature). | Awwal challenged "no technical debt" as the discipline floor and the session honoured it without compromising scope honesty. The legacy retirement was the largest cleanup pass — touched ~24 files across both apps + the shared types package. The staff-invitation safety check (separate code path, completely untouched, verified by tracing every type/queue/worker/template/service surface) was the most important verification — the Awwal precaution turned out unnecessary but the cross-check was the right move. The `9-14-magic-link-login` follow-up is the only "left undone" item, and it's properly framed as a feature gap (deferred from Task 1.7 by design) rather than as 9-12 debt — the difference matters for honest tracking. |
| 2026-05-11 (session 7, late evening) | **Meta-review of session-6 auto-fixes — 15 MR-* findings catalogued, 3 auto-fixed.** Diff-review pass on the session-6 fixes themselves catches 2 NEW HIGH regressions introduced by the fixes (a high-severity rate-limit miscalibration that would have broken the wizard in production; and a concurrent-dispatch race in SmsOtp). (a) **MR-11** — session-6's choice of `magicLinkRateLimit` (3/email/hour, per-email keyed) on `PUT/GET /registration/draft` would lock users out within seconds of starting Step 2 (useWizardDraft does 2-second-debounced auto-save → 20-60 saves per session). And per-email keying gives an attacker who floods with random emails a fresh bucket per email, defeating the H2 threat-model. **Fix**: new dedicated `apps/api/src/middleware/wizard-draft-rate-limit.ts` at 120/IP/15min, per-IP keyed. Generous for legitimate auto-save (~8/min average); bounds single-IP flooding. (b) **MR-1** — session-6's M2 reorder of SmsOtp `requestOtp` (dispatch-then-write) introduced a concurrent-call race window: two simultaneous requests for the same phone both see `lastSent = null`, both dispatch SMS, user receives 2 codes. **Fix**: atomic `SET key val EX ttl NX` claim BEFORE dispatch; provider failure DELs the claim so the user can retry. Closes the race AND preserves M2's "no state on provider failure" goal. Tests updated (MULTI/EXEC mock chain removed; 1 new test asserts the DEL-on-provider-failure path). (c) **MR-14** — renamed `EnumeratorPendingAggregate.pendingNinUnresolvedAt7d` → `pendingNinUnresolvedCount` (threshold-agnostic). Safe since no consumers exist yet (supervisor-dashboard widget UI deferred per D6). Deferred: **MR-5** (audit-chain serialization extends to wizard registrations — pre-existing Story 6-1 contract; flag for capacity planning), **MR-8** (magic-link click → JSON in browser — Task 1.7 frontend wiring), **MR-2/MR-6/MR-9/MR-10/MR-12/MR-13/MR-15** (verified correct / by-design / acceptable trade-off). **Quality post-fix**: API tsc clean, lint clean, vitest **2,122 pass / 5 pre-existing DB-constraint failures / 7 skipped** (+1 net test, zero new regressions). | Adversarial-review-of-the-review caught two production-breaking choices made in the haste of the session-6 fix sweep. MR-11 in particular would have shipped a wizard that locked users out at Step 2 — a far worse outcome than the original H2 finding. The pattern (review-of-review) is now-twice-validated as a Wave 0/1 muscle-memory practice (`feedback_review_patterns_deploy_scripts.md` Pattern #4). The MR-1 fix is also a slight improvement on the session-6 plan: atomic NX is simpler than MULTI/EXEC, makes the rate-limit-vs-dispatch ordering explicit, and uses the DEL-on-failure pattern that ioredis natively supports. |
| 2026-05-11 (session 6, late evening) | **Task 12.1 + 12.2 — adversarial code review pass complete.** `/bmad:bmm:workflows:code-review` run on the uncommitted working tree spanning all session-1-5 deliverables. **21 findings catalogued** (1 CRITICAL + 5 HIGH + 8 MEDIUM + 7 LOW) — full table in `### Review Follow-ups (AI)`. **16 auto-fixed inline**: (a) **C1** — `GET /auth/magic` is now PEEK-ONLY (RFC 7231 §4.2.1 safety restored; email-scanner prefetchers no longer burn tokens). New `POST /api/v1/auth/magic/consume` (rate-limited, audit-logged) is the explicit consume path. (b) **H1** — `POST /registration/wizard` now carries `registrationRateLimit` (5/IP/15min). Captcha integration noted as follow-up. (c) **H2** — `PUT/GET /registration/draft` carry `magicLinkRateLimit`; Zod schema flipped from `.passthrough()` to `.strict()` with `formHasNinQuestion`/`questionnaireFormId`/`questionnaireFormVersionId` enumerated. (d) **H3** — `GET /registration/draft?email=` branch deleted; only `?token=<wizard_resume>` hydration remains. (e) **H4** — `submitWizard` name parser no longer duplicates single-token names into both firstName and lastName (now firstName=name, lastName=NULL). (f) **H5** — new `MagicLinkService.consumeTokenTx` lets `completeNin` wrap consume + respondent-status promotion in one `db.transaction`; UPDATE failure after consume now rolls both back. (g) **M1** — reminder worker dispatch returns `{dispatched, reason}`; metadata.reminder_state.last_dispatched + .last_dispatch_reason record whether the dispatch actually delivered. (h) **M2/M3/M4** — `SmsOtpService.requestOtp` dispatches via provider FIRST, then writes Redis state via `MULTI/EXEC`; `verifyOtp` uses `crypto.timingSafeEqual`. (i) **M5** — unused `EmailService` import in `reminder.worker.ts` deleted. (j) **M6** — `submitWizard` insert + `logActionTx` + draft delete now in one transaction. (k) **M7** — `NIN_DUPLICATE` 409 no longer leaks `firstRegisteredAt`/`firstRegisteredVia` (anti-social-engineering). (l) **L2** — `getEnumeratorPendingNinAggregates` gained `unresolvedThresholdDays` option. (m) **L4** — anti-enumeration discipline applied across all wizard endpoints (generic 400 INVALID_INPUT, no Zod field structure). (n) **L7** — retired `POST /google/verify` 404 wrapped in `magicLinkRateLimit`. **5 deferred** with rationale: **M8** (route-level supertests — story-deferred to Playwright bundle per Task 11.1), **L1** (timing oracle, acceptable for short-lived tokens), **L3** (peek/consume alias kept for call-site readability), **L5** (PublicOnlyRoute omission on `/register/complete-nin` and `/register/complete` is intentional — magic-link landings must work regardless of auth state), **L6** (BRAND_COLOR duplication retained; widening EmailService API for one constant is unjustified). **Quality post-fix**: API tsc + lint + vitest 2,121 pass / 5 pre-existing DB-constraint failures / 7 skipped (zero new regressions); web tsc clean. **Story stays `in-progress`** per `feedback_review_before_commit.md` — Task 12.3 (commit + sprint-status flip to `review`) requires operator sign-off on the auto-fix diff. | Code review was the last gate before review-ready. Awwal's directive at session open: "Create action items (critical to low) and fix them all automatically" — interpreted as: log all 21 findings as `[AI-Review]` action items in Review Follow-ups, then auto-fix everything where the fix is bounded enough that one diff suffices. The 5 deferred items have documented rationale that survives future sessions. The biggest architectural shift was C1 — the magic-link GET path was prefetcher-fragile, and the peek-only refactor restores the standard email-magic-link safety pattern (peek-on-GET, consume-on-POST) used by industry-standard implementations like Auth0 / Stytch / Slack. M8 stays open as the single remaining MEDIUM finding because adding supertest fixtures is genuinely out of scope for an in-line review pass — Story Task 11.1 already routes that work to the Playwright bundle. |
| 2026-05-11 (session 5, late evening) | **Task 11.1 + 11.2 shipped.** Playwright E2E spec `apps/web/e2e/wizard-registration.spec.ts` covers AC#13 end-to-end coverage in the project-precedent pattern (active smoke-level + skipped full-stack with detailed re-enable preconditions, mirroring `nin-validation.spec.ts`). 5 active tests assert client-rendered surfaces that don't need API/DB: `/login` (public) cutover banner + existing-user header / `/staff/login` cutover absence / `/register` Step-1 mount with full 5-step indicator + auto-focus / `/register/complete-nin` token-missing banner / `/register/complete` civic-framing card with Sign-in + Marketplace CTAs (both `?source=pending_nin` + default branches). 3 skipped tests document the AC#13 end-to-end flows with full re-enable preconditions in JSDoc: (a) wizard happy-path with NIN (State C — needs `wizard.public_form_id` pinned to a NIN-less form), (b) wizard pending-NIN flow + magic-link `complete-nin` resume (needs email-sink test fixture because magic-link tokens are SHA-256 hashed at rest — can't be replayed from DB), (c) existing `public@dev.local` login post-migration (needs seeded public account). New `wizard` project added to `playwright.config.ts` (Desktop Chrome, regex match on the new spec). Spec validated via `playwright test --list --project=wizard` (8 tests listed) + `tsc --noEmit` clean + `eslint` clean on the spec file. NO source-code changes outside the spec + config. Vitest baselines unchanged. **Deferred per session-open scope-pick**: Task 11.3 sprint-status flip (gated on Task 12 code review running in a different LLM session per `feedback_review_before_commit.md`); Task 11.4 FRC item #3 flip in `epics.md` (gated on production deploy + first respondent completion — operator action); Task 12 adversarial code review (project pattern: different LLM session); Task 10.2 operator-gated Google Cloud Console credential revoke; Task 10.3 hybrid magic-link/OTP email template removal (still in use by legacy `/auth/public/register` + `verify-email` + `verify-otp` endpoints; scope-disproportionate this session). Story stays `in-progress`. | This session is intentionally narrow — Task 11.1 only — because the project pattern requires the adversarial code review (Task 12) to run in a separate LLM session before `review` flip per `feedback_review_before_commit.md`. The Playwright spec is the last code-side deliverable left on Story 9-12. The `test.skip()` discipline matches the existing `nin-validation.spec.ts` precedent: rather than building elaborate test fixtures inside this session (NIN-less form seed + system_settings pin + email-sink + public account seed), the spec captures the intended assertion shape so a future session with full-stack-ready CI can flip skip→active in one diff per test. The smoke-level active tests give real ongoing coverage of the cutover-messaging contract (AC#11) + the two standalone post-magic-link pages (AC#9) + the wizard mount/auto-focus contract (AC#1) without needing any backend infrastructure. |
| 2026-05-11 (session 4, evening) | **Tasks 7 + 8 + 9 + 10 (code-only) + 13 all shipped.** (a) **Task 7** — new `CompleteNinPage` (NinHelpHint banner + 11-digit numeric NIN input + Save + remind-me-later) + new `RegistrationCompletePage` (civic-framing card with Sign-in + Marketplace CTAs); both mounted as standalone routes outside `AuthLayout`. `wizard.api.ts` gained `completeNin()` + `deferReminder()` fetchers. FR21 duplicate handling renders the first-registered-at + go-back / contact-support block. 13 new tests. (b) **Task 8** — `LoginPage` cutover banner (Info-50 "New here?" + primary CTA to `/register`) + "Already registered? Sign in below." existing-user header — public-mode only; staff mode untouched. Legacy `RegistrationPage.tsx` + `RegistrationForm.tsx` + `RegistrationPage.test.tsx` deleted outright (no callers post session-3 route swap). 2 new LoginPage tests. (c) **Task 9** — `ActivationWizard` + `ActivationWizardUI` retro-fitted with the shared `WizardStepIndicator` via a small `buildIndicatorSteps()` helper (numeric `WizardStep[]` + `STEP_LABELS` → `{id, label}[]`); `TrustBadgesRow` mounted at the foot of the card on BOTH variants. 4 new tests. Backward-compat `completedSteps` prop retained (renamed `_completedSteps`). (d) **Task 10 (partial)** — Google OAuth retired. Backend `POST /auth/google/verify` returns inline 404 `GOOGLE_OAUTH_RETIRED`; `googleVerify` controller + `GoogleAuthService` + `google-auth-rate-limit` middleware + their tests deleted. Frontend `LoginForm` Google button + divider + `useGoogleAuth` hook + `GoogleOAuthWrapper` mount in `App.tsx` stripped; orphan frontend files (`google-auth.api.ts`, `useGoogleAuth.ts`, `GoogleOAuthWrapper.tsx`) deleted. `LoginForm.test.tsx` "Google OAuth Integration" describe block trimmed to a 2-case regression guard (no button + no divider). `rate-limit-coverage.test.ts` `/google/verify` entry rewritten as `rateLimiters: []` + retirement rationale. **Task 10.2 (revoke Google Cloud Console credentials)** is operator-only and remains open. **Task 10.3 (Hybrid Magic-Link/OTP email template removal)** explicitly DEFERRED — the template is still in use by the legacy `POST /auth/public/register` + `verify-email` + `verify-otp` endpoints, retiring those endpoints + the template is a scope-disproportionate change for this session and tracked separately. (e) **Task 13** — new shared `PendingNinPrompt` component (optional reason textarea + Save / Never-mind). `FormFillerPage.tsx` + `ClerkDataEntryPage.tsx` both imported the shared `NIN_QUESTION_NAMES` constant (single source of truth restored — the constant type widened to `readonly string[]` so `.includes` accepts `string` from `question.name` at all 4 call sites). On a NIN question, `NinHelpHint` inline opens the prompt; confirming stamps `_pendingNin: true` + `_deferReasonNin` into rawData and clears the NIN value. `ClerkDataEntryPage` additionally renders an Info-50 "Saved as pending-NIN" banner + Undo affordance when the flag is set; submit-side strips the NIN question from `trigger(fieldNames)` so the row submits without NIN validation when pending. 7 new PendingNinPrompt component tests. Full-page integration coverage for FormFillerPage/ClerkDataEntryPage pending flows deferred with Task 11.1 Playwright suite. **Quality**: api tsc + lint clean; web tsc + lint clean (2 surfaces hit during typecheck — `Loader2` re-imported after the OAuth-block excision in LoginForm; `NIN_QUESTION_NAMES` widened to allow `.includes(string)`). api vitest: **2121 pass / 5 pre-existing DB-constraint failures / 7 skipped** (same baseline as session 2 — confirmed not caused by this session). web vitest: **2467 pass / 2 todo / 0 failures** (+7 net over session-3 baseline of 2460). NO new dependencies; NO new audit-action keys (count stays at 40); NO new DB migrations. **Deferred per session-open scope-pick**: Task 10.2 operator action, Task 11 Playwright E2E + sprint-status flip + FRC #3 flip, Task 12 adversarial code review (project pattern: different LLM session). Story stays `in-progress`; sprint-status.yaml NOT flipped. | Largest cleanup pass in the story to date — closed every code-only outstanding task from the session-3 deferral list. Bounded by Awwal's explicit scope-pick at session open ("All code tasks (7–10 + 13), defer 11/12"), which kept the session shape disciplined: Task 10.3 (hybrid template) and Task 11.4 (FRC flip) recognised as out-of-band and tracked separately, not absorbed into this session. Task 9's `buildIndicatorSteps` helper sits at the right level — converts the existing numeric `WizardStep` API into the new `{id, label}[]` shape WITHOUT touching the wizard state machine or step components (true visual polish, zero logic change). Task 13's `PendingNinPrompt` reuse pattern (inline link → prompt → confirm → stamp rawData) propagates the wizard's pattern to the enumerator + clerk surfaces with minimum surface-area expansion. Task 10's Google OAuth retirement was the most file-touch-heavy but cleanest because the system was self-contained (one route + one controller method + one service + one middleware + their tests + 3 frontend files). |
| 2026-05-11 (session 3) | **Frontend wizard render path COMPLETE — Tasks 4 + 5 + 6 all shipped.** (a) **Task 4** — `WizardLayout` (sticky step indicator + content card + sticky `TrustBadgesRow` footer + autosave-status slot), `WizardStepIndicator` (desktop breadcrumb + mobile collapse, `aria-current="step"`, `<nav aria-label>` landmark), `useWizardDraft` hook (2s-debounced server-side autosave + magic-link `?token=` cross-device hydration; IndexedDB local cache deferred to follow-up per Dev Notes — server is canonical for AC#9). URL sync via `useSearchParams` (`?step=N`). Task 4.6 schema-introspection wired through `Step4Questionnaire` mount-side effect that stamps `formHasNinQuestion` + form-version IDs into `wizard_drafts.formData`. (b) **Task 5** — 5 step pages (`Step1BasicInfo` / `Step2ContactLga` / `Step3Consent` / `Step4Questionnaire` / `Step5NinAndAuth` dispatcher). Step 5 dispatcher's `deriveStep5State` is a pure function returning A/B/C from the form-data shape; tests cover all 8 truth-table cells. State A/B/C sub-views (`Step5NinCaptured` / `Step5PendingNin` / `Step5NinInput`) are separate files; State A and B share an `AuthChoiceFieldset` component re-exported from State A. `WizardPage.tsx` orchestrates step state, URL routing, `useWizardDraft`, magic-link kickoff after success (login purpose for active respondents; the pending-NIN path's `pending_nin_complete` magic-link is issued server-side by `submitWizard`), completion screen. **Task 5.4.0 design pick: Option B** (system_settings row for form discovery) per Awwal. **Backend ride-along**: `wizard.public_form_id` seed (`migrate-system-settings-init.ts`) + `NativeFormService.getPublicActiveForm()` + unauthenticated `GET /forms/public-active` + unauthenticated `GET /lgas/public` + `PUT /registration/draft` + `GET /registration/draft` (with `?token=` peek + anti-enumeration empty response) + `POST /registration/wizard` (FR21 dedupe inline, status derivation, pending-NIN auto-magic-link). **Task 5.4.3** — `<FormRenderer>` decomposition shipped as the new reusable component; **FormFillerPage refactor deferred** to a follow-up that can validate behaviour parity against the existing 540-line test suite without behaviour drift. (c) **Task 6** — `NinHelpHint` (3 variants), `PendingNinToggle` (`<button role="switch">`), `EmailTypoDetection` (`role="status" aria-live="polite"`), `TrustBadgesRow` (3 `<aside role="note">` badges). All carry `data-testid` selectors + comprehensive aria. **Quality**: API tsc/lint clean; web tsc/lint clean; web full suite **2,460 pass / 2 todo** (+43 new tests this session, 0 regressions); API `native-form.service.test.ts` 19/19 (+5 new); magic-link / pending-nin / audit / submission-processing 130/130 pass. NO new dependencies; NO new audit-action keys (count stays at 40); NO new DB migrations (system_settings seed extension is a row insert, not a schema change). **Route swap**: `/register` now mounts `WizardPage` directly (outside `AuthLayout`); old `RegistrationPage` lazy import removed. **Deferred to next session per user direction at session open**: Task 7 return-to-complete view, Task 8 cutover messaging + retire legacy `RegistrationPage` + `RegistrationForm` source files, Task 9 staff-activation wizard polish (retro-fit `WizardStepIndicator` + `TrustBadgesRow`), Task 10 Google OAuth retirement + hybrid email template removal, Task 11 Playwright E2E + sprint-status flip + FRC #3 flip, Task 12 adversarial code review, Task 13 enumerator + clerk `FormFillerPage`/`ClerkDataEntryPage` pending-NIN toggles. | Largest single session in the story — the entire user-visible wizard render path now exists end-to-end. Backend ride-alongs needed for Step 4 questionnaire injection (Option B form discovery) + Step 2 LGA list + final submit + draft persistence; each is small (~30-80 lines per controller/service method) and tightly bounded. FormRenderer decomposition was the riskiest piece — chose NOT to refactor FormFillerPage to swap to it this session because the existing 540-line test suite represents real-world regression coverage that's too valuable to risk on a behaviour-equivalence claim under time pressure. Future task can swap FormFillerPage to the new component once visual/behaviour parity is empirically confirmed. No behaviour drift either way — FormFillerPage keeps its own copy of the same logic; FormRenderer is the wizard-side copy. State-aware Step 5 dispatcher (Dev Notes 2026-05-10) implemented as a pure function with first-class unit tests — robust to form-schema changes (Super Admin can publish a NIN-less form and the wizard auto-flips to State C without code changes). Universal pending-NIN flow already covered server-side by sessions 1-2; this session wires the public-wizard half (the enumerator/clerk surfaces in Task 13 are still deferred per Risk #11 — FRC #3 is satisfied independent of those). |
| 2026-04-25 | Story drafted by impostor-SM agent per SCP-2026-04-22 §A.5. Status `ready-for-dev`. 14 ACs covering 5-step wizard + magic-link primary + SMS OTP infra-only + pending-NIN status path + NinHelpHint + email-typo detection + trust badges + return-to-complete + reminder cadence + migration cutover + bundled staff-wizard polish + tests + FRC flip. Depends on Story 11-1 schema. | A.5 sequencing: 9-12 follows 11-1 in dependency order. FRC item #3 — field-survey-blocking. |
| 2026-05-10 | **Universal pending-NIN — Option 1 scope expansion ratified (no code change in this commit).** Awwal directive: extend pending-NIN from public-only to all submission sources (enumerator + clerk). Rationale: "do it once and for all, no technical debt" — schema (Story 11-1) already supports it, marginal cost is ~2 dev-days vs the data-quality risk of carrying public-only forward + a follow-up story later. Six design decisions locked in new Dev Notes subsection "Universal pending-NIN — Option 1 design (2026-05-10)": **(D1)** race-resolution merge in `findOrCreateRespondent` (fuzzy-match name+phone of pending rows when NIN provided later → atomic UPDATE pending → active; `PENDING_NIN_PROMOTED` audit event); **(D2)** `resolveReminderDestination` pure function with 5-branch precedence per source (public→email; enumerator→email\|sms-flag-on\|supervisor; clerk→email\|supervisor; imported_*→no reminder); **(D3)** pay-on-completion productivity metric (pending submissions don't count toward enumerator KPI until status flips to active — abuse-resistant); **(D4)** audit actions 38→40 (`PENDING_NIN_CREATED` + `PENDING_NIN_PROMOTED`); **(D5)** optional `_deferReasonNin` field on rawData (auto-set for public, free-text for enumerator/clerk; never required); **(D6)** anti-abuse data surface (per-enumerator 7-day pending-rate aggregate query — supervisor-dashboard widget; UI soft warning deferred post-field-survey based on real data). Story scope: 5-7 → 7-9 dev-days. Risks #11 added. Task 3 expanded with 5 new subtasks (3.5 race-merge / 3.6 destination resolver / 3.7 anti-abuse query / 3.8 audit-actions+sentinel / 3.9 tests). NEW Task 13 covers enumerator FormFillerPage + clerk ClerkDataEntryPage pending-NIN toggle wiring (`_pendingNin` + `_deferReasonNin` rawData fields). FRC #3 (public wizard) remains satisfied by the public-only baseline — universal-pending-NIN is additive, not blocking. AC text NOT modified per dev-story workflow's permitted-edit rule; canonical SM amendment can land in a future Bob session. | Field-survey window of <2 weeks accommodates the +2 days. The race-resolution merge is the only genuinely new infrastructure piece — everything else builds on Story 11-1 schema + prep-input-sanitisation E.164 normaliser + Story 6-1 audit chain. Pay-on-completion productivity metric is the most important policy decision in this set (incentive design); without it, universal-pending-NIN creates an enumerator over-deferral abuse vector. |
| 2026-05-10 | **Step 5 NIN handling — state-aware dispatcher design clarification added to Dev Notes (no code change).** During the Step-4-injection clarification round, Awwal flagged a deeper conflict: the active form (`oslsr_master_v3.xlsx`) has a NIN question. The existing renderer special-cases NIN (`apps/web/src/features/forms/pages/FormFillerPage.tsx:21,111` — `NIN_QUESTION_NAMES = ['nin', 'national_id']` + `useNinCheck` hook for real-time modulus-11 + duplicate-check). Story's AC#1 Step 5 + AC#4 implicitly assumed NIN was NOT in the questionnaire → if implemented as written, user types NIN twice (Step 4 form + Step 5 input) with conflicting state. New Dev Notes subsection "Step 5 NIN handling — state-aware dispatcher (2026-05-10)" reframes Step 5 as a 3-state dispatcher: **State A** (NIN captured via questionnaire — Step 5 shows confirmation + auth choices, no NIN input), **State B** (pending NIN via inline toggle in Step 4 — Step 5 shows consequence-preview + auth choices + "Save as Pending" button), **State C** (form has no NIN question — falls back to original AC#1+AC#4 spec verbatim). Wizard introspects form schema at Step 4 mount via `formData.formHasNinQuestion` boolean — robust to form changes (Super Admin can publish a NIN-less form, wizard auto-flips to State C without code changes). Task 5.5 expanded to 4-subtask checklist with 3 sub-views (`Step5NinCaptured.tsx` / `Step5PendingNin.tsx` / `Step5NinInput.tsx`). Task 4 gains 4.6 (form-schema introspection); Task 6.1 amended for NinHelpHint inline `onPendingNinClick` wiring. New Risks entry #10. WizardDraftData TypeScript shape extended (`formHasNinQuestion`, `questionnaireFormId`, `questionnaireFormVersionId` — JSONB-backed, no migration). AC#4 toggle location is now state-dependent (inline in Step 4 for States A/B, in Step 5 for State C); behaviour unchanged. **AC text NOT modified** per dev-story workflow's permitted-edit rule; Dev Notes is the spec-amendment-of-record until next SM canonicalisation pass. | Second clarification surfaced by Awwal's question "the questionnaire already has a NIN Question, how do we use the pending nin approach in this regards." Same impostor-SM drift pattern at design-time as the Step-4 source-type-filter gap (both surfaced 2026-05-10). The state-aware Step 5 design is more robust than the originally-specced fixed UI — it handles the future case where a NIN-less form gets published without breaking. Catches before Task 5 implementation; saves ~1 day of rework + post-deploy UX-correction churn. |
| 2026-05-10 | **Step 4 questionnaire injection — design clarification added to Dev Notes (no code change).** During the magic-link Task 1 dev session, a clarifying question from Awwal exposed a real spec gap: AC#1 Step 4 says the wizard renders the published `respondentSourceTypes='public'` form, but the codebase has NO source-type / role / target-audience filter on the `questionnaire_forms` schema. Verified across `apps/api/src/db/schema/questionnaires.ts`, `packages/types/src/native-form.ts:88` (`NativeFormSchema` has no `targetRoles` field), `apps/api/src/routes/form.routes.ts:24-27` (`/forms/published` returns ALL published forms, no filter). New Dev Notes subsection "Step 4 Questionnaire Injection — design clarification (2026-05-10)" enumerates 3 discovery options: **A** schema extension (add `respondent_source_types TEXT[]` column + Native Form Builder UI checkbox), **B** `system_settings` row (`wizard.public_form_id` setting; reuses prep-settings-landing infrastructure), **C** convention-based (most-recently-published form). **Recommends Option B** — consistent with AC#7 SMS OTP toggle pattern, ~1-day implementation, no schema migration. Task 5.4 expanded to a 9-subtask checklist (5.4.0 Awwal design pick → 5.4.8 tests) covering: discovery endpoint (`GET /forms/public-active`), `<FormRenderer>` decomposition from existing `apps/web/src/features/forms/pages/FormFillerPage.tsx`, draft form-version locking via new `WizardDraftData.questionnaireFormId/VersionId` fields (TypeScript-only addition; no migration), edge case for "no public form configured" empty state, two-progress-indicator UX. New Risks entry #9. Email-service-template Risks entry #8 also flipped RESOLVED with the inline-string outcome from Task 1.8 dev-time inspection. | This is the impostor-SM drift pattern recurring at design time, not just file-path time — the original story v1 (and even the v2 retrofit) carried forward an architectural assumption (`respondentSourceTypes` filter on questionnaires) that doesn't match the codebase. Catching it BEFORE Task 5.4 implementation saves a day of rework. The Option B recommendation leverages prep-settings-landing infrastructure and stays consistent with the broader Awwal Path B decision (Settings infrastructure once, used by multiple stories). |
| 2026-05-10 (session 2) | **Backend block COMPLETE — Tasks 1.9 + 2 + 3 all shipped; backend layer done.** Universal pending-NIN (Option 1) implemented end-to-end: (a) **Task 3.1** — removed NIN-required throw in `extractRespondentData`; submissions without NIN (or with explicit `_pendingNin: true` flag) now create `pending_nin_capture` respondents. Form-schema NIN-question check preserved. (b) **Task 3.5 race-resolution merge** in `findOrCreateRespondent` — atomic `UPDATE...WHERE id=(SELECT…) AND status='pending_nin_capture' AND nin IS NULL` race-safe under concurrent merge attempts; preserves original `submitter_id` (D3 productivity-credit policy). (c) **Task 3.6 + 3.7** — new `apps/api/src/services/pending-nin.service.ts` with pure-function `resolveReminderDestination` (5-branch precedence per source) + `getEnumeratorPendingNinAggregates` (CTE-based 7-day rolling pending-rate query, supervisor-dashboard widget UI deferred per D6). (d) **Tasks 3.2 + 3.3** — new `RegistrationController` + `registration.routes.ts` mounted at `/api/v1/registration`. `POST /complete-nin` peeks magic-link → FR21 dedupe → atomic consume + status promote; `POST /defer-reminder` peeks (no consume) + stamps `metadata.reminder_deferred_at`. (e) **Task 3.8** — audit actions 38 → 40 (`PENDING_NIN_CREATED` + `PENDING_NIN_PROMOTED`); sentinel test bumped preemptively. (f) **Task 1.9 reminder worker** — new `apps/api/src/queues/reminder.queue.ts` + `apps/api/src/workers/reminder.worker.ts` exposing `runReminderSweep()`; daily 09:00 UTC cron; for each pending row computes elapsed since `reminder_state.deferred_at ?? created_at`, fires highest un-sent milestone (T+2/7/14d) via `MagicLinkService.sendMagicLinkEmail` with a fresh token, transitions to `nin_unavailable` at T+30d. `MagicLinkService` gained `peekToken` + `consumeToken` for the two-phase flows. Wired into `workers/index.ts:initializeWorkers + closeAllWorkers` + `scheduleDailyReminders()`. (g) **Task 2 SMS OTP infra-only (flat layout)** — new `services/sms-provider.adapter.ts` (interface + `NoopSmsProvider` rejects with `SMS_OTP_DISABLED` 503 + deferred-binding registry) + `services/sms-otp.service.ts` (Redis-keyed 6-digit code, SHA-256 hash, 5-min TTL, 60s per-phone re-issue throttle, atomic single-use DEL on verify) + `controllers/sms-otp.controller.ts` (Zod-validated phone+code, 503 short-circuit when `auth.sms_otp_enabled = false` per `getSetting<boolean>` accessor). New routes `POST /auth/public/sms-otp/{request,verify}` mounted in `auth.routes.ts` behind `magicLinkRateLimit`. **Schema extension**: `RespondentMetadata` interface gained optional `reminder_state: Record<string, unknown>` + `defer_reason_nin: string` fields (TypeScript-only; JSONB column needs no migration). **Tests**: 23 net-new tests pass — 11 in `pending-nin.service.test.ts` (every cell of the 5-branch destination resolver) + 6 in `sms-otp.service.test.ts` (rate-limit, dispatch, single-use, NoopSmsProvider rejection) + 6 in `submission-processing.service.test.ts` `race-resolution merge` describe block (merge / fall-through / 3 missing-field skip cases / submitter-credit preservation). Pre-existing tests updated: `audit.service.test.ts:165` sentinel 38→40, `submission-processing.service.test.ts` "missing NIN throws" → "missing NIN creates pending row", `submission-ingestion.integration.test.ts` same behaviour change + AuditService stub + `db.execute` stub for the merge call site, `rate-limit-coverage.test.ts` extended with 2 new SMS OTP route entries. **Pre-commit checks**: lint clean both apps, tsc clean both apps. **Test totals (apps/api)**: 2,131 pass / 5 pre-existing DB-constraint failures (test DB out-of-sync — confirmed pre-existing via git stash; respondents.constraints.test.ts + audit-principal-dualism.test.ts; not caused by this session). **Deferred**: 1.11 route-level supertest for `/auth/magic` (bundles cleanly with the wizard E2E suite in Task 11.1; cost is shared); JWT issuance for magic-link login (lands when frontend drives the redirect in Task 4-7); enumerator + clerk frontend toggle wiring for universal pending-NIN (Task 13 — couples with Tasks 4-9 frontend session). **Task 4-9 frontend wizard remains** — largest surface area per Risk #3, separate session. | Cohesive backend deliverable. The deferred-coupling pattern paid off: race-merge logic (`PENDING_NIN_PROMOTED`) + reminder worker landing together meant the audit chain has both fire-points wired in one pass; the `peekToken` / `consumeToken` decomposition came up naturally in `complete-nin` and turned out cleaner than a single `redeemToken` for the FR21-dedupe-can-fail path; the inline-closure trick to survive `vi.resetAllMocks()` in the integration test prevents future mock-state-decay bugs. The aggregate-query shape (D6) ships unwired — supervisor-dashboard widget UI affordance lands post-field-survey when there's real distribution data to set a threshold against. |
| 2026-05-10 | **Task 1 PARTIAL — backend magic-link foundation (1.1-1.7 + 1.10 done; 1.8 inline; 1.9 + 1.11 deferred to next session).** Status `ready-for-dev → in-progress`. Files shipped: 2 schemas (`magic-link-tokens.ts` + `wizard-drafts.ts`), migration `0012_add_magic_links_and_wizard_drafts.sql`, service `magic-link.service.ts` (issue/redeem/revoke/sendEmail with atomic single-use via UPDATE...RETURNING + 3 per-purpose TTLs), rate-limit middleware `magic-link-rate-limit.ts` (3/email/1hr per NFR4.4 budget), controller `magic-link.controller.ts` (POST /public/magic-link anti-enumeration + GET /magic redemption returns JSON), 4 new `AUDIT_ACTIONS` (`MAGIC_LINK_ISSUED` + `MAGIC_LINK_REDEEMED` + `PENDING_NIN_DEFERRED` + `PENDING_NIN_TRANSITIONED`). Auth.routes.ts wired with 2 new endpoints. Coverage map (`rate-limit-coverage.test.ts`) extended with both endpoints + threshold contract entry for `magicLinkRateLimit`. **Tests: 17 service tests pass** (issue + redeem + revoke + URL build + per-purpose TTLs + atomic single-use + expiry + JSON serialization + lowercase-email-storage). **Pre-commit checks**: lint clean, tsc clean, middleware+routes regression 133/133. **Deferred**: 1.9 reminder worker (couples with Task 3 pending-NIN promotion logic; cohesive landing in next session); 1.11 route-level supertest layer (requires app-fixture pattern matching existing `auth.routes.test.ts`); JWT cookie issuance for magic-link login (lands when frontend drives the redirect flow in Task 4-7). **Task 2 (SMS OTP infra) + Task 3 (pending-NIN) + Task 4-7 (frontend wizard) all queued for subsequent sessions per Risk #3 (5-7 dev-days realistic).** | First session of FRC #3 implementation. The magic-link primitive is the load-bearing piece for AC#6 + #9 + #10 — landing it as a clean, tested, atomic deliverable de-risks the rest of the story. JWT issuance was scoped out because there's no frontend yet to redirect to; carrying that complexity into a backend-only commit would be premature. Cohesive next-session boundary: Task 1.9 + Task 2 + Task 3 = remaining backend; then Tasks 4-9 = frontend wizard (largest surface, separate session per Risk #3). |
| 2026-04-30 | Validation pass (Bob, fresh-context mode 2 per `_bmad/bmm/workflows/4-implementation/create-story/checklist.md`). Rebuilt to canonical template structure: folded top-level "Dependencies", "Field Readiness Certificate Impact", "Technical Notes" (preserving all 5 subsections — Magic-link token storage discipline / Pending-NIN reminder cadence rationale / Server-side draft vs IndexedDB / Why Google OAuth retirement / SMS OTP feature flag relationship to prep-settings-landing / Email service template handling), "Risks" under Dev Notes; converted task-as-headings (`### Task N — Title` + `1.1.` numbered subitems) to canonical `[ ] Task N (AC: #X)` checkbox format with `[ ] N.M` subtasks; added `### Project Structure Notes` subsection covering new feature dir + wizard layout + existing auth pages inventory + staff activation wizard existing path + service-layer pattern + workers convention + drizzle barrel + settings dependency + audit logging + rate-limit clone pattern + JWT issuance reuse + frontend HTTP client + TanStack Query convention + CSP discipline + email service flat-file pattern + existing routes file; added `### References` subsection with 24 verified `[Source: file:line]` cites; moved top-level `## Change Log` under `## Dev Agent Record` as `### Change Log`; added `### Review Follow-ups (AI)` placeholder; added Task 12 (code review) per `feedback_review_before_commit.md`. **Five factual path corrections applied throughout:** (1) `apps/web/src/features/auth/pages/PublicRegisterPage.tsx` → `RegistrationPage.tsx` (verified actual filename); (2) `apps/web/src/features/auth/pages/PublicLoginPage.tsx` → `LoginPage.tsx` (verified actual filename); (3) `apps/web/src/features/staff-activation/` (fictional dir) → `apps/web/src/features/auth/components/activation-wizard/` (verified existing path; AC#12 retro-fits the existing wizard at this canonical location); (4) `apps/api/src/lib/settings.ts` referenced as if existing → relocated as HARD dependency on companion story `prep-settings-landing-and-feature-flags` which creates this lib (per Awwal Path B decision 2026-04-30 — Settings infrastructure is a separate prep task, not folded into 9-12); (5) `apps/api/src/services/email/templates/` referenced as existing dir → email service is currently flat file; Task 1.8 explicitly defers template-pattern decision to dev-time inspection (refactor only if needed; separate prep task if so). **AC#7 reframed to consume prep-settings-landing:** SMS OTP feature flag mechanism (system_settings DB table + `getSetting` accessor) lives in prep-settings-landing-and-feature-flags; super-admin toggle UI lives in Settings Landing Page; this story is the BACKEND consumer of the flag (route + provider adapter + service + audit on attempts), not the UI owner. **Four new audit actions documented** for `AUDIT_ACTIONS` const extension: `MAGIC_LINK_ISSUED`, `MAGIC_LINK_REDEEMED`, `PENDING_NIN_DEFERRED`, `PENDING_NIN_TRANSITIONED`. All 14 ACs preserved verbatim. Status `ready-for-dev` preserved. | Story v1 was authored by impostor-SM agent without canonical workflow load — same drift pattern as Stories 9-13 / prep-tsc / prep-build-off-vps / 11-1 / prep-input-sanitisation-layer / 10-5 / 9-11 / 11-2 / 11-4. This story is the LARGEST in the retrofit batch (14 ACs, 12 Tasks, 5 path corrections, 1 dependency-relocation per Awwal Path B). The new prep-settings-landing dependency was created during this same retrofit cascade as the cleanest resolution to Q2 (settings storage for `auth.sms_otp_enabled` feature flag) — avoiding the technical-debt path of a one-off settings hack scoped to 9-12. |

### Review Follow-ups (AI)

_Code review pass executed 2026-05-11 (session 6) via `/bmad:bmm:workflows:code-review` on the uncommitted working tree per `feedback_review_before_commit.md`. 21 findings logged below (1 CRITICAL + 5 HIGH + 8 MEDIUM + 7 LOW). 16 auto-fixed in this same session; 5 deferred with rationale. Test counts post-fix: **2,121 API pass / 5 pre-existing DB-constraint failures / 7 skipped** (zero new regressions); API tsc + lint clean; web tsc clean. Routes added: `POST /api/v1/auth/magic/consume`. New schema fields: `wizard_drafts.form_data.reminder_state.last_dispatched` + `.last_dispatch_reason` (TypeScript-only)._

- [x] [AI-Review][CRITICAL] C1 — Magic-link GET endpoint consumes token on first hit; email-scanner prefetchers (Gmail/Outlook/Defender/Slack/Discord) will burn legitimate users' tokens before they click. **FIXED**: `GET /auth/magic` now calls `peekToken` (no `used_at` write, no audit event). New `POST /auth/magic/consume` (rate-limited, audit-logged) is the explicit consume path; frontend resume page should drive it from a Confirm button when wired in a future task. Rate-limit coverage map updated with rationale. [`apps/api/src/routes/auth.routes.ts:216-228` + `apps/api/src/controllers/magic-link.controller.ts:122-208`]
- [x] [AI-Review][HIGH] H1 — `POST /registration/wizard` had NO captcha and NO rate limit. **FIXED**: `registrationRateLimit` (5/IP/15min) wired into `registration.routes.ts:38`. Captcha integration deferred to follow-up — requires frontend hCaptcha widget on Step 5 of the wizard (not in scope for this code-review fix pass). [`apps/api/src/routes/registration.routes.ts:38`]
- [x] [AI-Review][HIGH] H2 — `PUT /registration/draft` had no auth, no rate limit, and used `.passthrough()`. **FIXED**: `magicLinkRateLimit` wired onto both PUT + GET (per-email primary, per-IP fallback). Zod schema flipped from `.passthrough()` to `.strict()` with all known wizard fields enumerated explicitly (`formHasNinQuestion`, `questionnaireFormId`, `questionnaireFormVersionId` added; `extras: z.record(z.unknown())` retained as forward-compat slot). [`apps/api/src/routes/registration.routes.ts:33-34` + `apps/api/src/controllers/registration.controller.ts:39-65`]
- [x] [AI-Review][HIGH] H3 — `GET /registration/draft?email=X` leaked the full draft. **FIXED**: `?email=` branch deleted; magic-link `?token=<wizard_resume>` is now the only hydration path. Frontend `useWizardDraft.ts` already only calls with `?token` (`apps/web/src/features/registration/hooks/useWizardDraft.ts:70`), so zero frontend-facing impact. [`apps/api/src/controllers/registration.controller.ts:385-433`]
- [x] [AI-Review][HIGH] H4 — Naive name parsing duplicated single-token names into both `firstName` and `lastName`. **FIXED**: single-token names now store `firstName=trimmed, lastName=NULL`; multi-token splits at the first space (`firstName=first chunk, lastName=rest`). Schema columns are both `text` (nullable) — no migration needed. Race-resolution merge in `submission-processing.service.ts:557-559` now keys on actual names, not duplicate pseudo-doubles. [`apps/api/src/controllers/registration.controller.ts:497-500`]
- [x] [AI-Review][HIGH] H5 — `completeNin` consume + UPDATE were non-transactional. **FIXED**: new `MagicLinkService.consumeTokenTx(tx, args)` runs the atomic-update consume inside a caller-provided transaction; `completeNin` wraps consume + status promotion in one `db.transaction(async tx => { ... })`. A UPDATE failure after consume now rolls both back, preserving the user's link for retry. [`apps/api/src/services/magic-link.service.ts:233-291` + `apps/api/src/controllers/registration.controller.ts:140-176`]
- [x] [AI-Review][MEDIUM] M1 — Reminder worker counted SMS + supervisor-task placeholders as "delivered". **FIXED**: dispatch functions now return `{dispatched: boolean, reason: string}`; `markMilestoneSent` records BOTH flags into `metadata.reminder_state` (`last_dispatched`, `last_dispatch_reason`). Audit chain consumers can identify silent gaps by querying `reminder_state.last_dispatched = false`. Milestone is still marked sent (avoids daily retry storm); operator visibility via the new metadata fields. [`apps/api/src/workers/reminder.worker.ts:96-168, 207-227, 304-321`]
- [x] [AI-Review][MEDIUM] M2 — `SmsOtpService.requestOtp` stored OTP before provider dispatched. **FIXED**: dispatch happens FIRST; Redis state is only written after the provider returns success. Provider failure leaves zero Redis state, so the user's next attempt isn't gated by the throttle. [`apps/api/src/services/sms-otp.service.ts:53-91`]
- [x] [AI-Review][MEDIUM] M3 — Two separate `redis.set` calls in `requestOtp`. **FIXED (session 6 via MULTI/EXEC; superseded session 7 by MR-1 NX claim)**: M3's original goal (atomic two-key write) is now achieved by the MR-1 NX-claim refactor — the rate-limit key lands atomically via `SET ... EX ... NX` BEFORE dispatch; the OTP hash lands AFTER dispatch confirms. Either side can fail independently, and provider failure releases the claim via `DEL`. No MULTI/EXEC needed. [`apps/api/src/services/sms-otp.service.ts:53-99`]
- [x] [AI-Review][MEDIUM] M4 — `stored !== hashCode(code)` byte-wise comparison. **FIXED**: `crypto.timingSafeEqual` after equal-length check. Both sides are SHA-256 hashes (32-byte buffers), so the timing-safe path is well-defined. [`apps/api/src/services/sms-otp.service.ts:95-105`]
- [x] [AI-Review][MEDIUM] M5 — `EmailService` import + `void EmailService` tsc-suppressor in reminder worker. **FIXED**: unused import deleted. The module-level worker side-effect (`export const reminderWorker = isTestMode() ? null : startReminderWorker()`) deliberately KEPT — matches the pattern across `email.worker.ts` / `import.worker.ts` / 7 other worker files; Story 9-10 fix was specifically about `connection.quit()` crash on SIGINT, not about the side-effect-at-import pattern. [`apps/api/src/workers/reminder.worker.ts:32-44, 358-363`]
- [x] [AI-Review][MEDIUM] M6 — `submitWizard` insert + audit + draft-delete were separate non-transactional operations. **FIXED**: wrapped in `db.transaction(async tx => { ... })` with `AuditService.logActionTx` so audit + insert + draft delete all commit atomically. Magic-link issuance for pending-NIN path stays OUTSIDE the transaction (deliberate — magic-link delivery is fire-and-forget and shouldn't roll back the registration). [`apps/api/src/controllers/registration.controller.ts:509-557`]
- [x] [AI-Review][MEDIUM] M7 — 409 response leaked prior registration timestamp + source. **FIXED**: both `completeNin` and `submitWizard` now return generic "This NIN is already registered. If you believe this is an error, please contact support." with no `details` payload. Frontend `CompleteNinPage.tsx:39-40,210-216` gracefully degrades — already wraps the display in `error.firstRegisteredAt && ...`. [`apps/api/src/controllers/registration.controller.ts:127-141, 477-487`]
- [x] [AI-Review][MEDIUM] M8 — **FIXED 2026-05-11 session 8 — "no technical debt" close-out.** 45 new route-level supertests across 3 new test files: (a) `apps/api/src/routes/__tests__/registration.routes.test.ts` (25 tests across `complete-nin`/`defer-reminder`/`PUT draft`/`GET draft`/`wizard` — Zod validation, anti-enumeration generic 400, NIN_DUPLICATE shape (no MR-7 leakage), transactional happy-paths, race-resolution promotion, alreadyPromoted branch, partial-unique-violation→409 NIN_DUPLICATE translation). (b) `apps/api/src/routes/__tests__/magic-link.routes.test.ts` (12 tests across `POST /public/magic-link`/`GET /magic`/`POST /magic/consume` — anti-enumeration sent:false branch, plaintext-never-in-response, GET is PEEK-ONLY (C1 contract verified — `redeemToken` NEVER called on GET; no `MAGIC_LINK_REDEEMED` audit on GET), POST consume idempotent + audited). (c) `apps/api/src/routes/__tests__/sms-otp.routes.test.ts` (8 tests across `request`/`verify` — flag-off 503 short-circuit, non-E.164 phone 400, throttle 429, wrong-code 400, success 200). All 45 pass. Pattern: `vi.hoisted` mocks + drizzle fluent-chain mocks for `db.insert/update/delete/query/transaction` + service-level mocks for `MagicLinkService`/`AuditService`/`SmsOtpService`/`isSmsOtpEnabled` + middleware pass-through. `audit-log-viewer.routes.test.ts` was the template.
- [ ] [AI-Review][LOW] L1 — Timing-oracle differences between "invalid" / "used" / "expired" paths in `redeemToken` follow-up SELECT. **DEFERRED**: acceptable trade-off for 72h tokens; the differential timing leak is bounded by the SELECT cost, not the cryptographic primitive. Not fixed. [`apps/api/src/services/magic-link.service.ts:147-171`]
- [x] [AI-Review][LOW] L2 — `pending_nin_unresolved_at_7d` filter hard-coded regardless of caller's `windowDays`. **FIXED**: new `unresolvedThresholdDays` option on `getEnumeratorPendingNinAggregates` (default 7) parameterises the SQL interval. [`apps/api/src/services/pending-nin.service.ts:118-148`]
- [ ] [AI-Review][LOW] L3 — `MagicLinkService.consumeToken` alias of `redeemToken`. **DEFERRED**: kept as semantic alias — `peekToken` + `consumeToken` reads cleaner at call sites than two calls to `redeemToken`. Replaced by `consumeTokenTx` for transactional callers (H5 fix). Single redundancy is acceptable.
- [x] [AI-Review][LOW] L4 — Inconsistent Zod-error responses across wizard endpoints. **FIXED**: `completeNin`, `deferReminder`, `saveDraft`, `submitWizard` all now return generic `400 INVALID_INPUT` with no `details.issues` payload, matching `requestMagicLink`'s anti-enumeration discipline. [`apps/api/src/controllers/registration.controller.ts:97-99, 215-217, 303-305, 454-456`]
- [ ] [AI-Review][LOW] L5 — `/register/complete-nin` and `/register/complete` missing `PublicOnlyRoute` wrapper. **DEFERRED**: the magic-link landing pages must work regardless of auth state (a logged-in user clicking a `pending_nin_complete` link should still complete their respondent record). Current behaviour is intentional. [`apps/web/src/App.tsx:592-610`]
- [ ] [AI-Review][LOW] L6 — `BRAND_COLOR` duplicated. **DEFERRED**: `EmailService.BRAND_COLOR` is `private static readonly`; importing requires widening the API surface for a single co-located co-branded constant. The duplicate adds zero maintenance cost (Oyo State maroon is fixed by Oyo State brand guidelines, not by code). Not worth the visibility flip. [`apps/api/src/services/magic-link.service.ts:34`]
- [x] [AI-Review][LOW] L7 — Retired Google OAuth route had no rate limit. **FIXED**: `magicLinkRateLimit` wired onto `POST /google/verify`; rate-limit coverage map updated. [`apps/api/src/routes/auth.routes.ts:48`]

_Meta-review of session-6 auto-fixes (2026-05-11 session 7) — 15 follow-up findings on the fixes themselves. Adversarial review of the diff catches: 2 NEW HIGH regressions introduced BY the fixes (MR-1, MR-11), plus 4 deferred design notes, plus 9 clean spot-checks. Auto-fixes applied to MR-1, MR-11, MR-14 inline this session._

_"No technical debt" close-out (2026-05-11 session 8) — Awwal directive to resolve every deferred item before commit. **Task 10.3** (hybrid template + 4 legacy registration endpoints): FIXED end-to-end across routes / controllers / service / email queue+worker / `@oslsr/types` / middleware / frontend pages / tests + snapshot. **MR-8 (frontend landing page)**: FIXED — new `MagicLinkLandingPage` + new `/auth/magic` frontend route + 12 tests. **MR-8 (login-purpose JWT)**: NOT 9-12 debt — feature gap that needs its own story; the landing page renders a deferred-notice for that branch. **M8** (route-level supertests): FIXED — 45 new tests across 3 new test files for all 9 wizard / magic-link / SMS-OTP endpoints. **MR-5** (audit-chain serialization): NOT 9-12 debt — reframed as capacity-planning note; the `SELECT ... FOR UPDATE` is the Story 6-1 hash-chain contract. Quality gates after session 8: API tsc + lint clean; API vitest **2,138 pass / 5 pre-existing DB-constraint failures / 7 skipped**; web tsc clean; web vitest **2,454 pass / 2 todo / 0 failures**. **Net delta this session**: 7 new files (1 frontend landing page + 1 frontend api lib + 1 frontend test + 3 backend route tests + 1 cleanup placeholder in `packages/types/src/validation/registration.ts`); ~18 file modifications across the legacy-retirement sweep; 6 files deleted outright (`RegistrationService` + its test + `VerifyEmailPage` + `ResendVerificationPage` + their 2 tests)._

- [x] [AI-Review][HIGH] MR-11 — Session-6 H2 fix wired `magicLinkRateLimit` (3/email/hour) onto `PUT/GET /registration/draft`. **Wrong limiter for the use case**: useWizardDraft does 2-second-debounced auto-save → typical 20-60 saves per wizard session. 3/hour would lock users out within seconds. AND per-email keying doesn't defend against the actual H2 threat (per-IP flood with random emails). **FIXED**: new dedicated `wizardDraftRateLimit` middleware at 120/IP/15min — permissive for 2s-debounced auto-save (~30/session typical) but bounds single-IP flooding. Per-IP keying replaces per-email. [`apps/api/src/middleware/wizard-draft-rate-limit.ts` (NEW) + `apps/api/src/routes/registration.routes.ts:34-35`]
- [x] [AI-Review][HIGH] MR-1 — Session-6 M2 fix reordered SmsOtp `requestOtp` to dispatch-then-write, but this introduced a concurrent-call race: two simultaneous calls for the same phone both see `lastSent = null` → both dispatch SMS → user receives 2 codes. The pre-fix write-then-dispatch was atomic on the rate-limit dimension. **FIXED**: atomic `SET key val EX ttl NX` claim BEFORE dispatch; provider failure DELs the claim so user can retry. Closes the concurrent-dispatch window AND preserves M2's "provider failure leaves no state" goal. [`apps/api/src/services/sms-otp.service.ts:53-99`]
- [ ] [AI-Review][MEDIUM] MR-5 — **REFRAMED as capacity-planning note, not 9-12 technical debt (2026-05-11 session 8 — "no technical debt" close-out review).** `submitWizard` runs the insert + `logActionTx` audit-log entry inside one `db.transaction`. `logActionTx` does `SELECT ... FOR UPDATE` on `audit_logs` to read the latest hash. That serializes the hash-chain tail. During concurrent wizard submits, registrations queue on this lock. **This is the Story 6-1 hash-chain CONTRACT**, not a 9-12-introduced issue: every audited mutation in the codebase has this property (deactivate user / role change / data create / settings flipped / magic-link issued etc — see `audit.service.ts:140-200` for the full surface). The wizard joining the audit-protected set is correct (NDPA + forensic chain integrity > throughput). Two alternative "fixes" both have worse trade-offs: (a) audit fire-and-forget OUTSIDE the transaction — drops integrity guarantee, registrations could be unaudited if the audit write fails; (b) batched / Merkle-tree audit chain — cross-story rewrite of Story 6-1. Neither belongs in 9-12. **Action**: capacity-plan the audit-log tail (Postgres `audit_logs` is the bottleneck — partition by date / index pruning / move to TimescaleDB if registrations cross ~50/sec sustained). Owner: a future infrastructure story, not 9-12. [`apps/api/src/controllers/registration.controller.ts:509-557` + `apps/api/src/services/audit.service.ts:226-244` + Story 6-1 ADR]
- [x] [AI-Review][MEDIUM] MR-8 — **PARTIALLY FIXED 2026-05-11 session 8 — "no technical debt" close-out.** Root cause of the JSON-in-browser UX: the email link URL `${APP_URL}/auth/magic?token=…&purpose=…` points at the FRONTEND host (`APP_URL` defaults to `http://localhost:5173` = Vite), but there was no frontend route at `/auth/magic`. Clicking from email landed on React Router's catch-all 404 (or — if a user typed the backend URL — on the API JSON response). **Fixed**: new frontend route `/auth/magic` rendering `MagicLinkLandingPage` (`apps/web/src/features/auth/pages/MagicLinkLandingPage.tsx`). On mount, peeks the token via `peekMagicLink` (idempotent, prefetcher-safe), renders a Confirm card with per-purpose copy + the destination email, and on Confirm click routes the user to the destination handler that owns the actual consume: `wizard_resume → /register?token=…` (`useWizardDraft` peeks + hydrates), `pending_nin_complete → /register/complete-nin?token=…` (`CompleteNinPage` peeks + consumes on Save). The landing page does NOT itself call `POST /auth/magic/consume` — single-use semantics stay honest (token consumed when user completes the downstream work, not when they click through the landing). Error UI covers `MAGIC_LINK_INVALID/ALREADY_USED/EXPIRED` + missing-params + network failure. New `apps/web/src/features/auth/api/magic-link.api.ts` (peek + consume fetchers). 12 new component tests in `MagicLinkLandingPage.test.tsx` (missing params × 3 / wizard_resume peek + Continue × 2 / pending_nin_complete peek + Continue × 2 / login-purpose deferred notice × 1 / peek-error × 4). **STILL deferred** as a separate concern (NOT 9-12 technical debt — it's a feature gap): JWT issuance for `login` purpose. The landing page currently renders a "Magic-link sign-in coming soon — use email + password" notice for the login branch because the backend `POST /auth/magic/consume` for purpose=login doesn't yet issue a session JWT (would require `AuthService.loginByMagicLink` + session cookie + ADR amendment). Captured as a separate follow-up below the table.
- [ ] [AI-Review][LOW] MR-2 — `crypto.timingSafeEqual` length-mismatch early-return path itself produces a measurable timing difference; both sides are always 32 bytes in production so it never fires. **NOT FIXED** — practical impact is zero. [`apps/api/src/services/sms-otp.service.ts:101-105`]
- [ ] [AI-Review][LOW] MR-3 — Name parser edge cases (apostrophes "D'Angelo", hyphens "Jean-Claude", double-spaces "Adebayo  Adejumo"). Verified: trim+collapse+first-space split handles all correctly. **NO ACTION** — already correct.
- [ ] [AI-Review][LOW] MR-4 — `completeNin` race semantic: if the respondent was already promoted by a race-resolution merge, the user's token is still consumed (single-use enforced) and the controller returns `alreadyPromoted: true`. Audit chain shows `MAGIC_LINK_REDEEMED` + no `PENDING_NIN_PROMOTED` (correct — no status change happened). **NO ACTION** — semantic is intentional. [`apps/api/src/controllers/registration.controller.ts:167-180`]
- [ ] [AI-Review][LOW] MR-6 — `getDraft` no longer accepts `?email=` (H3 fix removed it). Currently dead code in the frontend (useWizardDraft.ts:68-94 only hydrates with `?token`). Future same-session resume capability is reduced — if the frontend wants to support browser-refresh recovery without a magic link, it needs IndexedDB local cache (Dev Notes "Server-side draft vs IndexedDB" calls this out as deferred). **NOT FIXED** — by design; flag in Dev Notes. [`apps/api/src/controllers/registration.controller.ts:385-431`]
- [ ] [AI-Review][LOW] MR-9 — `magicLinkRateLimit` on retired `POST /google/verify` falls back to per-IP (no email in body). Reasonable. **NO ACTION** — verified correct.
- [ ] [AI-Review][LOW] MR-10 — `registrationRateLimit` (5/IP/15min) budget shared between `/auth/public/register` (legacy) and `/registration/wizard` (new) for a single IP. Intentional — a single IP shouldn't be able to flood either path. **NO ACTION** — correct.
- [ ] [AI-Review][LOW] MR-12 — Frontend `CompleteNinPage.tsx:39-40, 210-216` `firstRegisteredAt` / `firstRegisteredVia` display path is now dead code on the real backend (M7 fix). Frontend gracefully degrades via `error.firstRegisteredAt && ...`. Test continues to pass (test mocks the response with the fields). **NOT FIXED** — leaving the optional fields in the frontend type lets the path light back up if a future code-review decision restores the fields. Not worth the churn. [`apps/web/src/features/registration/pages/CompleteNinPage.tsx`]
- [ ] [AI-Review][LOW] MR-13 — Generic 400 `INVALID_INPUT` errors are less helpful for user debugging. Anti-enumeration trade-off accepted. **NO ACTION** — by design. [`apps/api/src/controllers/registration.controller.ts:97-99, 215-217, 303-305, 454-456`]
- [x] [AI-Review][LOW] MR-14 — `pending_nin_unresolved_at_7d` field name in `EnumeratorPendingAggregate` is misleading when `unresolvedThresholdDays != 7`. **FIXED**: renamed to `pending_nin_unresolved_count` (threshold-agnostic) since no consumers exist yet (the supervisor-dashboard UI is deferred to post-field-survey per D6). Cleaner interface. [`apps/api/src/services/pending-nin.service.ts:109-171`]
- [ ] [AI-Review][LOW] MR-15 — `.strict()` Zod schema enumerates all 18 frontend WizardDraftData fields. Verified in-sync 2026-05-11. **NO ACTION** — if a future field is added to frontend WizardDraftData, the backend `.strict()` will start rejecting drafts; the breakage is loud (HTTP 400) so the drift is caught fast. [`apps/api/src/controllers/registration.controller.ts:39-65`]

---

**Cross-story follow-up — NOT 9-12 technical debt; filed for owner clarity:**

- [ ] **`9-14-magic-link-login` (proposed)** — Build out the `login`-purpose magic-link flow end-to-end. **Why this isn't 9-12 debt**: Task 1.7 explicitly scoped JWT issuance out of Story 9-12 ("JWT issuance for full magic-link login flow deferred"); existing public_users continue to authenticate via `POST /auth/public/login` (email + password), which is unaffected by 9-12. Magic-link login is a NEW feature that lights up an additional auth channel. **Scope**: (1) frontend "Sign in with magic link" button on `/login` for type=public users → `POST /auth/public/magic-link` with purpose=login. (2) Backend: extend `AuthService` with a `loginByMagicLinkToken` method that consumes a magic-link token, looks up the public_user by email, issues `accessToken` + `refreshToken` cookies via the existing `loginPublic` token-issuance path. (3) Frontend `MagicLinkLandingPage` `login` branch: replace the "coming soon" notice with a Confirm card that POSTs `/auth/magic/consume` (or a new `/auth/magic/login` if we want a dedicated endpoint) → on success, server sets the refresh cookie + returns access token, frontend stores the access token + redirects to `/dashboard`. (4) ADR-015 amendment documenting the magic-link login as primary alongside password. (5) Tests: AuthService magic-link login path + frontend landing-page login branch + route-level supertest. **Effort**: ~3-5 dev-days. **Priority**: post-field-survey unless Awwal escalates — existing email+password login covers all current public_users and the field-survey scope.

