# Session Notes — 2026-04-21 to 2026-04-25

**Owner:** Awwal + Claude
**Window:** Tue 2026-04-21 → Sat 2026-04-25 (5 calendar days, ~12 hours active session time across spans)
**Previous session:** `docs/session-2026-04-11-12.md`
**Trigger:** what was meant to be a Story 9-9 retrospective walk-up turned into a multi-day operational + planning marathon
**Read this first** when resuming any of: Story 9-9, Story 11-1, Epic 10, Epic 11, the Sprint Change Proposal handoff chain, the Tailscale runbook, or the Baseline Report v2 plan.

---

## TL;DR

- **Started:** retrospective workflow invocation (intended as 9-7/9-8 reflection)
- **Pivoted:** Monday 2026-04-20 distributed SSH brute-force attack mid-session (CPU 100% / Memory 82% / 14+ attacking IPs) reframed the agenda
- **Ended:** Tailscale + SSH lockdown deployed; OS upgraded; Sprint Change Proposal authored, locked, and propagated through John (PM) + Winston (Architect) + Sally (UX) of the BMAD agent chain
- **Outstanding handoffs:** A.4 PM+Architect joint `create-epics-and-stories` → A.5 Bob's `create-story` per story → A.6 Dev implementation
- **Field readiness:** 1 of 6 Field Readiness Certificate items now ✅ (Tailscale live + SSH public port closed at sshd-key-only); 5 still pending

---

## Session arc — chronological

### Day 1 — Tue 2026-04-21 (the pivot)

- Awwal invoked the BMM retrospective workflow expecting a quiet Story 9-7/9-8 reflection
- Two CPU/Memory alert digest emails surfaced (Mon 2026-04-20 08:03 + 11:04 WAT) for the production VPS
- Investigation via SSH revealed a distributed SSH brute-force attack from 14+ IPs (`2.57.122.x`, `144.31.234.20`, `92.118.39.x`, `45.227.254.170`, `172.93.100.236`, `43.128.106.113`, `118.194.234.8`, `103.189.235.33`, `213.209.159.231`, `2.57.121.25`, `45.148.10.50`, `64.89.160.135`) hammering port 22 with usernames `root`, `ubuntu`, `oyotradeministry`, `test`, `user`, `hadi`, `amssys`. Detection-to-response latency: 19 hours
- Story 9-9 backlog (then Cloudflare-only) re-validated as urgent; Tailscale + SSH lockdown identified as P0
- Conversation moved into scope design — Ministry-pressure for progress report → Option A Baseline Report v2 commitment (full refresh, written as if delivered, gated by Deliverable Commitment Matrix)
- ITF-SUPA PDF (`Oyo_shortlisted_artisans.pdf`) introduced as concrete trigger for multi-source registry capability — Epic 11 designed
- API governance question (Epic 10) emerged in the consumer-auth design conversation
- Field-friction on NIN capture surfaced — Path A `pending_nin_capture` status model adopted over blanket NIN relaxation
- Codebase read confirmed admin audit dashboard gap (sidebar 13 items, 0 audit) → Story 9-11 scoped
- Public wizard collapse (Story 9-12) emerged from UX comparison with ITF-SUPA screenshots

### Day 2 — Wed 2026-04-22 (BMAD discipline correction + SCP)

- **Awwal flagged the BMAD discipline breach:** decisions had accumulated in chat without flowing through PM → Architect → UX → SM
- `correct-course` workflow invoked (incremental mode) — produced Sprint Change Proposal `_bmad-output/planning-artifacts/sprint-change-proposal-2026-04-22.md`
- Six change streams formalised:
  1. FR21 scoping refinement (scoped not removed)
  2. New Epic 10 (API Governance, 6 stories)
  3. New Epic 11 (Multi-Source Registry, 4 stories)
  4. Story 9-11 (Admin Audit Log Viewer)
  5. Story 9-12 (Public Wizard + Pending-NIN + NinHelpHint + Magic-Link Email)
  6. Story 9-9 scope expansion (Tailscale + 9 more subtasks)
- Field Readiness Certificate gate (6 items) defined as the field-survey go/no-go
- Akintola-risk composite-index lesson absorbed into Story 11-1 AC#11, Story 9-10 AC#4, Story 9-11 AC for audit-viewer-at-1M-rows; `prep-query-performance-gate` parked as named-but-not-in-scope item
- Scope locked
- Transfer Protocol template (D2) drafted at `docs/transfer-protocol-template.md`
- Epic 10-1 design brief authored at `docs/epic-10-1-consumer-auth-design.md` with Awwal's expanded-scope answers (LGA scoping + IP allowlist + time-bounded grants as MVP, not deferred)

### Day 3 — Thu 2026-04-23 (Tailscale buildout)

- John (PM) invoked in fresh CLI session via SCP Appendix A.1 prompt → returned PRD V8.2 (jumped V7.8→V8.2 rather than the V7.9 target — editorial call, no scope drift)
- Tailscale Phases 0–8 executed by Awwal
  - Pre-flight: account creation (lawalkolade@gmail.com Free tier), VPS install, laptop install
  - SSH key auth configured (id_ed25519 — turned out to already exist on laptop from 2026-01-19; needed only to be appended to VPS authorized_keys + ~/.ssh/config created)
  - Firewall narrowed to `100.64.0.0/10` (DO Cloud Firewall "OSLRS")
  - sshd hardened (`PasswordAuthentication no`, `PermitRootLogin prohibit-password`, `PubkeyAuthentication yes`)
  - **Discovered:** sshd_config drop-in override gotcha — `/etc/ssh/sshd_config.d/50-cloud-init.conf` had `PasswordAuthentication yes` which was overriding main config. Fixed by editing drop-in to match.
  - **Discovered:** Ubuntu service name is `ssh.service` not `sshd.service` (`sudo systemctl reload ssh` not `reload sshd`)
  - fail2ban installed
  - Verification: public-IP SSH timed out, key-disabled SSH refused, Tailscale SSH succeeded with no password prompt
- Emergency recovery runbook authored at `docs/emergency-recovery-runbook.md`
- DO Console verified failing under `100.64.0.0/10`-only firewall — initially diagnosed as ISP filtering (later proven wrong)

### Day 4 — Fri 2026-04-24 (Winston + UX prep)

- Winston (Architect) invoked via SCP Appendix A.2 prompt → returned amended architecture.md (4398 → 5077 lines), 3 new ADRs (018 Multi-Source Registry & Pending-NIN, 019 API Consumer Authentication, 020 Tailscale Operator-Access)
- New Decisions added: 1.5 (nullable-NIN schema), 2.4–2.8 (apiKeyAuth + magic-link + SMS OTP + Tailscale + ambiguous-auth), 3.4 (`/api/v1/partner/*` namespace), 5.3–5.5 (audit principal dualism + rate-limit metrics + pending-NIN events)
- ADR-013 gained Tailscale subsection; ADR-015 fully rewritten (Google OAuth retired; original preserved as "Superseded — Original ADR-015")
- **Caught Winston's one inaccurate claim:** ADR-020 line 3253 originally claimed firewall retained GH Actions IP ranges. Wrong — investigation confirmed the firewall was `100.64.0.0/10`-only and CI deploys would have failed on next push. Reality: last CI run was Apr 20 commit 36ccfbb (pre-firewall-change), so no broken state existed yet
- Sally's prompt drafted into SCP Appendix A.3

### Day 5 — Sat 2026-04-25 (Sally + Option A + OS upgrade)

- **Option A firewall change** — re-added `0.0.0.0/0` + `::/0` to SSH rule alongside `100.64.0.0/10`. Trade-off: sshd-key-only is the actual control; firewall narrows to defence-in-depth only. Long-term resolution: self-hosted GH Actions runner inside tailnet (Story 9-9 follow-up subtask)
- **DO Console architectural finding:** the moment `0.0.0.0/0` was re-added, DO Console started working. Initial "ISP WebSocket filtering" theory was wrong — DO Console connects via SSH from DO infrastructure IP ranges (e.g. `162.243.0.0/16` observed in Last login entries). The `100.64.0.0/10`-only firewall was blocking DO's own Console infrastructure. Documented in ADR-020 Consequences + emergency-recovery-runbook §1.1 + §2.2
- Sally (UX) invoked via SCP A.3 prompt → returned amended ux-design-specification.md (5226 → 6363 lines)
  - Journey 2 rewritten end-to-end (5-step wizard, magic-link primary, pending-NIN, trust badges); original preserved as "Superseded — Original Journey 2 (2026-01-22)"
  - 3 new Form Patterns + Visible Step Indicator Progress Pattern
  - Super-admin sidebar additions
  - 6 new custom components (NinHelpHint, SourceBadge, ImportDryRunPreview, AuditLogFilter, ApiConsumerScopeEditor, LawfulBasisSelector) with full anatomy/states/variants/accessibility/props/wireframe
  - Partner-API Rate Limiting UX patterns
  - NDPA Compliance Checklist updated
  - Journeys 5–8 authored
- **OS upgrade executed:** Ubuntu 24.04.3 → 24.04.4, kernel 6.8.0-90 → 6.8.0-110, 49 packages upgraded
  - Pre-flight: tailscaled enable-on-boot verified, PM2 startup hook (pm2-root.service via systemd) registered + `pm2 save`, Docker containers `unless-stopped` policy applied
  - Reboot 08:54:37 UTC, all services up post-reboot
  - **PM2 restart counter reset 916+ → 0** — establishes baseline for Story 9-10 investigation observability window
- Two snapshots taken: `pre-os-upgrade-2026-04-25` + `clean-os-update-2026-04-25`
- Sprint-status.yaml updated with 5 days of comment blocks + Epic 10/11 + new story keys
- Persistence pass executed (this session-notes file + memory updates + playbook appendices)

---

## Key decisions and why

### Path A — pending-NIN status model (2026-04-22)

**Decision:** Nullable NIN with partial unique index + status enum (`active | pending_nin_capture | nin_unavailable | imported_unverified`) instead of (a) blanket FR21 removal or (b) separate `external_beneficiaries` table.

**Why:**
- Field friction (forgotten NIN at submission) and secondary-data ingestion (ITF-SUPA PDF lacks NIN) had the same root cause: NIN-presence was being conflated with respondent-existence
- B2 (extend respondents, nullable NIN) preserves single canonical registry — analytics consistently asks "all respondents" not "all field respondents"
- Partial unique index `WHERE nin IS NOT NULL` chosen over `UNIQUE NULLS NOT DISTINCT` (PG 15+ feature) for Postgres portability
- FR21 dedupe still enforced at DB boundary for NIN-present rows

**Captured in:** ADR-018, FR21 + FR28, Story 11-1 AC, Story 9-12

### Option A — firewall trade-off (2026-04-25)

**Decision:** Re-add `0.0.0.0/0` to SSH firewall rule alongside `100.64.0.0/10` rather than maintain tailnet-only.

**Why:**
- GH Actions runners (CI deploy via `appleboy/ssh-action`) are on GitHub-hosted runner public IPs, not tailnet — would have been blocked
- GH Actions IP range list is ~6500+ entries; impractical for DO firewall (typically ~50 rule limit)
- sshd is hardened to key-only; firewall narrowing was defence-in-depth, not primary control
- Acceptable cost: brute-force attempts reach sshd and fail (CPU cost only); fail2ban handles repeat offenders

**Long-term:** self-hosted GH Actions runner inside tailnet → re-narrow firewall (Story 9-9 follow-up subtask)

**Captured in:** ADR-020 Consequences (line 3253), emergency-recovery-runbook §1.1

### Tailscale over Cloudflare ZT / WireGuard / DO Console-only (2026-04-22)

**Decision:** Tailscale overlay primary + DO Console (web) as break-glass + DO Recovery Console as deeper break-glass + DO Snapshot as ultimate.

**Why:**
- Lowest operator overhead; rotating-ISP-IP tolerance built in (key constraint for Awwal's Nigerian residential ISP)
- Free Personal tier covers operational scale (up to 100 devices, 3 users)
- Cloudflare ZT viable alternative if vendor consolidation with Cloudflare WAF (when domain lands) makes sense later
- WireGuard self-hosted rejected — overkill for 1-operator-3-device scenario
- DO Console alone rejected — clunky for daily ops, Console itself has SSH-port dependency

**Captured in:** ADR-020

### Option C — V2 Baseline Report (full refresh, gated submission) (2026-04-22)

**Decision:** Write Baseline Report v2 as fresh file (`_bmad-output/baseline-report/BASELINE-STUDY-REPORT-V2.md`) styled with new modern design system. Hold submission until Deliverable Commitment Matrix verifies every claim true.

**Why:**
- v1 from March 2026 (Chemiroy 22-chapter doc) is structurally sound but stale (Security Hardening Phase 2, Epic 9 work, Monday incident all post-date it)
- Awwal's editorial preference: write as if delivered, hold until true (Nigerian gov-deliverable pattern)
- v1 stays untouched (preserves history)

**Captured in:** SCP §2.4 implicit, runbook §6 follow-ups, future deliverable backlog

### Scope lock after SCP (2026-04-22)

**Decision:** No further trigger absorbed inline; second `correct-course` invocation required for any new scope.

**Why:** Six change streams in one session was already pushing limits of disciplined absorption. Without lock, every conversation grew the backlog.

**Captured in:** SCP §3.4

---

## Operational lessons (memorise these)

### Lesson 1: sshd_config drop-in override "first value wins"

`/etc/ssh/sshd_config.d/*.conf` are loaded in alphanumeric order. SSH config uses **first-match-wins** semantics. A directive in `50-cloud-init.conf` overrides the same directive in `60-cloudimg-settings.conf` AND in the main `sshd_config`. **Always grep all drop-ins after editing sshd_config.**

```bash
sudo grep -rE "^(PermitRootLogin|PasswordAuthentication|PubkeyAuthentication)" /etc/ssh/sshd_config.d/
```

### Lesson 2: Ubuntu SSH service name is `ssh`, not `sshd`

Debian/Ubuntu uses `ssh.service`. Red Hat / CentOS / Fedora use `sshd.service`. Always:

```bash
sudo systemctl reload ssh         # Ubuntu
sudo systemctl reload sshd        # RHEL family
```

### Lesson 3: DO Console is SSH-based, not hypervisor-OOB

DO Web Console works by establishing an SSH session from DO's own infrastructure IP ranges (e.g. `162.243.0.0/16`) to your droplet's port 22, then bridging to noVNC. **If you firewall SSH to operator-only sources, Console breaks** — and the failure mode looks like browser/ISP filtering, not firewall blocking.

DO Recovery Console likely shares this dependency (verification pending).

True hypervisor-OOB break-glass: **DO Snapshot restore** (always works regardless of firewall).

### Lesson 4: PM2 startup hook is required for survive-reboot

By default PM2 does not auto-start on boot. Run once:

```bash
pm2 startup    # prints a sudo command — copy and run it (registers pm2-root.service via systemd)
pm2 save       # snapshots current process list as resurrection set
```

After reboot, `systemd` brings up `pm2-root.service` which runs `pm2 resurrect` from the saved dump.

### Lesson 5: Docker containers need explicit restart policy

Default `docker run` policy is `no` (don't restart). For production:

```bash
docker update --restart=unless-stopped <container>...
```

`unless-stopped` survives reboots but respects manual `docker stop`. Alternative `always` survives even manual stops.

### Lesson 6: GitHub Actions deploys cannot live inside a tailnet-only firewall

`appleboy/ssh-action` runs from GitHub-hosted runner public IPs. Tailnet-only firewall blocks all CI deploys. Either:
- Widen firewall (Option A) — accept defence-in-depth degradation
- Move CI to self-hosted runner inside tailnet (Option B) — more work, cleaner

### Lesson 7: Postal-gov reports — "write as if delivered, hold until true"

Awwal's stated pattern for Ministry-facing deliverables: write the report assuming completion, gate the actual submission on a Deliverable Commitment Matrix verifying every claim. Don't ship aspirational claims; don't ship half-done either.

### Lesson 8: BMAD correct-course workflow is the discipline restorer

When scope decisions accumulate in conversation without flowing through canonical artifacts, run `correct-course` (incremental mode). Outputs a Sprint Change Proposal that cleanly routes to PM → Architect → UX → SM in sequence. Resists the "absorb scope inline" temptation that breaks downstream traceability.

---

## Team-voice rhythm

Six personas used consistently throughout the session. Helpful when asking them to weigh in:

- **Bob (Scrum Master)** — orchestration, sequencing, scope-lock policing
- **Charlie (Senior Dev)** — technical realism, architectural callouts, code-grounded analysis
- **Dana (QA Engineer)** — verification rigour, negative tests, hygiene catches
- **Fiona (Business Analyst)** — strategic + scope critique, capacity-reality challenges
- **Gabe (Tech Writer)** — documentation strategy, audience-tone calibration, copy review
- **Iris (Legal & Compliance Counsel)** — NDPA exposure, Transfer Protocol drafting, contract gaps

Add when needed:
- **Elena (Junior Dev)** — naive-question flusher (catches over-confident assumptions)

---

## Outstanding work at session close (2026-04-25 evening)

### Field Readiness Certificate — current state

| # | Item | Status |
|---|---|---|
| 1 | Tailscale live + SSH public port closed at sshd-key-only | ✅ Done 2026-04-23 |
| 2 | Story 11-1 schema + Akintola-risk composite indexes | ⏳ Awaiting Bob's create-story |
| 3 | Story 9-12 Public Wizard + Pending-NIN + NinHelpHint + Magic-Link Email | ⏳ Awaiting Bob's create-story |
| 4 | prep-input-sanitisation-layer | ⏳ Awaiting Bob's create-story |
| 5 | Alerting tier with at least one push channel (Story 9-9 subset) | ⏳ Awaiting Bob's create-story |
| 6 | Operations Manual enumerator-section (D4 subset) | ⏳ Drafting needed |

### Handoff chain — next steps

| # | Action | Status |
|---|---|---|
| A.1 | John (PM) — PRD V8.2 | ✅ Done 2026-04-23/24 |
| A.2 | Winston (Architect) — architecture revision + 3 ADRs | ✅ Done 2026-04-24 |
| A.3 | Sally (UX Designer) — UX spec revision + Journeys 5–8 + 6 components | ✅ Done 2026-04-25 |
| A.4 | PM + Architect joint — `create-epics-and-stories` | ⏳ Next |
| A.5 | Bob (SM) — `create-story` per story | ⏳ After A.4 |
| A.6 | Dev agents — implementation per priority order | ⏳ After A.5 |

### Loose ends from runbook §6.1

- ✅ Take DO Snapshot (`pre-os-upgrade-2026-04-25` + `clean-os-update-2026-04-25` both taken)
- ⏳ Reset + save VPS root password to password manager (do once DO Console fully reliable — currently working post-2026-04-25 firewall widening)
- ⏳ Clean up `github_actions_deploy` private key from laptop (should only live in GitHub Secrets — hygiene follow-up)

### Story 9-9 remaining subtasks

Tailscale ✅ + OS patching ✅ done. 8 remaining:

- Port audit (`ss -tlnp`, restrict Portainer)
- App-layer rate-limit audit on `/auth/*`
- Backup client-side encryption + quarterly restore drill
- Alerting tier (SMS / WhatsApp / paged) for CRITICAL
- Logrotate for PM2 + journalctl retention
- Second super-admin break-glass account
- SOC-style activity baseline / SSH log differentiation
- Cloudflare WAF/CDN (domain-gated on `oyoskills.com` purchase)

### Backlog parked items (named-but-not-in-current-SCP-scope)

- `prep-query-performance-gate` — CI EXPLAIN ANALYZE gate (Move 4 of Akintola-risk mitigation; moved to next SCP / sprint)
- `self-hosted-github-actions-runner` — Story 9-9 follow-up to allow re-narrowing firewall
- `phone-on-tailnet` — runbook §6.1 follow-up (single-point-of-failure mitigation)

### Future sessions should look at

1. PM2 ↺ counter trend over next 24/48/72h — observability window for Story 9-10
2. Cloudflare onboarding once domain lands
3. Verify DO Recovery Console actually works as break-glass (next quarterly drill)
4. ITF-SUPA partnership conversation with Ministry (deferred per Awwal)

---

## Files touched / created in this session

### Planning artefacts (canonical BMAD scope)

- `_bmad-output/planning-artifacts/sprint-change-proposal-2026-04-22.md` — created, includes §7 Completion Log + Appendix A.1/A.2/A.3 prompts
- `_bmad-output/planning-artifacts/prd.md` — V7.8 → V8.2 (John)
- `_bmad-output/planning-artifacts/architecture.md` — +679 lines incl. ADRs 018/019/020 (Winston) + ADR-020 corrections
- `_bmad-output/planning-artifacts/ux-design-specification.md` — +1137 lines (Sally)
- `_bmad-output/implementation-artifacts/sprint-status.yaml` — 7 new comment blocks + Epic 10/11 entries + new story keys

### Implementation artefacts

- `_bmad-output/implementation-artifacts/11-1-multi-source-registry-schema-foundation.md` — created, 11 ACs (incl. AC#11 composite index audit)
- `_bmad-output/implementation-artifacts/9-9-infrastructure-security-hardening.md` — Change Log entry + File List subtask block

### Operational documentation

- `docs/emergency-recovery-runbook.md` — created (8 sections + panic-start + 2 amendments incl. DO Console finding)
- `docs/transfer-protocol-template.md` — created (D2)
- `docs/epic-10-1-consumer-auth-design.md` — created (with Awwal's Section 10 answers)
- `docs/session-2026-04-21-25.md` — this file

### Memory updates (in `~/.claude/projects/C--Users-DELL-Desktop-oslrs/memory/`)

- `MEMORY.md` — pointer added to this session-notes file + state updates
- New topical files for stable findings (DO Console SSH-based, BMAD correct-course pattern)

---

*Session closed at PM2 ↺=0, Tailscale active, kernel 6.8.0-110, 0 OS updates pending, app health green. Strong infra posture entering the field-survey window.*

---

## Late-Saturday additions (after initial session-notes write)

After the persistence pass landed, the session continued with three more substantive items:

### Verification — CI deploy chain proven working twice

- **Commit `096987e`** (the SCP persistence commit, 29 files, 5249 insertions) pushed to `main`. Triggered first post-firewall-change CI run. Result: ✅ green. Evidence captured in this session: deploy step ran `appleboy/ssh-action@v1.2.5` from GitHub-hosted runner public IP → reached VPS firewall (Option A widened) → sshd accepted `github_actions_deploy` key from GitHub Secrets → ran the deploy script (drizzle schema sync, audit-log immutability migration, web build via vite + PWA, nginx config syntax check, PM2 restart). All steps green. Deploy confirmation message: `✅ Successfully executed commands to all hosts.`
- **Commit `1010d64`** (`.gitignore` chore: ignore session diagnostic scratch files + local test prep) pushed. Triggered second confirmation CI run. Also ✅ green. Pipeline durably working.
- Two CI runs in 24h validated: (a) firewall widening to `0.0.0.0/0` permits GH Actions runners, (b) sshd hardening still accepts the deploy key, (c) post-OS-upgrade nginx + PM2 + Drizzle integration all functional.

### github_actions_deploy laptop cleanup completed

- Pre-flight: confirmed key still on laptop (`Test-Path` returned `True`), confirmed GitHub Secrets has `SSH_PRIVATE_KEY`, confirmed CI deploys work without laptop reliance.
- Executed `Remove-Item` on both `github_actions_deploy` (private, 411 bytes) and `github_actions_deploy.pub` (public, 103 bytes) from `C:\Users\DELL\.ssh\`.
- Bonus: also removed orphan `config.txt` (145 bytes) — leftover from Apr 23 Notepad save when SSH config was first created. Only `config` (no extension) is what SSH actually reads.
- Final laptop `.ssh/` listing: `config` + `id_ed25519` + `id_ed25519.pub` + `known_hosts` + `known_hosts.old`. Five files, no orphans.
- Deploy key now exists ONLY in GitHub Secrets (canonical) and on VPS as the public-key half in `authorized_keys` line 1. Hygiene complete; quarterly rotation pattern from runbook §4 takes over from here.

### VPS root password — retained, not reset

- Awwal opted to retain the original root password set at DO droplet creation (assessed as strong). Declined the §6.1 reset action.
- Password lives in his password manager — DO Console access path remains functional whenever Console is needed for break-glass.
- Quarterly rotation cadence (runbook §4) still applies.

### .gitignore patterns added

- New patterns inside existing `# Temp files` block: `new_err*.txt`, `new_erroor*.txt`, `ssh_analysis*.txt` (wildcarded so future scratch files in same families auto-ignored).
- New block `# Local test prep — not part of OSLRS`: `aptitude-tests/`.
- Working tree `git status` now silent on the 4 leftover items.

### A.4 invoked

- John (PM) invoked in fresh CLI session via SCP Appendix A.4 prompt to run `bmad:bmm:workflows:create-epics-and-stories`.
- Output: amended `epics.md` formalising Epic 10 + Epic 11 + Epic 9 updates (9-10/11/12 added; 9-9 expanded with Tailscale + OS-patching subtasks marked DONE) + `prep-input-sanitisation-layer` standalone prep task.
- Verification pending at session close. After A.4 lands and is verified, A.5 (Bob's `create-story` per story) is the next handoff.

### What remains genuinely open at session-final close

| # | Item | Owner | Notes |
|---|---|---|---|
| 1 | Verify A.4 output (`epics.md`) | Claude (next interaction) | After John signals done |
| 2 | Draft A.5 prompt into SCP Appendix A.5 | Claude (next interaction) | After A.4 verified |
| 3 | Bob runs `create-story` per story | Awwal in fresh CLI | After A.5 prompt landed |
| 4 | Dev implementation begins | Awwal + dev agents | After stories filed |
| 5 | Field Readiness Certificate items 2–6 | Per-story owners | All gated on Bob's create-story |
| 6 | Story 9-10 PM2 trend observation | Awwal | 24/48/72h checkpoints from 2026-04-25 08:54 UTC |
| 7 | Phone on tailnet | Awwal | Single-point-of-failure mitigation (runbook §6.2) |
| 8 | DO Recovery Console verification | Awwal next quarterly drill | First drill due 2026-07-23 per runbook §7 |
| 9 | Self-hosted GH Actions runner | Story 9-9 follow-up | Pre-condition for re-narrowing firewall back to tailnet-only |

### Cosmetic items NOT fixed (acceptable as-is)

- Laptop `~/.ssh/config` has 1-space indent on `IdentityFile` line instead of 4 (typo in `Set-Content -Value` array). SSH ignores indentation — functionally fine. Skipped.
- Winston's architecture amendment header dated 2026-04-25 (forecast tomorrow when written 2026-04-24). Inconsequential.

---

*Final close-out 2026-04-25 evening. Three loose ends from runbook §6.1 all resolved (snapshot ✅, password ✅-decided-to-retain, laptop key cleanup ✅). Two CI runs green. SCP handoff chain at 4/6 complete (A.1/A.2/A.3 done + A.4 in flight). Persistence layer covers everything from this 5-day arc.*

---

## Day 6 — Sat→Sun→Mon arc continuation (2026-04-25/26)

The "5-day session" extended into a 6th day of substantive close-out work after the persistence pass landed. Key themes: A.5 Bob create-story chain, Epic 9 review, hand-off strategy crystallisation, project email architecture decision, terminology hygiene.

### A.4 + A.5 chain completion

- **A.4 PM (`create-epics-and-stories`)** — John executed; epics.md grew 2341 → 2817 lines (+476). Epic 10 (6 stories), Epic 11 (4 stories with 11-1 pre-existing), Stories 9-10/11/12 in Epic 9, prep-input-sanitisation-layer all formalised. John flagged two scope items for follow-up: (a) ADR-020 may need Decision-section refresh to match dual-source firewall state (Winston's domain), (b) SCP §A.4 wording "9 remaining 9-9 subtasks" was off-by-one; epics.md correctly shows 8 (OS patching landed 2026-04-25).
- **Architecture V8.2-a1 follow-up amendment** — Winston rewrote ADR-020 Decision section + Decision 2.7 + ADR-013 Tailscale subsection + Pattern Category 7 Operator SSH block to dual-source firewall + sshd-as-primary-control + fail2ban-as-load-bearing. New ADR-020 §"DO Console Access Vector" subsection. Documentation now mutually consistent across PRD V8.3 + architecture V8.2-a1 + runbook §1.1/§1.4/§2.2/§6.1.
- **A.5 Bob (`create-story` × 14)** — Bob filed 15 story files (off-by-one in SCP count; actual: 9-9 regen + 11-1 regen + 13 net-new = 15). Defensive scope expansions noted: 9-11 added 4th composite index `(consumer_id, created_at)` for forward Epic 10 cross-cut; 10-1 added 10-5 as hard dependency (DSA precondition for PII scope); 11-3 reuses 9-12's WizardStepIndicator (cross-story component). All within bounds.
- **Runbook stale-row cleanup** — §1.1 access-paths table had stale rows (Snapshot "Not yet taken" + Public IP SSH "Disabled") despite §1.4 being correctly updated by Bob during A.5. Patched 2026-04-25 evening.

### Hand-off strategy decision: TURNKEY PACKAGE (2026-04-26)

Pivotal session within the larger session. Awwal articulated as Project Lead for the Consultant: "asking for a `.oyostate.gov.ng` domain is a lot of hassle in Oyo State (the bureaucratic problem in a third world country ICT Procurements process since it is not the State ICT Team that is saddled with the design and development process)". Decision adopted:

- **Builder personally funds the missing project assets:** `oyoskills.com` domain (~₦15K/year at Namecheap/Porkbun) + project SIM (~₦500 + ~₦5K/mo airtime).
- **Builder provisions everything end-to-end:** domain, Cloudflare account on free tier, Email Routing forwarders, Resend domain verification, project phone with WhatsApp Business.
- **At Transfer Day, Ministry receives a turnkey package** — domain + Cloudflare account + DO account + Tailscale tailnet + GitHub Org + SIM card + all credentials + all documentation. One orchestrated session (~2 hours) replaces the previously-planned multi-month parallel-migration sequence.
- **Iris (legal):** turnkey is STRONGER for BOT compliance than partial migration — every asset traces to one source at signature.
- **Fiona (BA):** out-of-pocket bounded (<₦100K total project lifetime), recoverable via D2 §6 retainer reimbursement clause OR written off as goodwill cost.

This decision **supersedes** the earlier `oslsr.handover@gmail.com` bridge-Gmail plan and the Ministry-Workspace-coordinated migration plan.

### Project email architecture decision (2026-04-26)

Awwal asked: "How do I create the `info@oyoskills.com` email address as I don't have any money to purchase anything again?" Free stack adopted:

```
Domain registrar (Namecheap/Porkbun) → Cloudflare DNS (free tier)
   → Cloudflare Email Routing (free) — receives info@/admin@/support@/awwal@/noreply@oyoskills.com
   → Forwards to Builder's Gmail during Operate phase
   → Resend (already in budget) sends FROM info@oyoskills.com / noreply@oyoskills.com when domain verified
   → Gmail "Send mail as" lets Builder compose AS info@oyoskills.com via Resend SMTP
```

**Net cost: zero additional beyond the domain.**

**Critical pattern: `admin@oyoskills.com` as the canonical migration anchor.** Every project SaaS account (DO, Tailscale, hCaptcha, GitHub Org, Resend, Cloudflare itself) registered to `admin@oyoskills.com` from creation. At Transfer Day, ONE change — flipping the Cloudflare Email Routing forwarder destination from Builder Gmail to Ministry-provided email — migrates ALL SaaS account ownership in a single operation. No per-account migration ceremony.

This pattern collapses what was a 12-step parallel migration into 3 Transfer-Day operations:
1. Transfer Cloudflare account to Ministry
2. Flip forwarding destinations
3. Transfer DO + Tailscale tenancies

### DO/Domain/Cloudflare connection architecture (3-phase, 2026-04-26)

Documented as three independently-shippable phases:

- **Phase 1 — Email-only on `oyoskills.com` (no website migration yet)** — Cloudflare DNS + Email Routing only; gets `info@oyoskills.com` working without touching DO. Ships now.
- **Phase 2 — DNS-Only website (grey cloud)** — Add A records, expand Let's Encrypt cert to cover both `oyotradeministry.com.ng` + `oyoskills.com`, dual-domain nginx server_name. Stories 9-2 + 9-4 territory.
- **Phase 3 — Cloudflare Proxied (orange cloud)** — Story 9-9 Cloudflare WAF subtask. Origin keeps Let's Encrypt; Cloudflare SSL/TLS mode = Full (strict).

Each phase is independently useful. Phase 1 is the immediate pre-field-survey work.

### Epic 9 stories 9.1–9.8 verdict (2026-04-26)

Verdict request from Awwal. Aggregate: 5/8 done, 2/8 deferred-by-design, 1/8 in-progress.

| # | Verdict |
|---|---|
| 9-1 | Done, healthy |
| 9-2 | Deferred — domain-blocked; scope reduced ~80% by 9-5 |
| 9-3 | Done, low-risk polish |
| 9-4 | Deferred — pure DNS/ops; collapses with 9-2 |
| 9-5 | Done — **underrated; high-leverage** (centralized to env vars; future domain change = config edit) |
| 9-6 | Done — same-day hotfix for 3 supervisor analytics bugs |
| 9-7 | Done — comprehensive forward-fix; A grade on securityheaders.com; bonus discovery (Helmet 404 fallback CSP) |
| 9-8 | In-progress — **the one to push next.** Report-Only since 2026-04-11 (15+ days); single-line nginx flip to enforcing. Promotion checklist documented at `docs/9-8-csp-enforcement-promotion-checklist.md` (created 2026-04-26). |

### Three artefacts produced 2026-04-25/26

| File | Purpose |
|---|---|
| `docs/account-migration-tracker.md` | Living operational tracker; refactored 2026-04-26 for turnkey strategy. Tracks all SaaS accounts + email forwarders + domain + SIM. |
| `docs/transfer-protocol-schedule-1-asset-enumeration.md` | Legal-shaped Schedule 1 of D2; refactored 2026-04-26. 52 assets enumerated across 9 categories. |
| `docs/9-8-csp-enforcement-promotion-checklist.md` | Single-line nginx change procedure to close Story 9-8. 6 sections + rollback. |

### Terminology decision (2026-04-26)

Register vs Registry vs Registration disambiguation explicitly captured in this session, motivating creation of `docs/oslsr-glossary.md`. Five canonical OSLRS terms documented with usage rules:

1. **Registry** (capital R, proper noun) = institutional system + Ministry asset (what gets transferred)
2. **register** (verb) = the user action of enrolling
3. **registration** = either the act OR the resulting status (`respondents.status` enum)
4. **register** (noun, less formal) = the underlying list
5. **registrant** = NOT used in OSLRS; we say "respondent"

Glossary also disambiguates respondent/user/citizen, the four field roles (enumerator/clerk/supervisor/assessor), NIN/BVN/Phone (NDPA legal distinction), tailnet/VPN/firewall.

### Session 6-day arc finally closes 2026-04-26

| Track | State at Day 6 close |
|---|---|
| Sprint Change Proposal handoff chain | A.1 ✅ A.2 ✅ A.3 ✅ A.4 ✅ A.5 ✅; A.6 (Dev `dev-story` × 15) — pending Awwal invocation |
| Field Readiness Certificate | 1/6 items complete (Tailscale ✅) |
| Loose ends from runbook §6.1 | All 3 closed (snapshot ✅ + password retained ✅ + laptop cleanup ✅) |
| Hand-off strategy | Turnkey package adopted; `admin@oyoskills.com` anchor pattern; 49 assets enumerated in Schedule 1 |
| Email architecture | Free Cloudflare DNS + Email Routing + Resend + Gmail Send-As stack documented |
| Terminology hygiene | Glossary drafted (Schedule 6 of D2 once finalised) |
| CI deploy chain | Validated twice (commits `096987e` + `1010d64`) |
| Outstanding immediate operational work | (a) Buy `oyoskills.com` + SIM; (b) Set up Cloudflare DNS + Email Routing; (c) Push 9-8 to enforcing per checklist; (d) PM2 ↺ trend observation 24/48/72h |
| Outstanding planning work | A.6 invocation prompt; eventual delivery-track items (Baseline Report v2, knowledge transfer log, Operations Manual D4, DPIA D1) |
