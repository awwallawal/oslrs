# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
coverage/

# Environment & Secrets
.env
.env.*
!.env.example
*.pem
*.key
credentials.json
secrets.json

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# OS & Editor
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# Docker
docker-data/
postgres_data/
redis_data/

# Turbo
.turbo/

# Test artifacts
vitest-report.json
.vitest-live*.json
.vitest-config-loaded
test-pipeline.html
reporter*.log

# Playwright
playwright-report/
test-results/
apps/web/e2e/.auth/
apps/web/e2e/.wizard-resume-tokens.json

# Temp files
tmpclaude-*
new_error.txt
new_errorr.txt
new_errorrr.txt
new_err*.txt
new_erroor*.txt
ssh_analy*.txt
nul
api-test-output.txt
debug.log
full-test.log
table_discussion.txt
test-output-v2.txt
test-run-output.txt
# Scratch files at repo root (baseline-report tooling, dev experiments, etc.)
_tmp_*

# Redundant 9-58 master-form working copy (email-question rebuild base) — git history is the backup; not for commit.
test-fixtures/oslsr_master_v3_email.xlsx

# BMAD operator scratch — probe scripts, ad-hoc evidence dumps. NOT for commit.
_bmad-output/scratch/

# Slide-presentation working folder (SABER decks etc.) — side job, unrelated to OSLRS development.
_bmad-output/slide-presentation/

# Build artifacts (generated)
*.tsbuildinfo
apps/web/vite.config.d.ts
apps/web/vite.config.js

# Claude Code local settings
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Unrelated/personal docs
docs/agrikonsult_ffs_handout.html
docs/agrikonsult_ffs_presentation.html
docs/create_presentation.py
docs/sosina.txt
docs/Retrospective Workflow bmadbmm.txt
docs/Lawal Awwal*
docs/cv-awwal.html
docs/PageSpeed Insights*
docs/SKILLED LABOUR REGISTER ACTION PLAN.xlsx

# Local test prep — not part of OSLRS
aptitude-tests/

# Briefing HTML intermediates — regenerable from .md via scripts/render-briefing-pdf.mjs
docs/briefings/*.html

# Production data snapshots (PII) — never commit
docs/vps-snapshots/
docs/emails-sent-*.csv

# Session continuity log — narrative bridge between Claude Code sessions.
# Append-only across days. Never committed; mirrors local working memory.
# Cold-start checklist + open commitments + per-session notes live here.
# If working directory is shared (e.g. another machine), recreate from scratch.
SESSION_LOG.md

# Story 13-3 — Cloudflare Pages local wrangler state (never commit)
.wrangler/

# Saved Claude Code session transcript (scratch working memory) — never commit
zzzzzzzzzz.txt
